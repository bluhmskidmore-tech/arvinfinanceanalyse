# Stock Analysis 功能与性能优化基线（2026-07-18）

## 范围与固定协议

- 页面：`/stock-analysis`
- 工作流：首屏 workbench、候选复核队列、深度研究按需加载
- 固定参数：默认 `as_of_date`、`top_k=10`、`sector_window_days=20`、`include=main,evidence_summary`
- 浏览器：Chromium 148.0.7778.96，Windows x64，Node 24.13.0
- 样本：桌面冷启动 5 轮、同上下文暖返回 5 轮、移动冷启动 5 轮
- 移动约束：390×844、4× CPU、150ms RTT、1.6Mbps 下行、750Kbps 上行
- 后端：7 组进程内冷/暖交错样本；冷样本仅清空 workbench 进程内响应缓存
- 基准提交：`855311a7f51aa459afaed2b8a0c141ac29edb58c`；工作区存在未提交的并行修改

## 根因

1. 首屏在 workbench 成功后立即请求 `signal-confluence`，违反“首屏只请求聚合包、重型诊断按需加载”的页面边界。
2. 股票页从 `api/client` 静态导入 `useApiClient`；调用市场数据方法时又落入总客户端，导致首屏下载余额、债券、损益等无关域客户端。
3. 页面生产 CSS 为 640,412B，首屏 CSS 覆盖率约 24.03%；这是后续最大资源热点，但样式规则跨首屏、深度研究和大量覆盖层交织，当前不做无证据删除。
4. 后端不是稳态瓶颈：进程预热后 workbench 冷缓存约 10ms、暖缓存约 2.35ms，线上缓存命中约 1–2ms。

## 本轮改动

- 首屏不再自动请求 `signal-confluence`；用户打开深度研究后才启用该查询和对应 endpoint 状态。
- `clientContext` 为股票分析/Livermore 方法新增按需 market-data 域客户端分流。
- 股票页改从轻量 `clientContext` 导入 `useApiClient`，移除总客户端静态依赖。
- 新增可复跑的生产浏览器采样器、CSS 覆盖率能力和后端 7 轮 profiler。
- 原有闭环结论、阻断、回放和错误态测试改为先打开深度研究，再保持原断言不变。

## 浏览器前后对比（中位数）

| 指标 | 优化前 | 优化后 | 变化 |
| --- | ---: | ---: | ---: |
| 桌面冷启动 ready | 822.44ms | 543.22ms | -34.0% |
| 桌面 FCP | 104ms | 84ms | -19.2% |
| 桌面 LCP | 704ms | 540ms | -23.3% |
| 桌面最长任务 | 268ms | 184ms | -31.3% |
| 暖返回 ready | 121.96ms | 90.54ms | -25.8% |
| 移动冷启动 ready | 22,319.41ms | 19,890.15ms | -10.9% |
| 移动 LCP | 22,212ms | 19,816ms | -10.8% |
| 移动 FCP | 4,944ms | 4,940ms | 基本不变 |
| 首屏传输字节 | 4,053,664B | 3,569,783B | -11.9% |
| 首屏解码字节 | 4,039,264B | 3,558,983B | -11.9% |
| 首屏 API 字节 | 116,712B | 108,744B | -6.8% |

所有 15 个优化后样本均满足：

- 冷启动恰好 1 个 workbench 请求
- 首屏观察窗口内 0 个重型诊断请求
- 暖返回 0 个 workbench 请求
- 0 个 HTTP 失败、控制台错误或横向溢出

## 后端 Gate E

- 第一个未初始化进程冷样本：15,349.461ms，其中 compute 15,042.446ms；这是一次性初始化成本。
- 后续进程内冷缓存样本：约 7.8–11.85ms。
- 暖缓存中位数：2.351ms；缓存读取中位数：1.028ms。
- 14 个冷/暖样本去除 `generated_at` 和 `trace_id` 后只有一个规范化输出哈希。
- 启动预热服务已包含默认 stock-analysis workbench，因此不新增后端生产优化，避免为稳态约 10ms 的路径增加复杂度。

## 验证

- 285/285 定向前端测试通过。
- 5/5 新行为相关场景通过：闭环通过、闭环阻断、降级暂停、回放边界、共振错误态。
- `npm run typecheck` 通过。
- 目标文件 ESLint 通过。
- `npx vite build` 通过。
- 生产浏览器正式 5+5+5 轮通过。
- 后端 profiler 7 组冷/暖样本通过，输出哈希稳定。

## 外部阻塞与剩余风险

- `npm run build` 被未修改的 `riskHomeAdapter.ts:626` 期限类型错误阻塞；页面生产包已由 `vite build` 成功生成。
- `npm run debt:audit` 仅因未修改的 `frontend/src/api/contracts.ts`（7510/7429）和 `backend/app/services/pnl_service.py`（4218/4203）超基线失败；本轮未增加这些债务。
- GitNexus 对整个未暂存工作区检测到 139 个文件、661 个符号、29 条流程，风险为 CRITICAL；这是混合工作区结论，不代表本轮股票页改动。`createDeferredApiClient` 编辑前影响分析为 MEDIUM（5 个直接测试、0 条业务流程），`StockAnalysisPage` 为 LOW。
- 最大剩余页面热点转为全局 Ant Design/vendor 启动链；它跨越多页面，未在本轮改动。

## CSS 按需拆分追加结果

- 仅抽取每个选择器分支都受 `.stock-analysis-page__deep-zone`、`.stock-analysis-page__deep-review-workspace` 或股票选择工作区根节点约束的规则。
- 共延迟 420 条规则；源码首屏 CSS 从 717,081B 降到 584,680B。
- 生产首屏 CSS 从 640,412B（gzip 60.68KB）降到 521,542B（gzip 53.20KB）；深度研究首次展开时再加载 118.94KB（gzip 8.01KB）。

| 追加指标 | 拆分前 | 拆分后 | 变化 |
| --- | ---: | ---: | ---: |
| 首屏传输字节 | 3,569,783B | 3,450,966B | -118,817B / -3.3% |
| 移动冷启动 ready | 19,890.15ms | 19,296.60ms | -3.0% |
| 移动 LCP | 19,816ms | 19,236ms | -2.9% |
| 暖返回 ready | 90.54ms | 76.96ms | -15.0% |

桌面与移动首屏、深度研究主体共 6 张前后截图中，4 张完全逐像素一致；另外 2 张仅页面顶部 5–27px 的动态全局状态条不同，页面内容区域无像素差异。

新增启动守卫确保主页面不静态导入深度研究 CSS，延迟组件负责导入；页面定向测试 191/191、组合测试 273/273 通过。

剩余风险：桌面冷启动短样本中位数受本机调度波动影响较大，因此本轮保留依据以确定性的首屏资源减少、移动限速收益和视觉等价性为主，不声称桌面时延有稳定追加收益。
