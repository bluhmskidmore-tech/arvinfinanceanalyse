# W5.5-fe · liability-analytics 前端 Numeric 落地

**对应：** 后端 W5.5 commit `d270d26`（`backend/app/schemas/liability_analytics.py`）已迁 Numeric。
**参考模板：** W5.2-fe commit `9b699a8`（cashflow）、W5.4-fe commit `cb75bc6`（pnl-bridge）——两者都是 composer-2 交付，**直接 `git show` 看它们的 diff 学模式**，不要我再复述。

---

## Subagent 约束

- 白名单之外不得修改；越界 `BLOCKER:` 报告
- 禁 `git push` / `git reset --hard` / `git commit --amend` / `git rebase`
- 禁改 `package.json` / `pyproject.toml` / `frontend/src/utils/format.ts` / `tone.ts` / `DataSection*`
- 禁跑 Python 测试（纯前端 task）
- Windows PowerShell：管道 `Select-Object -Last N`；**不**用 head/tail
- 单次 commit，message 模板：`feat(liability-analytics): migrate contracts + pages to Numeric view-model [W5.5-fe]`

## 白名单路径

**Modify：**
- `frontend/src/api/contracts.ts`（**仅** liability 相关块；约 2480-2587）
- `frontend/src/api/client.ts`（**仅** 4 个 mock 方法：`getLiabilityRiskBuckets` / `getLiabilityYieldMetrics` / `getLiabilityCounterparty` / `getLiabilitiesMonthly`；其他 mock 方法、real 分支、import 顶部都不碰除必要 import 外）
- `frontend/src/features/liability-analytics/pages/LiabilityAnalyticsPage.tsx`
- `frontend/src/features/liability-analytics/components/*.tsx`（仅当组件直接消费 Numeric-化字段时；消费基础 string 字段的部分保持原样）
- `frontend/src/features/liability-analytics/utils/money.ts` / `concentration.ts` / `nimStress.ts`（仅当迁移需要；尽量保留）

**Create：**
- `frontend/src/features/liability-analytics/adapters/liabilityAdapter.ts`
- `frontend/src/features/liability-analytics/adapters/liabilityAdapter.test.ts`（5 场景）

**可最小修改（仅当 API 变化使既有测试 red 时）：**
- `frontend/src/test/` 中任何 `Liability*.test.tsx`

**绝对不动：** contracts.ts 其他域；任何 backend；`mock-contract.test.ts`（W5.8 做）。

## 字段单位（你自己从后端 schema 读权威值）

- 自己 `Read backend/app/schemas/liability_analytics.py` 里每个 class 的 `_NUMERIC_FIELDS: ClassVar[dict[str, tuple[NumericUnit, bool]]]`，**直接抄**到 adapter/mock 的 unit/sign_aware
- 不要自己猜

## 前端当前 shape（自己 grep 定位）

contracts.ts 里 `LiabilityBucketAmountItem` / `LiabilityNameAmountItem` 等目前用 `number | string | null` 三元 union（历史迁移遗留）。目标：governed amount / rate / cost 字段统一到 `Numeric | null`（null 表示 optional），`number` 彻底去掉。

`LiabilityYieldKpi` 的 `asset_yield / liability_cost / market_liability_cost / nim` 目前是 `number | null`（小数 ratio 语义）→ 翻 `Numeric | null`。

`LiabilityCounterpartyItem.value / weighted_cost / weighted_rate` / `LiabilityCounterpartyTypeSlice.value` / `LiabMonthlyXxxItem.avg_* / proportion / mom_change / mom_change_pct` 同理。

## 验收（`git diff --cached --stat` 自检）

- [ ] tsc 0 error（整个 frontend）
- [ ] adapter 5 场景 PASS
- [ ] mock 的每个 governed 字段是 5-key Numeric dict 或 null
- [ ] 组件里 `parseFloat|toFixed|toLocaleString|Number\(` 对 governed 字段清零（`Number(integer count)` 允许）
- [ ] contracts.ts 其他域 0 改动

返回只需：文件清单 + tsc/vitest 尾部 + BLOCKER（如有）+ 遗留。**不要**复述本 prompt 内容。
