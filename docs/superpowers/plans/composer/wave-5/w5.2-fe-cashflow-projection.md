# W5.2-fe · cashflow-projection 前端 Numeric 落地

**目标一句话：** 把 `/api/cashflow-projection` 的前端链升到 Numeric：`contracts.ts` 3 个 type 从 `string` 翻到 `Numeric`；mock client 用 `formatRawAsNumeric` 构造；新建 `features/cashflow-projection/adapters/` 承担 5 场景；`CashflowProjectionPage.tsx` 只读 adapter view-model，`parseFloat()` 清零。

**所属波次：** Wave 5 · Stage B（前端域落地，对应后端 W5.2 已合入的 `backend/app/schemas/cashflow_projection.py`）
**依赖：** `numeric-correctness-w5-stage-a-done`（HEAD `5920827`）；后端 W5.2 commit `26f33e6` 已在 main
**预计工时：** 50-70 分钟（3 type 升级 + 1 个 mock + adapter/test + page refactor）
**对应 Spec：** § 3.3 / § 5 / § 10.1 的 5 场景
**参考模式：** W2.3 dashboard-adapter（commit `d49b0cd`）、W3.3 pnl-adapter（commit `ac0e70f`）

---

## Composer Subagent 约束（必须遵守）

你是一个专注执行单一 task 的 composer-2 subagent。

- 仅允许修改白名单文件
- 遇白名单外问题，用 `BLOCKER:` 前缀返回；不要擅自修改
- 不要 `git push` / `git reset --hard` / `git commit --amend` / `git rebase`
- 不要创建 git worktree
- 不要修改 `package.json` / `pyproject.toml` / 任何依赖声明
- 不要修改 `backend/**`（后端 W5.2 已落地，shape 已是 Numeric）
- 不要改动 `frontend/src/utils/format.ts` / `tone.ts`（Wave 1 固定）
- 不要跑全量 `python -m pytest -q`（会 hang）；本 task **只**跑 frontend vitest + tsc
- **Windows PowerShell 环境**：管道用 `Select-Object -Last N`；不用 Unix `head`/`tail`
- 完成后按"返回清单"5 项回复

---

## 允许修改的路径（白名单）

**Modify：**
- `frontend/src/api/contracts.ts`（**仅** lines 2282-2312 这一段 cashflow-projection 相关 3 个 type；其他部分**一字不动**）
- `frontend/src/api/client.ts`（**仅** `mock` 分支里的 `getCashflowProjection` 方法体；不要动 `real` 分支的任何行；也不要动其他 mock 方法）
- `frontend/src/features/cashflow-projection/pages/CashflowProjectionPage.tsx`（全量组件改造为只读 adapter VM）

**Create：**
- `frontend/src/features/cashflow-projection/adapters/cashflowProjectionAdapter.ts`
- `frontend/src/features/cashflow-projection/adapters/cashflowProjectionAdapter.test.ts`

**不得修改任何其他文件。** 特别：
- ❌ `frontend/src/api/contracts.ts` 的 cashflow 之外章节（bond-analytics / liability / pnl-bridge 等都有其他 W5.X-fe task 管）
- ❌ 任何后端文件
- ❌ `frontend/src/utils/format.ts` / `tone.ts`
- ❌ `frontend/src/components/**`
- ❌ `frontend/src/test/CashflowProjectionPage.test.tsx` 如存在；**仅**当新的 API 导致该测试 tsc/vitest 红时，用最小改动让它重新合规（禁止扩大测试范围或加新断言）
- ❌ `frontend/src/test/contract/mock-contract.test.ts`（W5.8 统一扩展）

---

## 背景上下文

### 后端 W5.2 现状

`backend/app/schemas/cashflow_projection.py`（commit `26f33e6`）已经把以下字段从 float 迁移到 `Numeric`：

- `CashflowMonthlyBucket`: `asset_inflow` / `liability_outflow` / `net_cashflow` / `cumulative_net` → `Numeric(unit="yuan", sign_aware=...)`
- `CashflowMaturingAsset`: `face_value` / `market_value` → `Numeric(unit="yuan", sign_aware=False)`
- `CashflowProjectionPayload`: `duration_gap` / `asset_duration` / `liability_duration` / `equity_duration` / `rate_sensitivity_1bp` / `reinvestment_risk_12m` → `Numeric`（单位依字段）

**wire 形态**：`pydantic.model_dump(mode="json")` 后，每个 governed 字段都是 `{raw, unit, display, precision, sign_aware}` 5-key dict。

### 前端现状

`frontend/src/api/contracts.ts` 2282-2312：

```ts
// --- 现金流预测 / 久期缺口 (`/api/cashflow-projection`) ---
export type CashflowMonthlyBucket = {
  year_month: string;
  asset_inflow: string;
  liability_outflow: string;
  net_cashflow: string;
  cumulative_net: string;
};

export type CashflowMaturingAsset = {
  instrument_code: string;
  instrument_name: string;
  maturity_date: string;
  face_value: string;
  market_value: string;
  currency_code: string;
};

export type CashflowProjectionPayload = {
  report_date: string;
  duration_gap: string;
  asset_duration: string;
  liability_duration: string;
  equity_duration: string;
  rate_sensitivity_1bp: string;
  reinvestment_risk_12m: string;
  monthly_buckets: CashflowMonthlyBucket[];
  top_maturing_assets_12m: CashflowMaturingAsset[];
  warnings: string[];
  computed_at: string;
};
```

`frontend/src/api/client.ts` `getCashflowProjection` mock（当前 5042-5061）：字段全是 `"1.25000000"` 这样的数字字符串，会 tsc 错。

`frontend/src/features/cashflow-projection/pages/CashflowProjectionPage.tsx`：

- line 91-93：`buckets.map((b) => parseFloat(b.asset_inflow))` 等 → 改读 `b.asset_inflow.raw ?? 0`
- line 203-219：`<KpiCard value={result.duration_gap} ... valueVariant="text" />` → 改为 `value={vm.kpis.durationGap.display}`（KpiCard text variant 收字符串；vm 里每个 KPI 是完整 Numeric）
- line 250 / 256：`{result.equity_duration}` / `{result.reinvestment_risk_12m}` → 改为 `{vm.kpis.equityDuration.display}` 等
- line 277-284：表格 `dataSource={result.top_maturing_assets_12m}` 的列 render；`face_value` / `market_value` 列需要加 `render: (v: Numeric) => v.display`

### 字段单位映射（必须严格遵守）

| 字段 | unit | sign_aware | 备注 |
|---|---|---|---|
| `CashflowMonthlyBucket.asset_inflow` | `yuan` | `False` | 流入非负 |
| `CashflowMonthlyBucket.liability_outflow` | `yuan` | `False` | 流出非负（原始值就是 +，不是 -） |
| `CashflowMonthlyBucket.net_cashflow` | `yuan` | `True` | 差可正负 |
| `CashflowMonthlyBucket.cumulative_net` | `yuan` | `True` | 累计可正负 |
| `CashflowMaturingAsset.face_value` | `yuan` | `False` | 面值 |
| `CashflowMaturingAsset.market_value` | `yuan` | `False` | 市值 |
| `CashflowProjectionPayload.duration_gap` | `ratio` | `True` | 单位"年"（设计书 § 3.3 ratio 适用） |
| `CashflowProjectionPayload.asset_duration` | `ratio` | `False` | 年 |
| `CashflowProjectionPayload.liability_duration` | `ratio` | `False` | 年 |
| `CashflowProjectionPayload.equity_duration` | `ratio` | `True` | 年（可负） |
| `CashflowProjectionPayload.rate_sensitivity_1bp` | `yuan` | `True` | 1bp 敏感度金额 |
| `CashflowProjectionPayload.reinvestment_risk_12m` | `pct` | `False` | 设计书 § 3.3 / 小数比率 |

**说明：** 如果后端 schema 里某字段标注与上表不一致（比如 `reinvestment_risk_12m` 标成 `ratio` 而非 `pct`），**以后端为准**，在你的 adapter/mock 里用后端声明的 unit。最终前端消费 `.display` 时不关心具体 unit。

### 接受/拒绝的修改面积

- ✅ 动 `contracts.ts` **仅** cashflow 3 个 type（2282-2312 一行、一字都不超出）
- ✅ 动 `client.ts` **仅** `getCashflowProjection` 一个方法（mock 分支那个；real 分支是 `requestJson<CashflowProjectionPayload>` 不用改）
- ✅ 动 `CashflowProjectionPage.tsx` 的全部实现（这是本域唯一消费者）
- ❌ 绝对不动 contracts.ts 的其他章节（别的域由别的 task 管）
- ❌ 绝对不动 backend

---

## 实现步骤

### Step 1：升级 contracts.ts 3 个 cashflow type

严格替换 2282-2312 行整个块为：

```ts
// --- 现金流预测 / 久期缺口 (`/api/cashflow-projection`) ---
export type CashflowMonthlyBucket = {
  year_month: string;
  asset_inflow: Numeric;
  liability_outflow: Numeric;
  net_cashflow: Numeric;
  cumulative_net: Numeric;
};

export type CashflowMaturingAsset = {
  instrument_code: string;
  instrument_name: string;
  maturity_date: string;
  face_value: Numeric;
  market_value: Numeric;
  currency_code: string;
};

export type CashflowProjectionPayload = {
  report_date: string;
  duration_gap: Numeric;
  asset_duration: Numeric;
  liability_duration: Numeric;
  equity_duration: Numeric;
  rate_sensitivity_1bp: Numeric;
  reinvestment_risk_12m: Numeric;
  monthly_buckets: CashflowMonthlyBucket[];
  top_maturing_assets_12m: CashflowMaturingAsset[];
  warnings: string[];
  computed_at: string;
};
```

（`Numeric` 在 contracts.ts 顶部已经 export，不需要重新 import。）

### Step 2：升级 mock `getCashflowProjection`

`client.ts` 5042-5061 替换为（基于既有 `formatRawAsNumeric` 辅助；它在 `frontend/src/utils/format.ts`）：

```ts
    async getCashflowProjection(reportDate: string) {
      await delay();
      return buildMockApiEnvelope(
        "cashflow_projection.overview",
        {
          report_date: reportDate,
          duration_gap: formatRawAsNumeric({ raw: 1.25, unit: "ratio", sign_aware: true }),
          asset_duration: formatRawAsNumeric({ raw: 3.8, unit: "ratio", sign_aware: false }),
          liability_duration: formatRawAsNumeric({ raw: 2.55, unit: "ratio", sign_aware: false }),
          equity_duration: formatRawAsNumeric({ raw: 5.2, unit: "ratio", sign_aware: true }),
          rate_sensitivity_1bp: formatRawAsNumeric({ raw: 125_000, unit: "yuan", sign_aware: true }),
          reinvestment_risk_12m: formatRawAsNumeric({ raw: 0.185, unit: "pct", sign_aware: false }),
          monthly_buckets: [],
          top_maturing_assets_12m: [],
          warnings: [],
          computed_at: new Date().toISOString(),
        },
        { basis: "formal", formal_use_allowed: true },
      );
    },
```

**注意：**
- `formatRawAsNumeric` 在 `client.ts` 顶部导入；如果未导入，追加 `import { formatRawAsNumeric } from "../utils/format";`（相对路径以 client.ts 实际位置为准）
- `monthly_buckets: []` / `top_maturing_assets_12m: []` 保持空数组（旧 mock 也是空数组；本 task **不**新增模拟数据；组件测试验 empty 分支即可）

### Step 3：创建 adapter `cashflowProjectionAdapter.ts`

在 `frontend/src/features/cashflow-projection/adapters/cashflowProjectionAdapter.ts` 创建：

```ts
import type {
  ApiEnvelope,
  CashflowMaturingAsset,
  CashflowMonthlyBucket,
  CashflowProjectionPayload,
  Numeric,
  ResultMeta,
} from "../../../api/contracts";
import type { DataSectionState } from "../../../components/DataSection";

export type CashflowBucketVM = {
  yearMonth: string;
  assetInflow: Numeric;
  liabilityOutflow: Numeric;
  netCashflow: Numeric;
  cumulativeNet: Numeric;
};

export type CashflowMaturingAssetVM = {
  instrumentCode: string;
  instrumentName: string;
  maturityDate: string;
  faceValue: Numeric;
  marketValue: Numeric;
  currencyCode: string;
};

export type CashflowProjectionVM = {
  reportDate: string;
  kpis: {
    durationGap: Numeric;
    assetDuration: Numeric;
    liabilityDuration: Numeric;
    equityDuration: Numeric;
    rateSensitivity1bp: Numeric;
    reinvestmentRisk12m: Numeric;
  };
  monthlyBuckets: CashflowBucketVM[];
  topMaturingAssets: CashflowMaturingAssetVM[];
  warnings: string[];
};

export type CashflowAdapterOutput = {
  vm: CashflowProjectionVM | null;
  state: DataSectionState;
  meta: ResultMeta | null;
};

export type AdaptCashflowInput = {
  envelope: ApiEnvelope<CashflowProjectionPayload> | undefined;
  isLoading: boolean;
  isError: boolean;
};

function mapBucket(b: CashflowMonthlyBucket): CashflowBucketVM {
  return {
    yearMonth: b.year_month,
    assetInflow: b.asset_inflow,
    liabilityOutflow: b.liability_outflow,
    netCashflow: b.net_cashflow,
    cumulativeNet: b.cumulative_net,
  };
}

function mapAsset(a: CashflowMaturingAsset): CashflowMaturingAssetVM {
  return {
    instrumentCode: a.instrument_code,
    instrumentName: a.instrument_name,
    maturityDate: a.maturity_date,
    faceValue: a.face_value,
    marketValue: a.market_value,
    currencyCode: a.currency_code,
  };
}

function deriveState(
  envelope: ApiEnvelope<CashflowProjectionPayload> | undefined,
  isLoading: boolean,
  isError: boolean,
): DataSectionState {
  if (isLoading) return { kind: "loading" };
  if (isError) return { kind: "error" };
  if (!envelope) return { kind: "empty" };

  const meta = envelope.result_meta;
  if (meta.vendor_status === "vendor_unavailable") {
    return { kind: "vendor_unavailable" };
  }
  if (typeof meta.source_version === "string" && meta.source_version.includes("explicit_miss")) {
    return {
      kind: "explicit_miss",
      requested_date: readFilterDate(meta.filters_applied),
    };
  }
  if (meta.fallback_mode === "latest_snapshot") {
    return {
      kind: "fallback",
      effective_date: readFilterDate(meta.filters_applied),
    };
  }
  if (meta.vendor_status === "vendor_stale" || meta.quality_flag === "stale") {
    return {
      kind: "stale",
      effective_date: readFilterDate(meta.filters_applied),
    };
  }
  const payload = envelope.result;
  if (!payload || (payload.monthly_buckets.length === 0 && payload.top_maturing_assets_12m.length === 0)) {
    return { kind: "empty" };
  }
  return { kind: "ok" };
}

function readFilterDate(filters: Record<string, unknown> | undefined): string | undefined {
  if (!filters) return undefined;
  const rd = filters["report_date"];
  return typeof rd === "string" ? rd : undefined;
}

export function adaptCashflowProjection(input: AdaptCashflowInput): CashflowAdapterOutput {
  const state = deriveState(input.envelope, input.isLoading, input.isError);
  const envelope = input.envelope;
  if (!envelope || state.kind === "loading" || state.kind === "error") {
    return { vm: null, state, meta: envelope?.result_meta ?? null };
  }
  const p = envelope.result;
  const vm: CashflowProjectionVM = {
    reportDate: p.report_date,
    kpis: {
      durationGap: p.duration_gap,
      assetDuration: p.asset_duration,
      liabilityDuration: p.liability_duration,
      equityDuration: p.equity_duration,
      rateSensitivity1bp: p.rate_sensitivity_1bp,
      reinvestmentRisk12m: p.reinvestment_risk_12m,
    },
    monthlyBuckets: p.monthly_buckets.map(mapBucket),
    topMaturingAssets: p.top_maturing_assets_12m.map(mapAsset),
    warnings: p.warnings ?? [],
  };
  return { vm, state, meta: envelope.result_meta };
}
```

**说明：**
- 本 adapter 是"无业务计算、pure mapping + state 派生"的典型形态，与 W2.3 dashboardAdapter 同款
- 如果 `DataSectionState` 的具体 type shape 与上面不一致（例如 `ok` 状态不用 `{ kind: "ok" }` 而是 `"ok"` literal），以 `frontend/src/components/DataSection` 实际 export 的 type 为准；**不要**改 `DataSection`，改 adapter 向它对齐

### Step 4：写 5 场景测试 `cashflowProjectionAdapter.test.ts`

```ts
import { describe, expect, it } from "vitest";

import type { ApiEnvelope, CashflowProjectionPayload, Numeric, ResultMeta } from "../../../api/contracts";
import { adaptCashflowProjection } from "./cashflowProjectionAdapter";

function n(partial: Partial<Numeric> = {}): Numeric {
  return {
    raw: 1,
    unit: "yuan",
    display: "+1.00 亿",
    precision: 2,
    sign_aware: true,
    ...partial,
  };
}

function meta(partial: Partial<ResultMeta> = {}): ResultMeta {
  return {
    trace_id: "tr_test",
    basis: "formal",
    result_kind: "cashflow_projection.overview",
    formal_use_allowed: true,
    source_version: "sv_test",
    vendor_version: "vv_test",
    rule_version: "rv_test",
    cache_version: "cv_test",
    quality_flag: "ok",
    vendor_status: "ok",
    fallback_mode: "none",
    scenario_flag: false,
    generated_at: "2026-04-18T00:00:00",
    filters_applied: {},
    tables_used: [],
    ...partial,
  };
}

function env(overrides: Partial<CashflowProjectionPayload> = {}, metaOverride: Partial<ResultMeta> = {}): ApiEnvelope<CashflowProjectionPayload> {
  const base: CashflowProjectionPayload = {
    report_date: "2025-04-30",
    duration_gap: n({ raw: 1.25, unit: "ratio" }),
    asset_duration: n({ raw: 3.8, unit: "ratio", sign_aware: false }),
    liability_duration: n({ raw: 2.55, unit: "ratio", sign_aware: false }),
    equity_duration: n({ raw: 5.2, unit: "ratio" }),
    rate_sensitivity_1bp: n({ raw: 125000, unit: "yuan" }),
    reinvestment_risk_12m: n({ raw: 0.185, unit: "pct", sign_aware: false }),
    monthly_buckets: [],
    top_maturing_assets_12m: [],
    warnings: [],
    computed_at: "2026-04-18T00:00:00",
  };
  return {
    result_meta: meta(metaOverride),
    result: { ...base, ...overrides },
  };
}

describe("adaptCashflowProjection", () => {
  it("maps a normal payload into a view-model", () => {
    const out = adaptCashflowProjection({ envelope: env(), isLoading: false, isError: false });
    expect(out.state.kind).toBe("empty"); // lists empty so state="empty"
    // With non-empty lists it's "ok":
    const out2 = adaptCashflowProjection({
      envelope: env({
        monthly_buckets: [
          {
            year_month: "2025-05",
            asset_inflow: n({ raw: 1e8, unit: "yuan", sign_aware: false }),
            liability_outflow: n({ raw: 5e7, unit: "yuan", sign_aware: false }),
            net_cashflow: n({ raw: 5e7, unit: "yuan" }),
            cumulative_net: n({ raw: 5e7, unit: "yuan" }),
          },
        ],
      }),
      isLoading: false,
      isError: false,
    });
    expect(out2.state.kind).toBe("ok");
    expect(out2.vm?.kpis.durationGap.raw).toBe(1.25);
    expect(out2.vm?.monthlyBuckets[0].assetInflow.raw).toBe(1e8);
  });

  it("returns loading state when isLoading", () => {
    const out = adaptCashflowProjection({ envelope: undefined, isLoading: true, isError: false });
    expect(out.state.kind).toBe("loading");
    expect(out.vm).toBeNull();
  });

  it("returns error state when isError", () => {
    const out = adaptCashflowProjection({ envelope: undefined, isLoading: false, isError: true });
    expect(out.state.kind).toBe("error");
  });

  it("preserves sign_aware = true display on negative raw", () => {
    const out = adaptCashflowProjection({
      envelope: env({
        duration_gap: n({ raw: -0.75, unit: "ratio", display: "-0.75", sign_aware: true }),
        monthly_buckets: [
          {
            year_month: "2025-05",
            asset_inflow: n({ raw: 1e8, unit: "yuan", sign_aware: false }),
            liability_outflow: n({ raw: 5e7, unit: "yuan", sign_aware: false }),
            net_cashflow: n({ raw: 5e7, unit: "yuan" }),
            cumulative_net: n({ raw: 5e7, unit: "yuan" }),
          },
        ],
      }),
      isLoading: false,
      isError: false,
    });
    expect(out.vm?.kpis.durationGap.raw).toBe(-0.75);
    expect(out.vm?.kpis.durationGap.display).toBe("-0.75");
  });

  it("raises fallback state on fallback_mode=latest_snapshot", () => {
    const out = adaptCashflowProjection({
      envelope: env({}, { fallback_mode: "latest_snapshot", filters_applied: { report_date: "2025-03-31" } }),
      isLoading: false,
      isError: false,
    });
    expect(out.state.kind).toBe("fallback");
  });

  it("raises explicit_miss state on source_version tag", () => {
    const out = adaptCashflowProjection({
      envelope: env({}, { source_version: "sv_explicit_miss", filters_applied: { report_date: "2099-01-01" } }),
      isLoading: false,
      isError: false,
    });
    expect(out.state.kind).toBe("explicit_miss");
  });
});
```

**如果 DataSectionState 的 shape 与上面断言不完全一致**（例如 `{ kind: "empty" }` 实际是 `"empty"` literal，或 `kind` 字段名不同），以 `frontend/src/components/DataSection.tsx` 的真实 export 为准调整断言；但**不得**修改 DataSection 本身。

### Step 5：重构 `CashflowProjectionPage.tsx`

核心要点：
1. 用 `useMemo` 把 query 的结果喂给 `adaptCashflowProjection`
2. 拿到 `{ vm, state }` 后，chart / KPI / table 都读 vm
3. `parseFloat(...)` 全量删除，改成 `.raw ?? 0`
4. `<KpiCard value={...} valueVariant="text" />` 传 `.display`
5. 表格 `dataSource={vm.topMaturingAssets}`；`face_value` / `market_value` 列加 `render: (v: Numeric) => v.display`
6. loading/error 分支保持原样（AntD `<Spin>` / `<Alert>`）；或可切到 `<DataSection state={out.state}>` 但**不是**必须，`valueVariant="text"` 下保留 AntD 模式也行——**选更小 diff 的方案**

**要求：**
- 组件里**零** `parseFloat` / `Number(` / `toFixed` / `toLocaleString` / `/1e8`
- `result.foo` 这种对原始 payload 的直接读取**清零**（只能通过 vm）
- 导入 `adaptCashflowProjection` + `type CashflowProjectionVM` 等
- 删除 `CashflowMonthlyBucket` import 如不再直接使用（避免 unused import lint）

### Step 6：本地 tsc + vitest

```powershell
cd frontend
npx tsc --noEmit 2>&1 | Select-Object -Last 30
```

**Expected：** 0 error。**如有**来自 cashflow 外域（bond / liability 等）的 tsc 错，那是你把 contracts.ts 改出去了 → BLOCKER 汇报并 revert。

```powershell
npx vitest run src/features/cashflow-projection src/test/CashflowProjectionPage.test.tsx 2>&1 | Select-Object -Last 60
cd ..
```

**Expected：**
- 新的 5 场景 adapter test 全绿（约 6 条）
- `CashflowProjectionPage.test.tsx` 如存在且因 API 变化需最小修正，**只**做最小修正；不扩大断言范围
- 如有**预期外** fail，BLOCKER 汇报

### Step 7：Commit

```powershell
git add frontend/src/api/contracts.ts frontend/src/api/client.ts frontend/src/features/cashflow-projection/adapters/ frontend/src/features/cashflow-projection/pages/CashflowProjectionPage.tsx
# 如 Step 6 有 page test 微调，也一并 add
git commit -m "feat(cashflow-projection): migrate contracts + page to Numeric view-model [W5.2-fe]"
```

提交前 `git diff --cached --stat` 确认改动文件数 ≤ 6（3 必改 + 2 新建 + 可选 1 个 page test）。

---

## 验收 & 返回清单

- [ ] `git diff --name-only HEAD~1 HEAD` 严格等于白名单子集
- [ ] `npx tsc --noEmit` 在 frontend 下 **0 error**（含本域之外）
- [ ] 5 场景 adapter test PASS
- [ ] `CashflowProjectionPage.tsx` 里 `parseFloat` / `Number(...)` / `toFixed` 数量归零（grep 校验）
- [ ] contracts.ts 的 bond-analytics / liability / pnl-bridge 等其他章节零改动
- [ ] mock `getCashflowProjection` 返回的每个 governed 字段是 Numeric 5-key dict

**返回 message 必须包含：**

1. 修改/新增文件清单
2. Step 6 的 tsc + vitest 输出尾部
3. `rg -n "parseFloat|Number\\(" frontend/src/features/cashflow-projection` 输出（应该为空或仅剩非 governed 场景）
4. `BLOCKER:` 情况（如有）
5. 遗留建议（例如是否给 W5.7/W5.8 留了 hook / 是否发现本域有额外未治理字段）

---

## 不要做的事

- ❌ 不要改 contracts.ts 的 cashflow 之外章节（每次修 contracts.ts **只**限 2282-2312 那 31 行）
- ❌ 不要跑 `python -m pytest`
- ❌ 不要改 backend
- ❌ 不要动 `frontend/src/utils/format.ts` / `tone.ts`
- ❌ 不要动 `DataSection` 组件
- ❌ 不要修改 `frontend/src/test/contract/mock-contract.test.ts`（W5.8 统一扩展）
- ❌ 不要 `git push` / `git reset --hard` / `git commit --amend`
- ❌ 不要引入新的 npm 依赖
