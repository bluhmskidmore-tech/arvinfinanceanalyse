# W5.1-fe · bond-analytics 前端 Numeric 落地

**对应：** 后端 W5.1 commit `4fc4d96`（`backend/app/schemas/bond_analytics.py`）已迁 Numeric。
**参考模板：** W5.2-fe commit `9b699a8`、W5.4-fe commit `cb75bc6`（`git show` 这两个 commit 看模式，**不要**我再复述）。

---

## Subagent 约束

- 白名单外不得修改；越界 `BLOCKER:` 报告
- 禁 `git push` / `git reset --hard` / `git commit --amend` / `git rebase`
- 禁改 `package.json` / `pyproject.toml` / `frontend/src/utils/format.ts` / `tone.ts` / `DataSection*`
- 禁跑 Python 测试
- Windows PowerShell：管道 `Select-Object -Last N`
- 单 commit，message：`feat(bond-analytics): migrate contracts + pages to Numeric view-model [W5.1-fe]`

## 白名单

**Modify：**
- `frontend/src/api/contracts.ts`（**仅** bond-analytics 段：`BondAnalyticsRefreshPayload` / `BondAnalyticsDatesPayload` / `AssetClassBreakdown` / `BondLevelDecomposition` / `ReturnDecompositionPayload` / `ExcessSourceBreakdown` / `BenchmarkExcessPayload` / `KRDBucket` / `KRDScenarioResult` / `AssetClassRiskSummary` / `KRDCurveRiskPayload` / `BondPortfolioHeadlinesPayload` / `BondTopHoldingItem` / `BondTopHoldingsPayload` / `SpreadScenarioResult` / `MigrationScenarioResult` / `ConcentrationItem` / `ConcentrationMetrics` / `CreditSpreadBondDetailRow` / `CreditSpreadMigrationPayload` / `CreditSpreadTermStructurePoint` / `CreditSpreadDetailBondRow` / `SpreadHistoricalContextPayload` / `CreditSpreadAnalysisPayload` / `ActionTypeSummary` / `ActionDetail` / `ActionAttributionPayload` / `AccountingClassAuditItem` / `AccountingClassAuditPayload`；大约 131-498 行；其他段 0 改动）
- `frontend/src/api/client.ts`（仅 bond-analytics 相关 mock 方法：`getBondPortfolioHeadlines` / `getBondTopHoldings` / `getReturnDecomposition` / `getBenchmarkExcess` / `getKrdCurveRisk` / `getCreditSpreadMigration` / `getCreditSpreadAnalysis` / `getActionAttribution` / `getAccountingClassAudit` 等；自己 grep 定位；real 分支 + 其他 mock 方法不碰）
- `frontend/src/features/bond-analytics/**`（页面 + 组件 + `types.ts` + utils；直接消费 Numeric-化字段的地方改读 `.display` / `.raw`）

**Create：**
- `frontend/src/features/bond-analytics/adapters/bondAnalyticsAdapter.ts`（可拆多个 adapter 文件：portfolio-headlines / return-decomposition / benchmark-excess / krd-curve / credit-spread / action-attribution / accounting-audit；看 page 结构决定）
- 对应 `.test.ts`（每个 adapter 至少 5 场景）

**可最小修改（仅当既有测试 red 时）：**
- `frontend/src/test/` 里 bond-analytics 相关的测试文件

**绝对不动：** contracts.ts 其他域；backend；`mock-contract.test.ts`（W5.8 做）。

## 字段单位

自己 Read `backend/app/schemas/bond_analytics.py` 的每个 class `_NUMERIC_FIELDS`，**抄**过来。不要猜。

## 任务范围声明

本域 28+ types、多 page 多组件，是 Stage B 最大单个 task。**允许**你做以下取舍：
- **如果单个 composer 跑不完全部**：优先保证 `contracts.ts` 升级 + 核心 page（`BondPortfolioHeadlines` / `ReturnDecomposition` / `KRDCurveRisk`）落地；次要组件（`ActionAttributionView` / `AccountingAudit*` 等）如果 tsc 暴红，**必须**一起修到绿，不许半成品（tsc 0 error 是硬门槛）
- **如果某个 page 已有 `DataSection` + adapter 模式（例如 portfolio-headlines）**：沿用该模式扩展即可；无需推翻重来

## 验收

- [ ] tsc 0 error（整个 frontend）
- [ ] 新 adapter 5 场景 PASS（每个 adapter 至少一套）
- [ ] bond-analytics 下 `parseFloat|toFixed|toLocaleString|Number\(` 对 governed 字段清零
- [ ] contracts.ts 其他段 0 改动
- [ ] mock 返回的每个 governed 字段是 Numeric 5-key dict

返回只需：文件清单 + tsc/vitest 尾部 + BLOCKER（如有）+ 遗留。
