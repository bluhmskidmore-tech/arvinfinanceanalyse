# W5.8 · Mock ↔ contract runtime 对拍基线扩展

**目标：** 把 `frontend/src/test/contract/mock-contract.test.ts` 扩展到覆盖 Wave 5 已迁移的所有 mock 方法，确保每个 mock envelope 的 governed 节点都通过 `assertAllNumerics`。

**参考已有模式：**
- `mockClient.getOverview · W2.7 contract`
- `mockClient.getPnlAttribution · W2.7 contract`
- `mockClient.getVolumeRateAttribution · W3.6 contract` (以及 W3.6 其余 8 条)
- `mockClient.getHomeSnapshot · W4.3 contract`

---

## Subagent 约束

- 白名单外不得修改；越界 `BLOCKER:`
- 禁 `git push` / `reset --hard` / `commit --amend` / `rebase`
- 禁改 `package.json` / `pyproject.toml`
- 禁跑 Python 测试
- Windows PowerShell：管道 `Select-Object -Last N`
- 单 commit：`test(contract): extend mock-contract baseline with Wave 5 domains [W5.8]`

## 白名单

**Modify：**
- `frontend/src/test/contract/mock-contract.test.ts`

**不得修改任何其他文件。** 即使发现某个 mock 有 bug（比如某个字段漏 Numeric 化），也不要改 mock；**用 `BLOCKER:` 汇报**让主理另开 task 修。

## 要做的事

1. 自己 `Read frontend/src/test/contract/mock-contract.test.ts` 看既有结构
2. 自己 `Read frontend/src/api/client.ts` 的 mock 分支定位 Wave 5 相关方法：
   - `getCashflowProjection`（W5.2-fe 已落地）
   - `getPnlBridge`（W5.4-fe 已落地）
   - 如果存在 `getLiability*`（W5.5-fe 之后才会是 Numeric；**如果当前 mock 输出不是 Numeric，跳过并记在 BLOCKER 里**）
   - 如果存在 bond-analytics 相关（`getBondPortfolioHeadlines` / `getReturnDecomposition` / `getBenchmarkExcess` / `getKrdCurveRisk` / `getCreditSpreadMigration` / `getCreditSpreadAnalysis` / `getActionAttribution` / `getAccountingClassAudit`；W5.1-fe 之后）
3. 对**现在已经是 Numeric shape** 的 mock 方法，追加 `describe("mockClient.<method> · W5.X contract", ...)` 块，沿既有 W2.7 / W3.6 格式
4. 对**仍是 string/number legacy shape** 的 mock 方法，**不**追加测试（否则会 fail），在返回 message 里列出："该方法 mock 输出尚非 Numeric，建议 W5.X-fe 落地后再补测试"

## 判断"是否已 Numeric shape"的启发式

- 调 mock 方法，如果 envelope.result 里每个 governed 字段是 `{raw, unit, display, precision, sign_aware}` 5-key dict → 已迁移
- 如果是 `"0.00"` 这样的 string → 未迁移，跳过
- 你在 vitest 里写个临时 console.log（跑完删掉）或者直接 `JSON.stringify(envelope.result).includes('"unit"')` 判断

## 验收

- [ ] `npx vitest run src/test/contract` 全绿
- [ ] 新 describe 块至少覆盖：`getCashflowProjection`、`getPnlBridge`
- [ ] 发现的未迁移方法列在返回 message 的 BLOCKER/遗留里（**不**擅自改 mock）
- [ ] mock-contract.test.ts 之外 0 改动

返回只需：文件清单 + `npx vitest run src/test/contract` 输出尾部 + BLOCKER + 遗留。
