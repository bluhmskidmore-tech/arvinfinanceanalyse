# W4.5 · Fail-fast production build

**目标一句话：** 修改 `frontend/src/api/client.ts::parseEnvMode`，生产构建（`import.meta.env.PROD === true`）且未显式设置 `VITE_DATA_SOURCE` 为 `"real"` 或 `"mock"` 时**抛错**而非静默回落 mock；dev / test 模式保持原有"默认回落 mock 并警告"语义。

**所属波次：** Wave 4 · Snapshot & 口径（独立 task，最小工作量）
**依赖：** 无
**预计工时：** 15-25 分钟
**对应 Spec：** `docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md` § 9.1

---

## Composer Subagent 约束（必须遵守）

你是一个专注执行单一 task 的 composer-2 subagent。

- 仅允许修改白名单文件
- 遇白名单外问题用 `BLOCKER:` 前缀返回
- 不要 `git push` / `git reset --hard` / `git commit --amend`
- 不要修改 `package.json` 或安装依赖
- 不要创建 git worktree
- 不要跑全量测试
- 完成后返回：修改文件列表、测试命令输出摘要、越界/告警、遗留问题

---

## 允许修改的路径（白名单）

**Modify：**
- `frontend/src/api/client.ts` — **仅**改 `parseEnvMode` 函数（大约第 2298-2302 行），不得修改文件其他内容

**Create：**
- `frontend/src/test/parseEnvMode.test.ts`

**不得修改任何其他文件。**

---

## 背景上下文

### 当前 `parseEnvMode`（大约第 2298-2302 行）

```ts
const parseEnvMode = (): DataSourceMode => {
  const raw = import.meta.env.VITE_DATA_SOURCE;
  const envValue = typeof raw === "string" ? raw.trim().toLowerCase() : "";
  return envValue === "real" ? "real" : "mock";
};
```

**问题：** 生产构建未设置 `VITE_DATA_SOURCE=real` 时静默回落 mock —— 这是审计报告 P0-1。

### 新 `parseEnvMode` 语义

1. 如果 `VITE_DATA_SOURCE` 显式为 `"real"` 或 `"mock"` → 直接返回
2. 如果未设置：
   - 生产构建（`import.meta.env.PROD === true`）→ **抛 Error**，消息明确说明缺少显式声明
   - 非生产（dev / test）→ 打印 `console.warn`，返回 `"mock"`（保留现有行为）

---

## 实现步骤

### Step 1：写测试文件

创建 `frontend/src/test/parseEnvMode.test.ts`：

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

import { createApiClient } from "../api/client";

describe("createApiClient · parseEnvMode", () => {
  const originalEnv = { ...import.meta.env };

  afterEach(() => {
    vi.unstubAllEnvs();
    vi.restoreAllMocks();
  });

  describe("explicit VITE_DATA_SOURCE", () => {
    it('returns mode "real" when VITE_DATA_SOURCE="real"', () => {
      vi.stubEnv("VITE_DATA_SOURCE", "real");
      vi.stubEnv("VITE_API_BASE_URL", "http://localhost:8080");
      const client = createApiClient();
      expect(client.mode).toBe("real");
    });

    it('returns mode "mock" when VITE_DATA_SOURCE="mock"', () => {
      vi.stubEnv("VITE_DATA_SOURCE", "mock");
      const client = createApiClient();
      expect(client.mode).toBe("mock");
    });

    it("is case-insensitive for explicit values", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "REAL");
      vi.stubEnv("VITE_API_BASE_URL", "http://localhost:8080");
      const client = createApiClient();
      expect(client.mode).toBe("real");
    });

    it("trims whitespace around explicit values", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "  real  ");
      vi.stubEnv("VITE_API_BASE_URL", "http://localhost:8080");
      const client = createApiClient();
      expect(client.mode).toBe("real");
    });
  });

  describe("dev / test mode (PROD=false) · unset VITE_DATA_SOURCE", () => {
    it('defaults to "mock" with console.warn', () => {
      vi.stubEnv("VITE_DATA_SOURCE", "");
      vi.stubEnv("PROD", false as unknown as string); // simulate dev
      const warnSpy = vi.spyOn(console, "warn").mockImplementation(() => undefined);
      const client = createApiClient();
      expect(client.mode).toBe("mock");
      expect(warnSpy).toHaveBeenCalled();
    });
  });

  describe("production mode (PROD=true) · unset VITE_DATA_SOURCE", () => {
    it("throws Error when VITE_DATA_SOURCE is empty string", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "");
      vi.stubEnv("PROD", true as unknown as string);
      expect(() => createApiClient()).toThrow(/VITE_DATA_SOURCE/);
    });

    it("throws Error when VITE_DATA_SOURCE is unset", () => {
      vi.stubEnv("PROD", true as unknown as string);
      // VITE_DATA_SOURCE undefined
      expect(() => createApiClient()).toThrow(/VITE_DATA_SOURCE/);
    });

    it("throws Error when VITE_DATA_SOURCE is a bogus value", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "bogus");
      vi.stubEnv("PROD", true as unknown as string);
      expect(() => createApiClient()).toThrow(/VITE_DATA_SOURCE/);
    });

    it("does NOT throw when explicitly set to real", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "real");
      vi.stubEnv("VITE_API_BASE_URL", "http://localhost:8080");
      vi.stubEnv("PROD", true as unknown as string);
      expect(() => createApiClient()).not.toThrow();
    });

    it("does NOT throw when explicitly set to mock", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "mock");
      vi.stubEnv("PROD", true as unknown as string);
      expect(() => createApiClient()).not.toThrow();
    });
  });

  describe("mode override via options", () => {
    it("options.mode overrides env parsing", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "real");
      vi.stubEnv("VITE_API_BASE_URL", "http://localhost:8080");
      const client = createApiClient({ mode: "mock" });
      expect(client.mode).toBe("mock");
    });

    it("options.mode='real' does not trigger fail-fast even in PROD without env", () => {
      vi.stubEnv("VITE_DATA_SOURCE", "");
      vi.stubEnv("PROD", true as unknown as string);
      vi.stubEnv("VITE_API_BASE_URL", "http://localhost:8080");
      const client = createApiClient({ mode: "real" });
      expect(client.mode).toBe("real");
    });
  });
});
```

### Step 2：跑测试确认 FAIL

```bash
cd frontend
npm run test -- src/test/parseEnvMode.test.ts
```

**Expected：** FAIL（新的 production-mode fail-fast 测试会失败，因为现在 code 静默回落 mock 而不抛错）。

### Step 3：修改 `parseEnvMode`

打开 `frontend/src/api/client.ts`，找到 `parseEnvMode` 函数（约 2298-2302 行），**仅**替换它为：

```ts
const parseEnvMode = (): DataSourceMode => {
  const raw = import.meta.env.VITE_DATA_SOURCE;
  const envValue = typeof raw === "string" ? raw.trim().toLowerCase() : "";

  if (envValue === "real") return "real";
  if (envValue === "mock") return "mock";

  // Not explicitly set (or invalid value)
  const isProd = import.meta.env.PROD === true;
  if (isProd) {
    throw new Error(
      "VITE_DATA_SOURCE must be explicitly set to 'real' or 'mock' in production build. " +
      "Refusing to silently fall back to mock. " +
      "See docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md § 9.1.",
    );
  }

  // dev / test: default to mock with warning
  console.warn(
    "[client] VITE_DATA_SOURCE not set (raw=%o). Defaulting to 'mock' in dev mode. " +
    "Production build will fail fast; always declare explicitly in production.",
    raw,
  );
  return "mock";
};
```

**不要修改** `parseBaseUrl` / `createApiClient` / 其他任何函数。

### Step 4：跑测试确认 PASS

```bash
cd frontend
npm run test -- src/test/parseEnvMode.test.ts
```

**Expected：** 所有 11+ 条测试 PASS。

### Step 5：typecheck 0 errors

```bash
cd frontend
npm run typecheck
```

### Step 6：子集回归

```bash
cd frontend
npm run test -- src/test/ 2>&1 | Select-Object -Last 12
```

**Expected：** 相对 Wave 3 tag 无新增 fail。

### Step 7：Commit

```bash
git add frontend/src/api/client.ts frontend/src/test/parseEnvMode.test.ts
git commit -m "feat(api): fail-fast in production when VITE_DATA_SOURCE is unset [W4.5]"
```

---

## 验收 & 返回清单

- [ ] `parseEnvMode.test.ts` 所有测试 PASS（11+ 条）
- [ ] `client.ts` 的 `parseEnvMode` 已按新语义修改；其他内容未改
- [ ] typecheck 0 errors
- [ ] 子集回归无新增 fail
- [ ] `git diff --name-only HEAD~1 HEAD` 严格等于 2 个

**返回 message 必须包含：**
1. 修改文件列表
2. parseEnvMode.test 输出尾部
3. typecheck 状态
4. `BLOCKER:` 情况（如有）
5. 遗留问题

---

## 不要做的事

- ❌ 不要修改 `client.ts` 除 `parseEnvMode` 以外的任何函数
- ❌ 不要修改 `createApiClient` 的签名 / 其 body
- ❌ 不要修改 `DataSourceMode` 类型定义
- ❌ 不要修改 `vitest.config.ts` / `package.json`
- ❌ 不要创建 worktree
