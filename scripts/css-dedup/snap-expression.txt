(async () => { const sleep = ms => new Promise(r => setTimeout(r, ms)); try { if (document.fonts && document.fonts.ready) await document.fonts.ready; } catch (e) {} let stable = 0, last = -1; for (let i = 0; i < 40 && stable < 3; i++) { const h = document.documentElement.scrollHeight; if (h === last) stable++; else { stable = 0; last = h; } await sleep(200); } window.scrollTo(0, 0); await sleep(120); const deLocal = c => { const m = c.match(/^_([A-Za-z0-9_-]+)_[a-z0-9]+_\d+$/); return m ? m[1] : null; }; const items = []; const evidence = new Map(); const all = Array.from(document.querySelectorAll('*')); let mi = 0; for (const el of all) { const cls = typeof el.className === 'string' ? el.className : (el.getAttribute && el.getAttribute('class') || ''); const tid = el.getAttribute ? el.getAttribute('data-testid') : null; if (!cls && !tid) continue; const locals = [], globals = []; for (const c of cls.split(/\s+/).filter(Boolean)) { const l = deLocal(c); if (l) locals.push(l); else globals.push(c); } if (globals.length || locals.length) { const k = [...new Set([...locals, ...globals])].sort().join('|'); evidence.set(k, 1); } const antEl = globals.some(g => g.startsWith('ant-')); if (!tid && locals.length === 0 && !antEl) continue; const r = el.getBoundingClientRect(); const cs = getComputedStyle(el); const item = { i: mi++, t: tid || null, k: el.tagName.toLowerCase() + '.' + [...new Set(locals.length ? locals : globals)].sort().join('.'), x: Math.round((r.left + window.scrollX) * 100) / 100, y: Math.round((r.top + window.scrollY) * 100) / 100, w: Math.round(r.width * 100) / 100, h: Math.round(r.height * 100) / 100, s: [cs.color, cs.backgroundColor, cs.borderTopColor, cs.fontSize, cs.fontWeight, cs.lineHeight, cs.boxShadow, cs.opacity].join('§') }; const pb = getComputedStyle(el, '::before'); if (pb.content && pb.content !== 'none') item.pb = [pb.content, pb.color, pb.backgroundColor, pb.width, pb.height, pb.opacity].join('§'); const pa = getComputedStyle(el, '::after'); if (pa.content && pa.content !== 'none') item.pa = [pa.content, pa.color, pa.backgroundColor, pa.width, pa.height, pa.opacity].join('§'); items.push(item); } const ev2 = new Map(); for (const el of all) { const cls = typeof el.className === 'string' ? el.className : (el.getAttribute && el.getAttribute('class') || ''); const own = []; for (const c of cls.split(/\s+/).filter(Boolean)) { const l = deLocal(c); own.push(l || c); } const closure = new Set(own); let p = el.parentElement; while (p) { const pc = typeof p.className === 'string' ? p.className : (p.getAttribute && p.getAttribute('class') || ''); for (const c of pc.split(/\s+/).filter(Boolean)) { const l = deLocal(c); closure.add(l || c); } p = p.parentElement; } const key = el.tagName.toLowerCase() + '§' + [...new Set(own)].sort().join('.') + '§' + [...closure].sort().join('.'); ev2.set(key, 1); } const bandOf = (c, bi, src) => { const cs = getComputedStyle(c); const locals = (c.getAttribute('class') || '').split(/\s+/).map(deLocal).filter(Boolean); return { src, bi, k: c.tagName.toLowerCase() + '.' + locals.sort().join('.'), testid: c.getAttribute('data-testid') || null, display: cs.display, gtc: cs.gridTemplateColumns, gap: cs.gap, padding: cs.padding, minHeight: cs.minHeight }; }; const bands = []; const mainEl = document.querySelector('main') || document.body; { const desc = Array.from(mainEl.querySelectorAll('*')); let bi = 0; for (const el of desc) { const disp = getComputedStyle(el).display; let p = el.parentElement; while (p && p !== mainEl && getComputedStyle(p).display === 'contents') p = p.parentElement; if (p !== mainEl) continue; if (disp === 'contents') { bands.push(bandOf(el, 1000 + bi++, 'shell')); continue; } bands.push(bandOf(el, bi++, 'band')); } } return JSON.stringify({ meta: { href: location.href, vw: window.innerWidth, vh: window.innerHeight, docH: document.documentElement.scrollHeight, elCount: all.length, textLen: (document.body.innerText || '').length, measured: items.length }, bands, items, evidence: [...evidence.keys()], evidence2: [...ev2.keys()] }); })()