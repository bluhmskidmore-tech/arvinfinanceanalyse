---
phase: 03
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/services/pnl_service.py
  - backend/app/tasks/pnl_materialize.py
  - frontend/src/api/contracts.ts
  - frontend/src/features/pnl/PnlByBusinessPage.tsx
  - tests/test_pnl_api_contract.py
autonomous: true
requirements: [REC-01]
---

# Plan 03-02: Per-Cutoff Recovery and Status Closure

## Objective

Isolate failures to the exact cutoff, retry only transient failed/stale cutoffs, and keep the selected cutoff visibly recoverable while unaffected current partitions continue serving normally.

## Execution Context

Execute after plan 03-01. Reuse the current `/pnl-by-business` status panel and selected-cutoff query. Frontend work is additive Tier 2 only and is optional when the existing contract already closes the requirement. Preserve safe messages and exact-cutoff manual retry.

<threat_model>
T-03-02: prevent automatic retry storms through transient-only classification and bounded attempts.
T-03-04: do not expose raw database/worker exception text in the status API or UI.
T-03-05: manual recovery remains exact-cutoff and duplicate-safe.
</threat_model>

<tasks>

<task id="03-02-01" requirement="REC-01">
  <name>Isolate cutoff results and retry eligibility</name>
  <read_first>
    <file>backend/app/tasks/pnl_materialize.py::_rebuild_pnl_by_business_precompute</file>
    <file>backend/app/tasks/pnl_materialize.py::_pnl_by_business_precompute_run_record</file>
    <file>tests/test_pnl_api_contract.py</file>
  </read_first>
  <action>Catch and classify exceptions per exact target cutoff while retaining the single writer lock. Continue later targets, persist completed/failed cutoff_results, and record partial_failed for mixed outcomes. Build automatic retry targets from failed/stale exact cutoffs only and only for transient categories; validation and cutoff-drift failures are permanent. Keep retries bounded by the actor policy.</action>
  <acceptance_criteria>
    <criterion>One cutoff failure does not prevent later target cutoffs from completing.</criterion>
    <criterion>Mixed outcomes are persisted as partial_failed with exact cutoff evidence.</criterion>
    <criterion>Only transient exact-cutoff failures are automatically retryable.</criterion>
    <criterion>Permanent failures remain visible after retry exhaustion.</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_pnl_api_contract.py -q -k "cutoff and (failure or retry or partial)"</automated></verify>
  <done>Worker behavior isolates and classifies failures at cutoff granularity.</done>
</task>

<task id="03-02-02" requirement="REC-01">
  <name>Expose exact selected-cutoff recovery state</name>
  <read_first>
    <file>backend/app/services/pnl_service.py::_pnl_by_business_precompute_status_record</file>
    <file>backend/app/services/pnl_service.py::_pnl_by_business_precompute_status_payload</file>
    <file>frontend/src/api/contracts.ts::PnlByBusinessPrecomputeStatus</file>
    <file>frontend/src/features/pnl/PnlByBusinessPage.tsx::PnlByBusinessPrecomputeStatusPanel</file>
  </read_first>
  <action>Resolve the selected cutoff's entry from cutoff_results before deriving lifecycle, safe error, and serving mode. Return additive retry/recovery fields only if needed. Keep failed/stale selected cutoff on live_fallback, unaffected current cutoff on precomputed, and manual retry scoped to the exact selected cutoff. If frontend changes, minimally render the additive state in the existing panel with no layout redesign.</action>
  <acceptance_criteria>
    <criterion>Failed/stale selected cutoff is visible and reports live fallback.</criterion>
    <criterion>An unaffected current cutoff still reports precomputed serving.</criterion>
    <criterion>Only categorized safe error text reaches the UI.</criterion>
    <criterion>Manual retry sends the exact selected cutoff.</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_pnl_api_contract.py -q -k "precompute_status or manual_rebuild or exact_cutoff"</automated></verify>
  <done>The existing status panel closes exact-cutoff recovery with safe evidence.</done>
</task>

<task id="03-02-03" requirement="REC-01">
  <name>Run page-level recovery gates</name>
  <read_first><file>.planning/phases/03-incremental-rebuild-and-recovery/03-VALIDATION.md</file><file>frontend/package.json</file></read_first>
  <action>Run complete backend page contract and adjacent parity checks. If frontend changed, run its targeted test, scoped lint/typecheck, browser-visible check where available, and required frontend debt audit. Run GitNexus detect-changes before the final exact-path commit.</action>
  <acceptance_criteria><criterion>All changed-path checks are green and detect-changes shows only expected PnL flows.</criterion></acceptance_criteria>
  <verify><automated>pytest tests/test_pnl_api_contract.py -q</automated></verify>
  <done>REC-01 and phase-wide validation evidence are complete.</done>
</task>

</tasks>
