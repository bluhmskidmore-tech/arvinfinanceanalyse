---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/pnl_service.py
  - backend/app/tasks/pnl_materialize.py
  - tests/test_pnl_api_contract.py
autonomous: true
requirements: [INC-01, INC-02]
---

# Plan 03-01: Cumulative Invalidation and Durable Successor

## Objective

Freeze the exact same-year cumulative cutoff set after a governed change and coalesce overlapping automatic refresh requests into one durable queued successor.

## Execution Context

Read `03-CONTEXT.md`, `03-RESEARCH.md`, `03-VALIDATION.md`, root `AGENTS.md`, `backend/CLAUDE.md`, and `tests/CLAUDE.md`. Do not change PnL formulas, schema, or shared queue/cache infrastructure. Before editing each existing symbol, run GitNexus upstream impact; before commit, run detect-changes.

<threat_model>
T-03-01: reject target amplification outside available same-year month-end cutoffs.
T-03-02: prevent retry/queue fan-out by maintaining at most one queued successor per year.
T-03-03: prevent stale overwrite by persisting frozen targets and reloading the latest effective successor event.
</threat_model>

<tasks>

<task id="03-01-01" requirement="INC-01">
  <name>Freeze governed cumulative invalidation targets</name>
  <read_first>
    <file>backend/app/services/pnl_service.py::_available_pnl_by_business_precompute_cutoffs</file>
    <file>backend/app/services/pnl_service.py::_enqueue_pnl_by_business_precompute_refresh</file>
    <file>backend/app/tasks/pnl_materialize.py::_materialize_pnl_facts</file>
    <file>tests/test_pnl_api_contract.py</file>
  </read_first>
  <action>Add a page-local helper that maps the earliest committed change date to the first available same-year month-end cutoff on or after it and returns that cutoff plus all later available cutoffs. Use explicit frozen `as_of_dates` for formal-source post-commit refresh and result-changing approved manual-adjustment transitions. Persist earliest change date and targets. Append auditable no-task/no-change evidence when appropriate. Keep earlier cutoffs untouched.</action>
  <acceptance_criteria>
    <criterion>Non-month-end changes start at the first available later month-end.</criterion>
    <criterion>Targets are sorted, unique, same-year, and limited to repository availability.</criterion>
    <criterion>Formal-source and manual-adjustment triggers dispatch only after committed evidence exists.</criterion>
    <criterion>Empty target and identical-current cases are auditable without worker dispatch.</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_pnl_api_contract.py -q -k "incremental or invalidation or cumulative_cutoff"</automated></verify>
  <done>INC-01 target selection and trigger tests pass.</done>
</task>

<task id="03-01-02" requirement="INC-02">
  <name>Coalesce one durable queued successor</name>
  <read_first>
    <file>backend/app/services/pnl_service.py::_queue_pnl_by_business_precompute_refresh</file>
    <file>backend/app/services/pnl_service.py::_effective_pnl_by_business_precompute_run_records</file>
    <file>backend/app/tasks/pnl_materialize.py::_rebuild_pnl_by_business_precompute</file>
    <file>tests/test_pnl_api_contract.py</file>
  </read_first>
  <action>Inside the existing page-local dispatch lock, preserve running/queued records as immutable events. Automatic overlap with a queued successor appends a widened queued event using the same run_id and does not send another actor. Running-only creates and sends one successor. Manual duplicate requests retain conflict behavior. At worker start, resolve the latest effective record for run_id so the widened target list is authoritative, then remove already-current targets by cutoff fingerprint before building.</action>
  <acceptance_criteria>
    <criterion>At most one effective queued successor exists per year.</criterion>
    <criterion>Overlapping automatic requests union targets and send no second actor.</criterion>
    <criterion>A queued actor consumes the latest widened target evidence.</criterion>
    <criterion>Manual duplicate requests still raise conflict.</criterion>
  </acceptance_criteria>
  <verify><automated>pytest tests/test_pnl_api_contract.py -q -k "coalesce or successor or duplicate"</automated></verify>
  <done>INC-02 orchestration tests pass without changing shared queue infrastructure.</done>
</task>

<task id="03-01-03" requirement="INC-01,INC-02">
  <name>Close plan 01 validation</name>
  <read_first><file>tests/test_pnl_api_contract.py</file><file>.planning/phases/03-incremental-rebuild-and-recovery/03-VALIDATION.md</file></read_first>
  <action>Run the targeted and complete PnL API contract tests. Record any environmental failure. Do not widen unrelated tests unless a shared boundary is proven.</action>
  <acceptance_criteria><criterion>Targeted orchestration tests and complete page contract are green.</criterion></acceptance_criteria>
  <verify><automated>pytest tests/test_pnl_api_contract.py -q</automated></verify>
  <done>Plan 01 has executable evidence for INC-01 and INC-02.</done>
</task>

</tasks>
