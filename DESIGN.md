# Design System — MOSS V3

> 本文件是全站版式、配色、字体、密度与动效的**唯一权威**。改动任何界面外观前先读本文；数值以 `frontend/src/theme/designSystem.ts` 与 `frontend/src/styles/tokens.css` 为单一来源。QA 与代码审查中，明显违背本文的实现应被标出。

---

## 1. Product Context

- **What this is:** 面向机构固收与组合管理的业务工作台（经营日报、债券分析、风险、绩效、治理与报表），强调可读数字、可追踪口径与页面级闭环。
- **Who it's for:** 投资/交易、风控、经营与管理层——需要在统一界面里"先看结论、再下钻证据"的用户。
- **Project type:** 内部业务 Web 应用（重数据仪表与表格，**非营销站**）。营销页设计法则（Hero 规则、Bento、图片策略等）不适用；本文吸收的是通用审美纪律：一致性锁、降噪、状态语义化、文案自审。
- **North star:** 冷静、可审计。数字与单位一眼可辨；异常、缺口、兜底显式呈现，**不用装饰掩盖缺失数据**。

---

## 2. 主题体系（深色为默认）

项目现存两套视觉语言，但产品默认且最终收敛方向是**全系统深色机构终端**。**一页一主题，页内禁止混用或中途翻转**（Page Theme Lock）；IB 浅色仅用于尚未迁移的存量页面或经产品明确书面批准的例外，不再作为新页面或重做页面的默认方案。

### 2.1 IB 浅色工作台（受控例外 / 存量迁移）

纸感浅底 + 白卡 + 深藏青强调 + 深色侧栏。该体系保留用于兼容尚未完成迁移的页面；任何新增使用都必须记录路由、原因、批准人和退出条件。

| Token | 值 | 用途 |
|---|---|---|
| `--ib-paper` | `#f4f3f0` | 应用底 |
| `--ib-surface` / `--ib-surface-muted` | `#ffffff` / `#f7f6f3` | 卡片 / 次级面 |
| `--ib-hairline` | `#e2e0da` | 细分隔线 |
| `--ib-ink` / `--ib-ink-secondary` / `--ib-ink-muted` | `#16191d` / `#5c6370` / `#8a8f98` | 正文 / 次文 / 弱文 |
| `--ib-accent` (+hover/surface) | `#14366b` | 主强调、链接、主按钮 |
| `--ib-up` / `--ib-down` / `--ib-warn` | `#1f7a4d` / `#b42318` / `#b54708` | 语义色 |
| `--ib-rail-*` | `#10161f` 系 + 金条 `#c9a85c` | 左侧导航深栏 |
| `--ib-radius` | `2px` | 全局锐角卡片 |
| `--ib-serif` | Georgia / Noto Serif SC 栈 | 仅标题/刊头 |

### 2.2 深色机构终端（Decision Desk，全站默认）

用于全系统业务页面，包括经营日报、组合、债券、股票、市场数据、宏观、损益与治理工作流。近黑藏青底 + 分层面板 + **去饱和**冷强调。

> `/product-category-pnl` 已由产品确认锁定为深色完成版；不得用旧 IB 浅色实现或历史快照覆盖。现存浅色页面属于迁移债务，不构成后续设计权威。

| Token | 值 | 用途 |
|---|---|---|
| `--dh-api-bg` | `#070a12` | 页面底（非纯黑） |
| `--dh-api-panel` / `-2` / `-3` | `#0c1421` / `#101a2a` / `#132033` | 三层面板递进 |
| `--dh-api-line` / `-soft` | `rgba(103,119,142,.34/.22)` | 边线两级 |
| `--dh-api-ink` / `soft` / `muted` | `#e8eef7` / `#a9b6c7` / `#8593a8` | 文字三级 |
| `--dh-api-blue` | `#72a7dc` | 强调/链接（去饱和） |
| `--dh-api-green` / `amber` / `red` | `#66b98b` / `#c9a565` / `#d47a72` | 语义色（去饱和） |

**规则：**
- 深色页语义色必须用以上去饱和色阶，禁止把浅色主题的高饱和 `#ef4444`/`#2d8a5e` 直接搬进深色页。
- 面板层级靠 `panel → panel-2 → panel-3` 背景递进 + 细边线表达，**不靠阴影**（深色底上阴影无效且脏）。
- 页面主题由页面根容器一次性声明（token 重映射），子组件禁止各自覆盖主题变量。

### 2.3 选择规则

- 新页面、重做页面和恢复页面默认使用深色机构终端主题，并在页面根容器一次性声明 `theme-dh-api` 或其统一后继主题标记。
- IB 浅色只允许作为明确记录的临时兼容或产品例外；不得因旧文档、旧截图或旧提交把已确认的深色页面回退为浅色。
- `/product-category-pnl`、经营日报首页、组合首页、债券分析和股票分析均为已锁定深色路由。
- 同一路由树下的详情页/下钻页跟随其入口页主题，避免跳转时明暗闪切。

### 2.4 存量浅色迁移边界

| 项 | 约定 |
|---|---|
| **状态** | 仍为浅色的页面可以在迁移完成前继续运行，但不得作为新功能的视觉模板 |
| **改动规则** | 业务修复可保持现状；视觉重做、页面恢复或新增主要区块时应整页收敛到深色，不制造新的明暗拼接 |
| **Token** | 深色实现统一使用 `--dh-api-*` 或其后继全局 token；禁止扩张自制 `--command-*`、裸 navy hex 或第三套主题 |
| **例外记录** | 必须写明路由、批准人、批准日期、原因和退出条件；没有记录即按深色默认处理 |
| **验收口径** | 页面根主题标记、1440×900 截图和定向回归必须共同证明未发生浅色回退 |

---

## 3. Typography

- **UI/正文：** 本地系统栈 `PingFang SC / Microsoft YaHei UI / Noto Sans SC / Segoe UI`（`fontFamily.sans`）。**不引入外链展示字体**——内网可用性优先。
- **数字/KPI/表格：** 等宽栈（`fontFamily.tabular`）+ `font-variant-numeric: tabular-nums`。**所有对比性数字必须 tabular**，保证列对齐与变动可扫。
- **衬线（`--ib-serif`）：** 仅限 IB 浅色主题的页面刊头/大标题。深色终端页不用衬线。
- **Scale（px）：** 页标题 18–20；卡片标题 14–16；正文 13–14；辅助 11–12；KPI 主值 20–24。**数据行最小 12px**——便读性优先于塞满一屏。
- **多行标签对齐：** 同一横带内的 KPI/卡片标签统一 2 行 clamp（`-webkit-line-clamp: 2` + 固定 `min-height`），完整文案入 `title`。禁止因标签换行不一致导致同排数值错位。
- **大写宽字距徽标（GOV / FORMAL / ANALYTICAL / READY 等）：** 属既定终端语言，仅用于**状态/口径徽标**。禁止把它扩散为普通区块标题装饰；每个模块头最多一个。

---

## 4. Color

- **单一数值源：** `designSystem.ts`（`designTokens` + `ibTokens`）与 `tokens.css`。新色先入 token，禁止散落硬编码 hex。
- **浅色例外主题锚点：** 主色 `primary[600]` `#1850a1`（偏蓝少青）；链接/次强调 `info[500]` `#3b82f6`。仅供已登记的浅色例外或迁移中页面使用；新组件默认使用 2.2 的深色终端 token。
- **语义色统一：** `semantic.profit/up = #2d8a5e`、`loss/down = #ef4444`（浅色）；深色页用 2.2 的去饱和对应色。同一页面同一语义只允许一种色值。
- **Color Consistency Lock：** 页面级强调色一旦确定，全页统一。不允许第 7 个区块突然出现主题外的新强调色。
- **禁用：** AI 紫/霓虹渐变、纯黑 `#000` 大底、高饱和撞色、无语义的彩色装饰。
- **域内调色板（`cockpit` / `warm` / `institutional`）：** 可继续使用，但仅限视觉层（visual-only），不承载业务语义；新页面优先从 IB / dh-api 两套主色系取色，确有需要再引用域内调色板，并保持与所在页面主题的明暗与饱和度协调。

---

## 5. Spacing & Layout

- **Base unit:** 4px（`designTokens.space`）。页内垂直节奏用 8/12/16/24 组合；同一栅格行内卡片 padding 与 gap 必须一致。
- **Density:** compact 为主。深色终端页更紧（面板 padding 12–15px，行高 1.25–1.35）。
- **栅格：** 主内容 + 右栏布局用 `minmax(0, 1fr) + 固定右栏（320px 级）`；三列区大桌面 8-8-8，窄屏先折 12+12 再单栏，**禁止**压缩字号保列数。
- **不等高网格必须 `align-items: start`：** 多列卡片/新闻组内容量天然不等时，禁止默认 stretch 把空卡拉出大段空白（"空白比内容多"是版式事故）。左右双栏若追求齐底，用 `align-self: stretch` 让**卡片背景**补齐，而非留裸空白断层。
- **空态收缩：** 0 条数据的分组收缩到消息框自身高度（min-height ≤ 120px 级），居中一句话说明 + 原因，不占满等高格。
- **圆角一致性（Shape Lock）：** IB 主题全局 `2px` 锐角；深色终端页 `6px`。一页一套圆角制度，禁止混用大圆角"玩具感"卡片。
- **卡片使用纪律：** 仅当层级需要时才用卡片容器；同级信息优先用 `border-top` / `divide` / 留白分组。禁止"格子套格子"。

---

## 6. 状态与数据可信呈现

这是本系统区别于普通仪表盘的核心：**状态即内容**。

- **五态齐备：** 每个数据区块必须可感知 loading / 空 / 错 / stale / partial。无数据时禁止用演示数冒充正式口径。
- **占位防重排：** 懒加载区块的 Suspense 占位必须带与相邻面板一致的背板（背景+边线）和接近真实高度的 `min-height`，禁止 1px 占位导致内容到达时页面跳动。
- **缺值占位符：** 表格/指标缺值统一 `—`（金融惯例）。**同一缺失原因整列缺失时**，明细行用安静的 `—`，原因说明只在区块头部/汇总处出现一次——禁止把"缺 XXX"文案在每行重复。
- **状态圆点必须承载语义：** 圆点颜色 = 数据状态（通过/部分/缺口/延迟）。禁止纯装饰圆点。
- **状态信息去重：** 同一事实（如"数据已更新 04:40"）全页最多出现两处（工具条 + 底栏）。卡片内不再重复全局报告日；来源/口径细节（如 `home.snapshot`、basis 代码）收进 `title`/tooltip，不占正文版面。
- **溯源标识分层：** 端点路径、trace 类技术信息属于"证据层"，只出现在证据链/API 目录等专门模块，不散落到业务结论区。

---

## 7. 文案（Copy Register）

- **一页一语域：** 面向用户的文案统一**简体中文业务语言**。禁止中英混排的技术微标签（如 `coverage map · expanded landed domains`、`date-gated`、`domains landed`）——这类内容要么中文化，要么收进 tooltip，要么删除。
- **例外：** 既定大写状态徽标（GOV/FORMAL/READY 等）、接口路径、代码标识可保留英文，因为它们是"证据引用"而非叙述文案。
- **`·` 分隔符配额：** 单行元信息最多 1 个 `·`。超出时改分行、竖线分栏或删字段。
- **禁止微元句：** 区块标题旁不加"解释这个组件如何实现"的装饰性小字。标题 + 状态徽标已足够。
- **em-dash 禁令：** 叙述文案中不用 `—` 作修辞停顿（表格缺值占位 `—` 除外，见第 6 节）。
- **发布前自审：** 通读所有可见字符串，清除语法不通、指代不明、AI 味修辞的句子。宁要平实，不要"聪明但错"。

---

## 8. Motion

- **Approach:** minimal-functional。仅状态切换、折叠、Tab、路由过渡、hover/active 反馈。
- **无限循环装饰动效默认禁止**（扫光、网格漂移一类），**唯一豁免：经营日报首页 hero 的环境呼吸光**（`dhAmbientCanvasBreathe`，约 6.4s 缓慢透明度呼吸，无数据语义，业主 2026-07-19 拍板保留）。豁免仅限该处，不得扩散到其他页面或组件；`prefers-reduced-motion` 下必须完全静态或隐藏。
- **时长/曲线：** 用 `designTokens.motion`（fast 150ms hover / base 200ms 展开 / easeOut）。
- **必须尊重 `prefers-reduced-motion`：** 所有非 hover 动效在 reduce 下退化为静态。
- **滚动监听：** 禁止裸 `window.addEventListener("scroll")` 驱动动画；用 IntersectionObserver 或 idle-callback 门控（现有分级加载门控即此模式）。

---

## 9. Page Locks（区块顺序锁）

以下页面的信息区块顺序未经产品确认**不得调整**。

### 9.1 债券分析 / 组合工作台首页

1) 宏观市场条 → 2) 本日判断（结论文本+轻装饰）→ 3) 组合 KPI 横带 → 4) 三列：曲线与波动 | 四象策略 | 收益归因 → 5) 三列：结构/风险/今日焦点 → 6) 双列：事件日历 | 重点券表

### 9.2 经营日报首页（深色终端，2026-07 现状）

- **首屏：** 工具条（报告日/搜索/状态胶囊 ≤3 个/刷新）→ 早间决策一览（判断 hero + 来源核验）→ KPI 横带（规模/损益/久期/YTM，4 卡等高对齐）
- **下半屏（懒加载，带骨架占位）：** 宏观/日历上下文（左前瞻+过往、右政策资金面，双栏齐底）→ 市场/日历上下文 → 债券信息新闻（三组，空组收缩）→ 快捷下钻 → 重点券 + 风险暴露/增减仓 → 收益趋势 → 结构看板 → 券商研报 → 证据链路
- **右栏（320px）：** 待复核 → 待办事项 → 数据质量（含来源台账，一卡合并）
- **首屏问题：** "今日组合的可执行判断是什么、数据是否可信"，两问都必须在首屏可答。

---

## 10. Relationship to Code

- **数值单一来源：** `frontend/src/theme/designSystem.ts`（scale、语义色、motion）+ `frontend/src/styles/tokens.css`（CSS 变量）。域内扩展 token 需与本文语义一致。
- **正式金融指标：** 前端只消费后端/adapter 输出，不自行推导（与 `AGENTS.md`/`CLAUDE.md` 一致）。
- **全站版式契约（opt-in）：** 未在第 9 节锁定顺序的页面，以 [`docs/frontend-layout-contract.md`](docs/frontend-layout-contract.md) 为默认骨架。
- **验收工具：** 改动版式后跑 `npm run test`（相关域）、`npm run debt:audit`、浏览器 1440×900 基准截图对比；不等高网格、状态去重、文案语域属于审查必查项。

---

## 11. Anti-Patterns（一票否决清单）

1. 页内主题翻转（浅色页中插深色区块，反之亦然）；存量浅色页必须按整页边界迁移
2. 等高 stretch 造成的空卡大留白；左右栏裸空白断层
3. 同一状态文案在页面 3 处以上重复；每行重复同一缺失原因
4. 无限循环装饰动效（§8 已豁免的首页 hero 呼吸光除外）；不尊重 reduced-motion
5. 中英混排技术微标签出现在业务叙述位
6. 单行 2 个以上 `·`；区块标题旁的装饰性微元句
7. 高饱和语义色直接用于深色终端页
8. 无语义装饰圆点；AI 紫渐变；纯黑大底
9. KPI 标签换行导致同排数值错位
10. 1px 懒加载占位造成内容到达时重排
11. 用随机演示数据冒充正式口径

---

## 12. Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-04-27 | 引入根目录 DESIGN.md，债券分析页以完稿为权威 ±5% | 统一协作与实现验收 |
| 2026-04-27 | 延续系统字体栈，不强制外联展示字体 | 内网可用性，减少加载变量 |
| 2026-05-29 | 经营驾驶舱首页改为分层卡片+更大留白 | 解决格子套格子拥挤感 |
| 2026-06-12 | 引入 IB 浅色 restyle（ibTokens：纸底/深栏/衬线刊头/2px 锐角） | 机构感统一视觉语言 |
| 2026-07-07 | 全文重写：正式承认「IB 浅色 + 深色终端」双主题体系并各自定 token 权威；新增状态呈现、文案语域、防重排、反模式清单 | 旧文档只覆盖浅色体系，与首页深色终端现状脱节；沉淀本轮首页整改（KPI 对齐、状态去重、动效移除、空态收缩、中文化）为可审查标准 |
| 2026-07-07 | `cockpit`/`warm`/`institutional` 域内调色板继续保留使用，定位为 visual-only；新页面优先 IB / dh-api 取色 | 存量页面依赖广，强制冻结成本高；以"优先级引导"代替"禁令" |
| 2026-07-19 | 经营日报首页 hero 呼吸光（`dhAmbientCanvasBreathe`）由"违规待删"改为**正式豁免保留** | 业主明确表态喜欢该动效；限定单点豁免 + reduced-motion 静态化，避免禁令被整体架空 |
| 2026-07-19 | `/market-data` 首屏 command sheet **保留深色岛**（§2.4）；主体仍 IB 浅色；深色岛收口到 `--dh-api-*` | 业主拍板保留交易终端扫读锚点；用书面单点豁免代替"整页改浅/整页改深"；禁止自制第三套深色 palette 扩散 |
| 2026-07-19 | 现金流页久期缺口色：正缺口**禁止**映射 `--ib-up`；默认中性次级墨色（`--ib-ink-secondary`），结构偏离需强调时用 `--ib-warn`；负缺口可用 `--ib-down` 仅标方向 | ALM 正缺口≠经营利好；避免绿=好误导；业主采纳评估推荐 |
| 2026-07-19 | 余额分析风险/告警红由页内亮红（如 `#c5162e`）**收束到 `--ib-down`** | 浅色页语义色统一走 IB；业主采纳评估推荐 |
| 2026-07-19 | 宏观工具箱从 §2.2 深色名单移除，保持 IB 浅色 | 工具表单型非驾驶舱；与全局统一方案 D1-b 一致 |
| 2026-08-09 | 全系统视觉默认与最终收敛方向改为深色机构终端；IB 浅色降级为受控例外 / 存量迁移状态；`/product-category-pnl` 锁定深色完成版 | 业主最新确认“全系都应该是深色系”；防止旧设计文档或历史浅色快照再次覆盖已验收页面。此前与本条冲突的浅色默认、宏观浅色和市场数据深色岛决定均视为历史记录，不再作为当前实现权威 |
| 2026-08-11 | 视觉 token 审计基线 `tokens.css` 105→106、`designSystem.ts` 203→205：承认 8-09 深色收敛新增的 `--ib-accent-hover` 深色重映射 token，并把 `theme.ts` 链接 hover/active 裸 hex 收敛为 `dhApiTokens.color.blueHover/blueActive`（theme.ts 归零）；经营日报延迟区块 14 处 8/5/4px 圆角按 Shape Lock 归一为 `var(--dh-api-radius)` / 2px | 8-09 “restore dark route foundation / confirmed option two” 两笔提交未跑视觉审计即落库；新 token 属业主拍板的深色收敛产物，且 §10 规定数值单一来源就在 tokens.css/designSystem.ts，故基线在这两个 token 权威文件上抬升；散落页面里的 hex 与偏格圆角按 §4/§5 收敛而非放宽基线 |
| 2026-08-12 | 经营日报首页数据功能五件套（归因徽标 / Top5 集中度 / 超额方向色 / 各期限 DV01 条 / 待复核事项预览）的**二轮评审修复**：DV01 条全量展示后端固定 9 桶（此前 slice(0,8) 静默丢 30Y 长端），条宽计算入 view builder、无数值桶不画最小条；KRD 与事项预览的 loading/error/stale 以行内状态行露出（§6 五态，empty 仍收缩）、事项读取失败时不再落入「暂无治理待办」空文案；风险概览用 Top5 集中度**替换**与首屏 KPI 带完全重复的 DV01 格（§6 去重，保持 5 格避免窄面板数值截断）；事项预览来源标注「余额分析台账 · 截至日期」只在首行出现、次行显示行级事由（§6 去重）；徽标与 KRD 数值 11→12px（§3 数据行底线）；900-1024 断点隐藏归因徽标行保住「进入专题页」链接（92px 预算内被 overflow 裁切）；`data-tone="flat"` 补 muted 样式 | 十人评审后业主要求「再次 review」；五名专项评审（业务口径/React/架构/设计合规/视觉走查）发现 30Y 丢桶属业务正确性缺陷、静默吞态违反 §6、DV01 格重复违反 §6 去重；同轮完成 KRD 客户端注册进 homeSupplementalClient（首页轻量 chunk 架构）与余额日期查询 key 补 mode 维度 |
| 2026-08-12 | 经营日报首页 Nocturne 换肤的**配套排版与微交互**（同日实施，补记范围）：支撑带三面板等高对齐（40px 头线 / 28px 次行线 / 底边齐平、内部行均分）、英文眉标删除、覆盖计数收纳为行内徽标、长政策说明收进行级 title；KPI sparkline（无真实 history 不画线，禁用常数兜底序列）、KPI 变动色按数值符号取向（warning 保留琥珀）、组合透视分布微条（行底 2px）、国债收益率期限小图（按 1/3/5/10 年真实比例定位）、收益趋势标题走势线；懒加载区块一次性入场显现、KPI 数字滚动（900ms，测试与 reduced-motion 直显终值）、刷新旋转指示与 toast；持仓抽屉 portal 补 `data-moss-theme-scope` 防主题逃逸；十人评审后修复：导航壳背景 `!important` 压制、900-1024 断点隐藏 KPI 走势线、分布行 11px/数值 12px、工具栏控件 10→11px、负零变动归中性 | 业主要求 review+优化后落地；「仅换肤」原记录范围失真，按审计要求补记；假走势线违反 §6「禁止演示数冒充」故一并修正 |
| 2026-08-11 | 经营日报首页采纳 **Nocturne 换肤（仅换肤，不动版式）**：页面与所属工作台外壳切换为 Nocturne 色板（底 `#161826`、卡面 `#232532`、描边 `#3f424d`、唯一强调蓝紫 `#9184d9`、低饱和绿 `#5abd99` / 红 `#d97b6c`（自 `#de8577` 加深一档与琥珀拉开） / 琥珀 `#d5b26e`），`--dh-api-radius` 页面级重映射为 8px；**涨跌色统一为绿涨红跌**（`up/ok`=绿、`down/bad`=红，KPI 变动/收益率曲线/资金利率行翻转，持仓变动矩阵原本即绿增红减不变）；色板唯一定义入 `tokens.css` 的 `[data-moss-theme-scope="dashboard-home"]` 块（含 shell grid `:has()` 作用域），页面模块 CSS 全部走 `--nct-*`/`--dh-api-*` 引用；tokens.css hex 基线 106→122 | 业主 2026-08-11 确认设计交接包「三个布局方向设计评审」（Nocturne 三方向）为首页视觉参考并选定 1a 方向色彩语言，同时明确**首屏排版布局保持现状不重构**；换肤同步消除同页两套涨跌色冲突（原 KPI `up→红/down→绿` 与持仓矩阵 `增→绿/减→红` 同屏相悖，违反 §4 Color Consistency Lock）；蓝紫 accent `#9184d9` 为业主单点批准的低饱和蓝紫，不解除 §4 对高饱和 AI 紫（audit FORBIDDEN 列表）的禁令，其他页面不得自行引用；8px 圆角属 Nocturne 圆角制度、页内统一，符合 §5 一页一套圆角 |
| 2026-08-12 | 总账损益 `/ledger-pnl` 按业主指示照首页 Nocturne 标准实现：`tokens.css` 与 `workbenchDeferredChrome.css` 均以选择器别名扩展到 `[data-moss-theme-scope="ledger-pnl"]`（**无新增色值**，铬件块与 macro-toolkit 共用）；页根声明 scope + `theme-dh-api`，两个 antd 抽屉补同一 scope 防主题逃逸，深色 owner 仍由 `ThemedRouteBoundary` 独占（页根重复声明 `data-moss-theme="dark"` 会让 `live-route-dark-theme` 判定出两个 owner）；7 个样式文件约 371 处 `--ib-*` 直接改走 `--dh-api-*`（`--ib-*` 在路由边界即被算成钢蓝字面值再继承，Nocturne scope 翻不动），77 处 `var(--ib-radius, 2px)`、2 处 `--ib-radius, 6px`、3 处 `--moss-radius-sm`、8 处 pill `999px` 归一为 `var(--dh-api-radius)`（8px），24px 数字圆标改 `50%`，候选面板装饰性 3px 顶条删除、满饱和强调块换软底+强调描边；页标题 32→20px、副文 15→12px、模式徽标去大写宽字距，工具条改 32px 控件并把口径说明移出 label（原先塞在 label 内把两个下拉基线顶歪）；9 个章节统一编号分区头（CSS counter + 眉标 + 发丝线 + 五态状态位）并补 `scroll-margin-top`；KPI 由五张独立卡改单框五格等高横带（发丝竖分割、等宽数值、标签 2 行 clamp、1280/720 断点），表体 12px / 表头 11px / 发丝底边 / 数值右对齐走等宽栈；**功能审计结论从页底移到首屏 01「当日结论」**并入章节导航；后端枚举中文化（`ready · candidate` → `可用 · 候选口径`，未登记枚举原样透出）；补展示 `/api/ledger-pnl/data` 已返回但页面未用的 `summary.total_pnl`/`count`（按 `data_status` 收敛，避免把后端补零当真零） | 业主 2026-08-12 指示「按首页标准实现」，路径沿用同日 macro-toolkit 已验收的 scope 别名方案；首屏结论上移落实 §9「首屏一问」与 `frontend/AGENTS.md`「主结论首屏可见」；KPI 卡上 `MTR-LPN-001 / pending_confirmation=true` 经业主同意**保留可见**仅做视觉降权（`LedgerPnlPage.test.tsx` 断言其为治理必显项），未收进 tooltip；同轮修掉两处既有缺陷——`LedgerPnlSectionNav.css` 的 `--dh-api-ink-muted` 变量名不存在导致徽标色声明整条失效、章节锚点缺 `scroll-margin-top` 致跳转被粘顶导航压住。验收：视觉 token 审计 passed（无基线增长，off-radius 179/225），`live-route-dark-theme` 的 `ledger-pnl` 通过，`LedgerPnl` 301 测试通过 |
| 2026-08-12 | 宏观工具 `/macro-toolkit`、`/macro-observation`（同组件）按业主指示照首页 Nocturne 语言重构：`tokens.css` Nocturne scope 以选择器别名扩展到 `[data-moss-theme-scope="macro-toolkit"]`（无新增色值），页面 CSS 收敛旧 dh-api 钢蓝 rgba 软色与浅色遗产（白色半透明底、浅色 palette-step、白内阴影）到 var/color-mix 链，圆角随 `--dh-api-radius`（8px），分区头对齐首页编号语言（CSS counter 01/02…+ 眉标 + 发丝线），KPI/表格按首页密度纪律收口，antd Tag/主按钮/开关强调色翻至 Nocturne 语义链，装饰性彩色左条删除、状态语义条统一 2px 并保持绿涨红跌语义 | 业主 2026-08-09 确认全系统深色收敛方向、2026-08-12 指示宏观工具照首页重塑；2026-07-19「宏观工具箱保持 IB 浅色」决定按 2026-08-09 条目降级为历史记录 |
| 2026-08-12 | 组合工作台 `/portfolio` 按业主指示跟进首页 Nocturne 换肤（**仅换视觉层**，不动布局几何/DOM 结构/styles.* 类名/testid）：`tokens.css` Nocturne scope 以选择器别名扩展到 `[data-moss-theme-scope="portfolio-home"]`（无新增色值）；因该路由深色 owner 为 `ThemedRouteBoundary`，钢蓝基准块以 `.workbench-shell-grid:has(.themed-route-boundary.theme-dh-api)`（0-3-0）在外壳上定义 `--dh-api-*`，同块追加同权后置的 `.workbench-shell-grid.workbench-shell-grid:has(...)` 提权别名行外壳才翻得动；页内 V3 皮肤块删除全部 `--dh-api-*` 钢蓝字面量与页底 radial 渐变（数值唯一来源回归 tokens.css scope），约 160 处 rgba/rgb 字面量按语义家族收敛为 `--dh-api-*` 的 var/color-mix 链（文件 rgba/rgb 归零），13 处硬编码 6px 圆角归一 `var(--dh-api-radius)`（8px）；卡片制度由「微蓝渐变 + 蓝光边 + 内阴影」降级为首页平卡语言（`--dh-api-line` 描边 + panel 平底 + 无阴影，hover 描边 accent 34% mix），嵌套深底面板走 panel-2/panel-3 mix；ECharts 走新增 `nocturneTokens` 常量组（`designSystem.ts`，数值源=tokens.css Nocturne scope，仅供 canvas 类消费），结构条形图与归因瀑布切换取色；激活 ant-tabs 文字被 antd cssinjs（0-4-0、注入靠后）压住，页内以重复类 0-5-0 收敛至 accent；`designSystem.ts` hex 基线 205→221（nocturneTokens 16 个 hex 镜像，因并行任务在途仅手工上抬本文件条目，未全量重写基线） | 业主 2026-08-12 指示组合页跟进 Nocturne；换肤前后 Playwright 几何审计同态对比 8 横带 + 11 代表卡 rect/cols/gap/padding 全等、docH 不变、无横向溢出，布局零变化有据；页根不重复声明 `data-moss-theme="dark"`（避免 `live-route-dark-theme` 双 owner），沿用 ledger-pnl 同款 scope 别名路径 |
| 2026-08-12 | 市场总览 `/market-overview` 按业主指示照首页 Nocturne 排版标准**重做**：`tokens.css` Nocturne scope 以选择器别名扩展到 `[data-moss-theme-scope="market-overview"]`（无新增色值）；移除 `dhLightPage` 浅色残留，工具栏/组内子导航/章节导航对齐首页 dhTopbar 语言，01-04 编号分区头统一（CSS counter + 眉标 + 发丝线），行情带由独立卡改首页同款 KPI 单框横带（发丝竖分割、等宽数值）；ECharts 色板改运行时注入（`readCssVar` 读取 scope 内 `--nct-*` 计算值，jsdom 无布局环境回退静态值），tone 业务语义保留（利率上行=警戒琥珀而非涨绿，与 §4 语义色纪律一致）；外壳钢蓝烘焙以页内三写类名 + `!important` 直引 `--nct-*` 压制（网格底/rail/rail-mark/主列 main/导航 hover 与 active 高亮） | 业主 2026-08-12 指示「按照首页的排版布局来做」；验收：定向 Vitest 209 项、Playwright smoke 与 `live-route-dark-theme`、typecheck 通过，视觉 token 审计无基线增长；提交 2b219aca |
| 2026-08-12 | Nocturne 路由外壳统一收口：实测发现 `workbenchShell.css` 的 §Dark terminal 块以 `.workbench-shell-grid:has(.themed-route-boundary.theme-dh-api)`（0-3-0）+ `!important` 把钢蓝渐变**烘焙**在外壳网格/rail/主列上，且 `tokens.css` 钢蓝基准块以同选择器在外壳上定义 `--dh-api-*`，导致 scope 别名（0-2-0）对外壳**静默失效**——除已逐页压制的 dashboard-home / portfolio-home / market-overview 外，其余 8 条已接入 Nocturne 的路由（`/macro-toolkit`、`/macro-observation`、`/ledger-pnl`、`/average-balance`、`/positions`、`/bond-dashboard`、`/liability-analytics`、`/bank-ledger-dashboard`，Playwright computed-style 探测确认 grid/rail/main/终端条全部仍为钢蓝）外壳一直是钢蓝近黑（与 Nocturne rail 肉眼难辨）。修复取**统一收口**而非逐页复制 market-overview 压制块：① `tokens.css` 三个 Nocturne 别名块的外壳选择器统一双写类名升权至 0-3-0（portfolio-home 先例推广到全部 scope），变量翻转即自动修复主列纸面、终端条与非 cockpit 路由的 rail hover/active（cockpit 壳 `/`、`/portfolio` 的激活态另有 0-4-0 `!important` 金蓝烘焙，两页已按各自页内制度验收，不在本次范围）；② `workbenchShell.css` 末尾新增「Nocturne 外壳统一收口」终层压制变量触不到的烘焙渐变（网格 radial 提亮层、rail 98% 不透明钢蓝渐变、institutional-console 金蓝装饰），终端条钢青装饰层按启动性能守卫压在 `workbenchDeferredChrome.css` 末尾分册；值全部引用 `--nct-*`、特异性与 `!important` 对齐烘焙规则并后置、不依赖动态 import 注入顺序。其中 rail-mark 一条对非 cockpit 路由是把 `transparent` 基线改为 Nocturne 实面的**视觉决定**（对齐 market-overview 验收观感），cockpit 壳（dashboard-home / portfolio-home）的 rail-mark 保持透明制度不入列表；页内既有压制块 portfolio-home / market-overview 特异性更高且取值等价保留不动，dashboard-home 页内块（0-3-0 无 `!important`）由终层接管、取值同为 Nocturne 色板。**新增 Nocturne scope 时共四处**：tokens.css 主色板/paper 别名块与终层三条选择器列表各加一行；若该路由渲染终端条（`showShellTerminalBar`），tokens.css 终端条块与 `workbenchDeferredChrome.css` 分册再各加一行（`theme.test.ts` 有列表 parity 断言护栏）。已知残留：page-v2 面板烘焙（`.moss-page-v2-*`）不在本次范围，`/positions` 首屏结论块仍为钢蓝渐变，待后续收口 | 逐页复制压制块会让 10 个 scope × 4 条规则漂移且新路由必然重蹈覆辙，统一层一处收口；非 Nocturne 深色路由（`/risk-overview`、`/risk-tensor` 对照实测）钢蓝外壳保持原样；验收：13 路由探测对照全数翻转/保持、修复路由 1440×900 截图无明暗拼接、`WorkbenchShell`/`liveRoute` Vitest 91 项、`live-route-dark-theme` 全量、lint/typecheck 通过、视觉 token 审计无基线增长；三模型（Opus/GPT/Grok）交叉审查后按共识修正 rail-mark 列表、维护指引与本条措辞 |
| 2026-08-13 | Nocturne 外壳收口**两项已知残留修复**（承接上条）：① page-v2 面板烘焙——`workbenchShell.css` §83-94 以 0-4-0 `!important` 把钢蓝渐变 `linear-gradient(rgba(16,26,42,.98), rgba(12,20,33,.98))` 与结论左边线 `rgba(114,167,220,.72)` 烘焙在 `.moss-page-v2-surface / -evidence-panel / -decision-hero__conclusion` 上（`/positions` 首屏结论块实测确认）；收口终层追加两条压制：背景压平 `var(--nct-surface)`、结论左边线 `var(--nct-accent)`，烘焙里的 `border-color: var(--dh-api-line)` 与 `box-shadow: none` 经变量翻转已是 Nocturne 值（实测 border 为 `--nct-ring`）不重复声明；scope 列表与 grid/rail 同构全量 10 条（未渲染 page-v2 类的路由 `:has()` 命中但零效果，保 parity 可断言），`:is()` 合并三类目标维持 0-4-0 同权后置。② cockpit 壳 rail hover/active 金蓝烘焙（§cockpit 0-4-0 `!important`）——**实测定性分化**：`/portfolio` 激活项仍金蓝（金蓝渐变 + 金描边 + `inset 3px 0 0 rgba(201,165,101,.84)`）、hover 仍钢蓝 `rgba(114,167,220,.08)`；`/`（dashboard-home）已被页内 0-5-0 `!important` 块（dashboardHomeOptionTwo）接管为蓝紫、`/market-overview`（`isModuleHomePage` 亦走 `--cockpit` 变体壳）由页内 0-6-0 块等值接管，均无视觉缺陷；终层以同权 0-4-0 后置补压**三个 cockpit scope 全列兜底**（防止将来按「终层唯一权威」清理页内块时金蓝烘焙静默回归），实际生效目标是无页内块的 `/portfolio`。取值：hover accent 9%/22% 与 active accent 14% + `inset 2px 0 0 var(--nct-accent)` 对齐 tokens.css `--moss-shell-rail-active-bg` / `--moss-shadow-institutional-active` 语义，active 边线 30% mix 沿用 market-overview 验收块先例。`theme.test.ts` parity 断言同步：page-v2 背景与结论左边线两列表 = palette 全量、cockpit hover/active 两列表 = 三个 cockpit scope；`nocturneScopeSet` 增加可选选择器过滤（rail-mark 与 page-v2 背景压制 body 声明相同，按选择器区分），三模型（Opus/GPT/Grok）交叉审查全 PASS 后按建议补 market-overview 兜底行与结论列表选择器过滤 | 上条 45e00032 收口时明确记录的两项已知残留；延续「统一终层、值全引 `--nct-*` 不新增色值、特异性与 `!important` 对齐烘焙并后置」制度，不动 tokens.css 与页内既有压制块。验收：Playwright computed-style 修前/修后对比——`/positions` 结论块由钢蓝渐变+钢蓝左边线翻为 `#232532` 平底 + `#9184d9` 左边线，`/` 激活态修前后完全不变（accent 13% + inset 2px），`/portfolio` 激活/hover 翻为 accent 链，非 Nocturne 深色对照 `/pnl-by-business` 的 page-v2 钢蓝烘焙保持原样；`/positions` 1440×900 截图无明暗拼接；theme+WorkbenchShell 定向 Vitest 102 项、typecheck 通过；debt:audit 本次触碰文件零增长（`theme.test.ts` hex 15>基线14 为 HEAD 既有欠账，本次 diff 无 hex） |
| 2026-08-13 | 股票分析 `/stock-analysis` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式**）：`tokens.css` 主色板/paper 别名块以选择器别名扩展到 `[data-moss-theme-scope="stock-analysis"]`（无新增色值；不渲染终端条故终端条块不加），收口终层 grid/rail/rail-mark/page-v2 背景/结论边线五条列表各加一行，cockpit hover/active 兜底列表扩为四 scope（该页 `isStockAnalysisMinimalShell` 走 `--cockpit` 变体壳，金蓝烘焙实测命中）；**scope 声明三处**——`MarketWorkbenchFrame` 新增可选 `themeScope` 透传（frame 顶栏/子导航不在页根子树，scope 须落在 frame 根；market-data 不传零影响）、页根 `<section>` 同元素补声明（页根自带 `theme-dh-api` 类，同元素钢蓝基准块会挡掉从 frame 继承的翻转值，靠 tokens.css 源序覆盖，ledger-pnl 同款）、复核助手抽屉 body 与个股抽屉 antd panel 各补 scope 防 portal 主题逃逸；`StockAnalysisPage.css` 68 处 rgba 字面量按语义家族收敛为 `--dh-api-*` 的 var/color-mix 链（文件 rgba 归零：钢蓝/亮蓝/浅色主色 `rgba(24,80,161)` 收敛到 accent，tailwind 红琥珀与暗语义实底收敛到 red/amber/green mix，白色系收敛到 ink mix，白色 72% 实底列表项改 `--sa-dh-card-soft`），共享骨架 `dhCard:hover` 钢蓝字面量以页内升权覆盖为 accent 34% mix（对齐首页 hover 制度）；`stockAnalysisTokens.ts` 内联 `#93bfff` / 白 18% 改 var/color-mix 链；ECharts 取色（`stockChartPalette` 与个股抽屉 K 线）由 `dhApiTokens` 钢蓝常量切到 `nocturneTokens` 常量组（canvas 无法消费 CSS 变量，组合工作台先例）；`theme.test.ts` cockpit 两断言扩为四 scope，`StockAnalysisChartModel` / `StockDetailDrawer` 色值断言同步切源 | 业主 2026-08-13 指示「/stock-analysis 色系风格与首页一致」，沿用 scope 别名 + 终层收口既定路径（决议 2026-08-12「新增 scope 共四处」维护指引）；验收：浏览器 computed-style 探测外壳网格 `#161826`、rail `#131522`、frame 顶栏与卡面 `#232532`、页根 `--dh-api-bg #161826` / radius 8px、rail 激活项 accent 14% + `inset 2px 0 0 #9184d9`、个股抽屉 panel `#161826`，首屏/中部/抽屉截图与首页对照同色系无明暗拼接；theme parity 37 项、StockDetailDrawer/WorkbenchShell/PageChrome 等定向 Vitest、eslint、typecheck 通过，视觉 token 审计本次触碰文件零增长；`StockAnalysisPage.test.tsx` 4 例失败经 diff 归因为并行会话对 `marketDataClient.ts` 的在途重构（choice_stock 刷新链路），与本次换肤无关 |
| 2026-08-13 | 利率风险总览 `/risk-overview` 按业主指示照首页 Nocturne 排版标准重做：`tokens.css` scope 别名扩展到 `[data-moss-theme-scope="risk-overview"]`（无新增色值；终端条两处不加——`isModuleHomePage` 不渲染终端条），收口终层五条列表与 cockpit hover/active 补 risk-overview 行（该页亦走 `--cockpit` 变体壳；2026-08-12 收口条目中「`/risk-overview` 钢蓝外壳保持原样」的对照定位自本条起失效）；页根挂载的 `dhApiBackedHome`（dashboardHomeShell）以钢蓝字面量重写 `--dh-api-line/line-soft/*-soft` 6 个变量压掉 scope 别名，页内以 `.roV6Scope` 三写（0-3-0）变量压制块拉回 `--nct-*` 链；共享壳 `dhRefreshBtn`/`dhRailCard`/下钻行的钢蓝 rgba 硬编码不走变量、变量压制翻不动，以 TSX 本地皮肤类 + 三写前缀高特异性覆盖（平卡 + accent hover，撤钢蓝光带织纹与金→钢蓝渐变圆点）；工具栏收敛首页 dhTopbar 语言（`risk-overview-toolbar`），01-04 节题统一编号件，8 卡 KPI 改单框横带（等高分格 / 发丝竖缝 / 等宽数字 / 标签 2 行 clamp），999px 胶囊与偏格圆角归一 `var(--dh-api-radius)`（语义圆点除外），表体 12px / 表头 11px；1280 断点不降 KPI 字号（§3 主值下限 20px，本页格宽足够，首页同断点降号先例不复用）；「Drill-down」英文眉标删除、「血缘 · LINEAGE」中文化、03 节题 meta `·` 超配额改斜杠（§7）；缺值占位字面量归一 EM_DASH（riskHomeAdapter 的 `"-"` 与页面 `"—"`）；英文 disclosure 原文零改动、容器由截断改完整可见；风险警戒琥珀/红语义保留，未译作涨跌语义 | 业主 2026-08-13 指示按 market-overview 同套方案重做；三模型（Opus/GPT/Grok）交叉审查：GPT 运行态实测抓到共享壳钢蓝残留（刷新钮 `rgba(13,23,40)`、卡 hover `rgba(114,167,220)`）与 1280 断点 KPI 实测 18.56px 违反 §3 下限，Opus 抓到 riskHomeAdapter 漏列提交集与本条目缺失，均已修复并复测（按钮/卡默认与 hover 全部翻 Nocturne 链、1280 实测 20px）。验收：theme/WorkbenchShell/RiskOverview/riskHome/ModuleWorkbenchHome 297 项 Vitest（含新增 scope 声明与工具栏断言）、`risk-overview-desktop-layout` 10/10（几何锁无需改）、`live-route-dark-theme`、typecheck 通过；debt:audit 本任务文件无基线增长；scope 共享行随并行任务提交 98f190d0 先行入库，本条对应页面实现提交 |
| 2026-08-13 | 总账损益 `/ledger-pnl` 首页标准二轮补残（业主指示「这一页也按照首页标准进行前端改造」，实测页面主体已是 Nocturne，收敛三处残留）：① 外壳治理横幅（`workbench-notice[data-notice-tone="governance"]`，「临时例外」）基础样式是浅色 cream（warning-50/200 字面量）且 Nocturne 外壳翻转后正文继承近白 ink 落在浅底上不可读，在 `workbenchDeferredChrome.css` 既有「Nocturne scope 铬件收敛」6-scope 列表（ledger-pnl / average-balance / positions / bond-dashboard / liability-analytics / bank-ledger-dashboard，其余 scope 不渲染该横幅或另有壳形态）追加横幅深色规则：琥珀软底 + 琥珀标题 + 12/11px 密度 + `--dh-api-radius`（`!important` 对齐同文件 desktop-aligned 层的 2px 锐角烘焙）；② 共享溯源面板 `FormalResultMetaPanel` 面向浅色主题（neutral-900 标题在深色页不可见、999px 徽章胶囊、16/14px 大圆角、tone 色浅色内联字面量、长 trace/version 标识溢出卡片边界）——组件徽章 tone 改经 `--formal-meta-badge-*` CSS 变量出口（内联回退值 = 原浅色字面量，浅色页面零变化，深色页可在自身 scope 重定义），共享 CSS 补 `overflow-wrap: anywhere`（溢出属任何主题下的缺陷），页 scope 内补 neutral-900→ink 重映射（pnl-by-business / product-category / portfolio-home 同款先例）、圆角归一 `--dh-api-radius`、徽章 tone 走 green/amber/red soft 链、明细 12px 密度 + 标签列 88-108px；③ 候选面板「证据与待办」与「全部指标」两视图互斥显示（hidden 切换）但沿用双列 grid 模板，可见一方只占左列留出右半页空白（§11.2），证据区改整行独占 + 来源/队列卡 `auto-fill minmax(420px)` 两列流铺、不等高卡顶对齐（§5 align-items: start） | 页面 2026-08-12 已按首页标准重做，本轮为业主复查指令下的残留收敛；治理横幅与溯源面板皆为共享件，浅色路由零影响（横幅规则仅列出的 6 scope 生效、徽章变量未定义时回退原值）。验收：Playwright computed-style 实测横幅琥珀软底/8px 圆角、面板标题 ink、徽章 Nocturne 绿链/8px、明细 12px、证据区全宽 1108px 双列、溢出卡计数 0；LedgerPnl 22 文件 317 项、FormalResultMetaPanel/WorkbenchShell/theme 72 项 Vitest、eslint（仅存量 stock-analysis 告警）、typecheck 通过；debt:audit 视觉 token 与字号地板审计均无基线增长 |
| 2026-08-13 | Nocturne 收口残留：`ThemedRouteBoundary` 兜底底色跟随 scope 色板——boundary 自带 `theme-dh-api` 类，`.theme-dh-api` 钢蓝基准块在 boundary 自己身上定义 `--dh-api-bg`，后代页根的 scope 翻转够不到（变量继承不回流），全部 Nocturne 路由的 boundary `background: var(--dh-api-bg)` 一直解析为钢蓝 `#070a12`（frame/卡片缝隙处透出，与 `#161826` 肉眼难辨故此前未察觉，stock-analysis 换肤子代理钢蓝残留扫描抓到、`/ledger-pnl` 对照实测确认为共性）；`tokens.css` 新增 boundary 收口块：`.themed-route-boundary.theme-dh-api:has([data-moss-theme-scope="…"])`（0-3-0 胜基准块 0-2-0，无需 `!important`）列表与主色板块同构全量 13 scope，`color`/`background` 直引 `--nct-ink`/`--nct-bg`（值从外壳 grid 的 `:has` 块继承，boundary 不在 Nocturne 外壳内时 fallback 回钢蓝基准）；`theme.test.ts` 新增 `boundaryOverride == palette` parity 断言（marker `background: var(--nct-bg, var(--dh-api-bg))`）。**新增 Nocturne scope 时 tokens.css 共四块**：主色板、boundary、paper、终端条（后两块按壳形态取舍） | stock-analysis 换肤验收唯一未清残留，业主指示解决；无 boundary 的路由 `:has()` 不命中即零效果，保 parity 同构可断言。验收：`/stock-analysis`、`/ledger-pnl` boundary 实测 `#070a12`→`#161826` 翻转、非 Nocturne 深色对照 `/risk-tensor` 保持 `#070a12` 不动；theme parity 25 项、`live-route-dark-theme` 全量 38 路由通过；视觉 token 审计本次触碰文件零增长（boundary 块全 var 引用无新增色值） |
| 2026-08-13 | Nocturne 铺开摩擦源收口（共享层钢蓝欠账，业主授权选最优解执行）：① `tokens.css` Nocturne 主色板块补齐 boundary 深色重映射块两个未覆盖的钢蓝字面量——`--ib-accent-hover` 重映射 `var(--nct-accent-400)`（链接 hover 亮阶，marketHome/portfolio/macro-toolkit 等多页真实消费）、`--moss-institutional-row-hover` 重映射 accent 8% mix（agGrid 与 institutional console 表格行 hover）；`--ib-rail-active-bg`/`--moss-color-primary-rgb` 等查明在 Nocturne 页无下游消费不补。② `dashboardHomeShell.module.css` 共享骨架钢蓝字面量收敛十处：`dhApiBackedHome` 的 6 个 `--dh-api-line/line-soft/*-soft` 钢蓝快照**删除**（与全局 `.theme-dh-api` 基准值完全相同的冗余快照，曾把 scope 别名压掉、迫使 risk-overview 以 `.roV6Scope` 页内拉回，删除后全局链接管：钢蓝上下文取值不变、Nocturne 自动翻转）；`dhRailCard` 背景钢蓝渐变 `rgba(13,23,40,.96)`+白光泽降级为 `var(--dh-api-panel)` 平卡（对齐 Nocturne 平卡语言）；`dhRailCard:hover` `rgba(87,166,216,.34)` 与 `dhCard:hover` `rgba(114,167,220,.25)` 收敛为 accent mix；`dhRailCardHeader`/`dhRailActionRow`/`dhDeferredIndexHead`/`dhDeferredIndexItem` 的 `rgba(36,49,73)` 深钢蓝分隔线收敛 `var(--dh-api-line-soft)`；`dhRail::before` 钢蓝底板渐变与边线收敛 bg/soft mix 链；`--dh-home-nocturne-*` 雏形字面量（`#e9e9ed`/`#75798c`/`#1a2a38`）改 `var(--nct-*, 旧值)` 链并入单一色源。消费方核查：`dhApiBackedHome` 仅 risk-overview 使用（首页已迁 optionTwo 骨架）、`dhCard` 族消费方全在 `ThemedRouteBoundary` 内（`--dh-api-*` 恒有定义，浅色页零回归风险）。已知残留：该文件 1248-1810 区间的装饰层钢蓝（hero 光带/织纹/金→钢蓝渐变圆点，唯一消费方 risk-overview 已以 TSX 本地皮肤类覆盖可见部分）与 `.dhPage` 死声明兜底块，按最小改动不动 | stock-analysis 与 risk-overview 两页接入时各自重复发现并页内打补丁的共性摩擦源，一次除根降低后续每页接入成本；antd `workbenchTheme` 钢蓝常量项经评估**不做过渡双主题机制**——业主方向是全系统收敛 Nocturne，待铺开完成后一次性切换常量即可。验收：risk-overview 右栏卡实测钢蓝渐变→`#232532` 平卡、页根 `--dh-api-line` 由钢蓝快照翻 `#3f424d`（`--nct-ring`）、分隔线翻 `--nct-line`；首页 computed-style 与截图对比零变化（雏形变量原本已被 tokens.css 外壳块压过，改 var 后取值不变）；ledger-pnl 实测 `--ib-accent-hover #b5abfc`、行 hover accent 8% 生效；theme parity 25 项与 DashboardHomeStatus 通过、视觉 token 审计全量 passed（无基线增长）、`live-route-dark-theme` 36/38 通过（macro-toolkit/macro-observation 两条失败为并行会话对该目录 19 文件拆分重构的中间态：页根 testid 60s 未渲染，与本次 CSS 变量改动无关）；tokens.css/shell 两文件连带并行在途的 `pnl-by-business` scope 行一并提交（同列表块无法拆分，保持 parity 闭包） |
| 2026-08-13 | 市场数据 `/market-data` 按业主指示照首页 Nocturne 标准整页重塑（1 个集成会话 + 10 个分区子代理并行）：**接入**走既定 scope 别名模式（无新增色值）——`tokens.css` 主色板/paper/终端条三块加 `[data-moss-theme-scope="market-data"]`（该路由渲染终端条，`workbenchDeferredChrome.css` 分册同步），`workbenchShell.css` 收口终层五条列表各加一行，boundary 块由并行会话统一补齐；scope 经 `MarketWorkbenchFrame` 的 `themeScope` 属性落在 frame 根（stock-analysis 同款）。**token 迁移**：`MarketDataPage.css` 623 处 `--ib-*` 按 boundary 重映射表转 `--dh-api-*`（`--ib-*` 在路由边界算成钢蓝字面量再继承、scope 翻不动，ledger-pnl 先例），23 处浅色 `--moss-color-neutral/primary/info/warning-*`（含深色页近黑文字的现网 bug）与 23 处浅色 rgba/图纸纹理/白内高光按语义收敛 var/color-mix 链，浅色微阴影归 none；共享 `MarketWorkbenchFrame.module.css` 全量 `var(--ib-*, 浅色fallback)`→`var(--dh-api-*)`（75 处 + 61 hex fallback 清零，深色消费方 stock-analysis 取值等价）；`LivermoreStrategyPanel.css` 独立文件 27 处 ib/21 hex/17 moss-color 清零。**图表**（“太丑”主诉）：`marketDataChartTheme` 调色板/tooltip/轴/热力色阶全量切 `nocturneTokens`（字段名不变），期限结构图改国债=accent、国开=green、Δbp 柱降权 soft 渐变沉底、图例去分页，通用时序图 y 轴万/亿缩写、日期稀疏化、图例 plain 按行数预留底部（窄卡 2 项/行、封顶 4 行），NCD 热力白→蓝浅阶改 panel2→accent 深阶+分位翻色+隐藏色条，sparkline/紧凑表/联动条形图/panelStyle 内联白卡全部随 token 链翻转。**结构性修复**：删除 legacy `grid-row: 3` 钉扎（正式利率板被钉在主列第 3 行、压过 JSX 顺序致编号错序）；期限结构适配器 `parseRatePercentText` 把 "1.14%" 解析成 raw=1.14 违反 Numeric 契约（unit="pct" 的 raw 是小数分数，后端实测 0.011217↔"+1.12%"），除以 100 归一后曲线轴从 90-240 假 bp 恢复 0.9-2.4%；主列被双侧栏挤压（1440 视口仅 ~760px）下三列主题卡 225px/曲线画布 228px 不可读，主题卡组 ≥1180 收敛两列（三列瀑布退至 ≥1720）、正式利率 ≤1560 两列+曲线独占整行（画布 228→726px）。**版式**：hero 标题去重（终端条已有大标题）+ 32px 工具条 + Select 弹层 getPopupContainer 防主题逃逸，KPI 改首页单框横带，01-04 编号分区头，右栏证据卡语义色计数网格，涨跌色统一绿涨红跌（修掉 sparkline 红涨绿跌与 ticker 翻转后的页内冲突），空态收缩 ≤120px，「点击展开」装饰句与超配额「·」清除（§7），懒加载占位带背板防重排（§6） | 业主 2026-08-13 指示「风格与首页一致，拆分 10 个子代理来做，完善图表」；CSS 预算 ratchet 收紧后余量反增（5988→约 5930 行 / 659→568 `!important`，净删 legacy override 层）。验收：market-data 全域 + 页面 + 预算 + theme parity + 深色路由契约 32 文件 195 项 Vitest 通过，typecheck / eslint / debt:audit（视觉 token 与字号地板）无基线增长；Playwright 1440×900 全页展开截图走查（正式利率曲线 726px 画布、主题卡 344px、图例不压轴、热力深阶、折叠区/Livermore/Tushare/右栏全数 Nocturne），并行会话对 api 层重构造成的 dev server 瞬时报错与本次改动无关 |
| 2026-08-13 | 业务种类损益 `/pnl-by-business` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/testid**）：接入走既定 scope 别名模式（**无新增色值**）——`tokens.css` 四块（主色板、ThemedRouteBoundary 兜底、主列纸面 paper、终端条）以选择器别名扩展到 `[data-moss-theme-scope="pnl-by-business"]`，该路由渲染终端条故终端条两处都加（tokens.css 终端条块 + `workbenchDeferredChrome.css` 分册），`workbenchShell.css` 收口终层五条列表（grid 底 / rail / rail-mark / page-v2 面板背景 / 决策 hero 结论左边线）各加一行，cockpit hover/active 固定五 scope 列表不动（本路由非 cockpit 壳）；2026-08-13 page-v2 残留修复条目以 `/pnl-by-business` 为「非 Nocturne 深色对照」的定位自本条起失效；页根 `<section data-testid="pnl-by-business-page">` 声明 scope，页面 CSS 既有 legacy `--moss-color-*`→`--dh-api-*` 桥接自动跟随翻转，页内钢蓝/浅色残留字面量按语义收敛为 `--dh-api-*` var/color-mix 链（少量）；同日一并做视觉 token 审计**基线对账**（非新增违规）：首页皮肤统一阶段1 的 MOVE 迁移把 20 个 legacy hex 从 `dashboardHomeShell.module.css` 逐字搬进 `dashboardHomeOptionTwo.module.css` 但未对账 per-file 基线，致 `debt:audit` 持续红灯，现对账为 shell 84→38、optionTwo 0→20（净 -26），理由已注记在 `scripts/audit_visual_tokens.mjs` 头部 | 业主 2026-08-13 指示「对照首页色系优化」，沿用 scope 别名 + 终层收口既定路径（2026-08-12 收口条目「新增 scope 共四处」与 2026-08-13 boundary 收口「tokens.css 共四块」维护指引）；验收项：定向 Vitest（theme parity、pnl 路由冒烟）、`npm run debt:audit` 无基线增长、Playwright/浏览器截图对照首页色板同色系（结果以本条对应提交的最终验收确认为准） |
| 2026-08-13 | 业务结构与FTP后收益分析 `/pnl-by-business-insights` 跟进姊妹页同款 Nocturne 换肤（业主指示「同步」，**仅换色，不动版式/DOM/testid**）：接入同走 scope 别名模式（**无新增色值**）——`tokens.css` 四块以选择器别名扩展到 `[data-moss-theme-scope="pnl-by-business-insights"]`（该路由渲染终端条，终端条两处都加），`workbenchShell.css` 收口终层五条列表与 `workbenchDeferredChrome.css` 分册各加一行，cockpit 固定列表不动；页根 `<section data-testid="pnl-by-business-insights-page">` 声明 scope。**--ib-\* 继承陷阱的轻量解**：本页与 KpiCard 子树全量消费 `--ib-*`，而 `--ib-*` 在 ThemedRouteBoundary 的 `.theme-dh-api` 块上即被算成钢蓝字面值再继承（ledger-pnl 2026-08-12 结论），本页不做 371 处式引用改写，改在页根按 theme-dh-api 语义映射重声明 16 个 `--ib-*`→`--dh-api-*`（含 `--ib-radius`→`var(--dh-api-radius)` 8px 圆角制度归一），令其在 scope 内重新解析、全 var 引用零字面量；**共享 pageStyles 内联表格主题化**（同日先以路由内 `!important` 校正过渡、随即真偿还）：三张分析表（集中度/负FTP持续性/份额漂移）inline 烘焙浅色字面量（白底 `#ffffff`、浅灰边、近黑深墨字，深色页呈浅色孤岛且变量翻转够不到），`pageStyles.ts` 表格样式改 `--ib-*` 变量链、原浅色字面量降级为 var 回退值（KpiCard.css 同款主题感知模式；全库消费方仅本页三表与 cashflow 的无色 modeBadgeStyle，浅色路由走回退值逐像素不变），路由内 `!important` 校正层随之删除；负FTP警示单元格原以 `designTokens.color.warning[700]` 内联条件色，改 `data-warning` 数据属性钩子 + `!important` 琥珀规则（`--dh-api-amber`，压过 tdStyle 内联 ink 色）保住警示语义，顺带移除本页对 designTokens 的唯一依赖 | 业主 2026-08-13 指示「同步」姊妹页色系；桥接式接入优于整文件引用改写：diff 最小、KpiCard 等子树组件一并翻转、与姊妹页 legacy `--moss-color-*` 桥同构。验收项：定向 Vitest（theme parity、insights 页面测试）、`npm run debt:audit` 无基线增长、浏览器截图对照首页色板（结果以本条对应提交的最终验收确认为准） |
| 2026-08-13 | 债券分析 `/bond-analysis` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/类名/testid，§9.1 区块顺序锁不动**）：接入走既定 scope 别名模式（**无新增色值**）——`tokens.css` 三块（主色板、ThemedRouteBoundary 兜底、主列纸面 paper）以选择器别名扩展到 `[data-moss-theme-scope="bond-analysis"]`（`showShellTerminalBar` 对本路由为 false 故终端条块与 `workbenchDeferredChrome.css` 分册均不加），`workbenchShell.css` 收口终层五条列表各加一行，cockpit hover/active 兜底列表扩为六 scope（本路由 `isBondAnalysisMinimalShell` 走 `--cockpit` 变体壳），`theme.test.ts` cockpit 两断言同步扩列；**scope 声明三处**——页根 `<section data-testid="bond-analysis-overview">`、`BondAnalyticsView` 的 Suspense 整页骨架 div（防加载期外壳 `:has()` 不命中闪钢蓝）、复核助手 antd Drawer 及其 body（防 portal 主题逃逸）；页面既有 `.bondWorkbenchPage` 的 `--ib-*`/`--moss-color-*`→`--dh-api-*` 页根重映射块随 scope 自动翻转，块内 19 处钢蓝/浅红/金/亮绿字面量按语义家族收敛 var/color-mix 链，页根钢蓝渐变底/顶栏渐变/alert 金琥珀/卡面白微光/`rgba(126,157,194)` 钢蓝灰线全部收敛（`BondAnalyticsViewContent.module.css` rgba/hex 归零），页内外壳 `:global(.workbench-shell-grid--bond-analysis)` 网格/rail 钢蓝烘焙**删除**（收口终层 0-3-0/0-4-0 !important 接管后为死代码）；`DV01RiskView.module.css` 七处白底/浅蓝实底收敛为页根重映射面板链、`InstitutionalCockpit/DetailSection/FilterActionStrip` 的浅色渐变与 3.5% 浅阴影收敛（阴影归 none，§2.2 不靠阴影）；`dashboardCockpit.css` 的 bond-analysis `:where` 作用域块三处浅色 rgba 收敛（toolbar 底线/eyebrow/divider）；`cockpitTokens` 的 `toneColor`/`readinessSurface`/`EYEBROW`（DOM style 消费）由浅色 designTokens 面切 `--dh-api-*` 暗底软色链；ECharts/canvas 取色全链切 `nocturneTokens` 常量组（期限结构曲线 IB accent→accent、宏观利率三线、归因/基准超额瀑布红涨绿跌保方向换去饱和值、KRD 饼与 DV01 渐变 `accent300→blue`、信用利差全组 option builder、集中度饼），DOM inline 的 `#cf1322`/`#3f8600`/`#faad14`/`#8090a8`/`#5c6b82`/`#f0f0f0` 与 designTokens neutral 系切 `var(--dh-api-*)` 字符串；明细激活 tab 文字/下划线被 antd cssinjs 钢蓝 colorPrimary 压住，`.tabRail` 三写提权收敛至 accent（portfolio-home 先例）；共享「展开」胶囊白 56% 实底与正式结果徽标 inline 浅色面页内升权/`[title$=…]`+!important 收敛（stock-analysis / pnl-by-business 先例）；`bondAnalyticsCockpitTokens`/`echartsRiskCharts`/`InstitutionalCockpit` 三个测试的色值断言同步切源；`ACTION_COLORS`/`DONUT`/`DISTRIBUTION` 分类色板（visual-only）与 `var(--x, #fallback)` fallback 不动；死代码组件（HeadlineZone/ReadinessMatrix/FuturePanel/WatchlistCard/BondKpiRow，零 import）不动 | 业主 2026-08-13 指示按首页 Nocturne 色系换肤，沿用 scope 别名 + 终层收口既定路径（2026-08-12「新增 scope 共四处」与 2026-08-13 boundary 收口维护指引）。验收：浏览器 computed-style 外壳网格 `#161826`（渐变压平 `background-image: none`）、rail `#131522`、boundary `#161826`、页根 `--dh-api-bg #161826` / panel `#232532` / blue `#9184d9` / radius 8px / 边框 `#3f424d`、rail 激活项 accent 14% + `inset 2px 0 0 #9184d9`、明细激活 tab 与 ink-bar `#9184d9`、tone 面/徽标/展开胶囊全翻暗底软色；全页扫描（首屏 + 明细展开态 1798 元素）`rgb(114,167,220)`/`rgb(24,80,161)`/`rgb(16,26,42)` 归零，`rgb(12,20,33)` 仅剩 antd cssinjs 组件基础皮肤 7 处（按钮/输入框底，2026-08-13 摩擦源条目已定不做过渡双主题、待全站收敛后一次切 `workbenchTheme` 常量）；1440×900 首屏/中部/明细截图无明暗拼接；theme parity 25 项、定向 Vitest 29 文件 248 项、`live-route-dark-theme`(bond-analysis)、eslint、typecheck 通过，视觉 token 审计本次触碰文件零增长；antd Select 日期弹层（portal，非 Drawer/Modal）主题逃逸未处理，记为残留 |
| 2026-08-13 | 收益归因 `/pnl-attribution` 按业主指示照首页 Nocturne 标准**改造重构**（3 个子代理并行：外壳接入 / 页面骨架 / 图表）：**接入**走既定 scope 别名模式（**无新增色值**）——`tokens.css` 四块（主色板、boundary 兜底、paper、终端条；该路由渲染终端条故终端条两处都加）以选择器别名扩展到 `[data-moss-theme-scope="pnl-attribution"]`，`workbenchShell.css` 收口终层五条列表与 `workbenchDeferredChrome.css` 分册各加一行，cockpit 固定列表不动；页根声明 scope + `theme-dh-api`（深色 owner 仍由 ThemedRouteBoundary 独占）。**token 迁移**走 ledger-pnl 式整量引用改写：`PnlAttributionView.css` 约 107 处 `--ib-*` 按 boundary 重映射表转 `--dh-api-*`，四个 Campisi 面板 60+ 处内联 `var(--ib-*)` 同步，浅色 boxShadow 与 `--ib-shadow`（无暗色重映射）删除，圆角与 999px 胶囊归一 `var(--dh-api-radius)`（语义圆点除外），死 CSS `advanced-table__profit/__loss`（红涨绿跌残留、TSX 零引用）删除；`--ib-serif` 衬线撤出（§3 深色终端不用衬线）。**排版对齐首页**：编号分区头 01 归因决策条（主结论首屏，治理字段标签中文化：Page status→页面状态 / Formal use→正式使用 / Owner approval→业主批准 / Closure→口径闭合，`formal_use_allowed=false` 等 value 保留为证据引用；`TPL hybrid exception`→`TPL 混合口径例外`）→ 02 口径边界 → 03 双口径归因工作台（页签行 + 环比/同比右置）→ 04 当前归因视图；产品分类摘要卡改首页 KPI 单框横带（发丝竖分割、等宽数值 20px、标签 2 行 clamp），页头 20px/12px、控件 32px、表体 12px/表头 11px；质量/降级徽标按 §6 去重收敛到决策条一处，meta strip 保留证据层六字段。**图表全链切 `nocturneTokens`**：`pnlAttributionChartPalette` 整组切换，五个图表组件 ECharts 显式取色 + 共享 `dhApiChartTheme` 内嵌钢蓝 tooltip/图例/轴线的组件侧覆盖，Campisi 面板 canvas 同步（子代理分工缝隙由集成会话补齐）；涨跌方向核对五组件原本即绿=盈利/红=亏损，无反向。**`tone.ts` 新增 `TONE_DH_CSS_VAR`**（positive/negative/warning/neutral → `--dh-api-green/red/amber/muted`）：`TONE_CSS_VAR` 发出的 `var(--ib-up)` 在 Nocturne scope 内仍解析为边界钢蓝绿 `#66b98b`，与页面 `--dh-api-green`（`#5abd99`）同页双绿违反 §4 一致性锁；按 tone.ts 自身「色板必须加在本文件」规则新增导出，TPLMarketChart 相关系数卡与两个 Campisi 面板切换，`tone.test.ts` 补方向守卫断言。**业主要求「排版整齐、不突兀」后的二轮自查修复三处**：① 产品分类/高级归因两页签的溯源元信息卡组（3 张 / 8 张，约 1–2.5 屏）从页签内容最前移至末尾，业务结论（KPI 横带 / Campisi 决策级解释）紧跟结果元信息条（§6 证据层收进专门位置、先结论后证据）；② 归因状态后端枚举中文化——KPI 未解释差异副行 `complete/incomplete`→「闭合完整/闭合不完整」、移动读数行级 `complete/partial`→「完整/部分」，未登记枚举原样透出（ledger-pnl 先例）；③ `pnlAttributionAdapter` 状态横幅 detail 去掉「生成时间」（结果元信息条与溯源卡已各露出一次，第三处重复违反 §6）并把 `·` 连接改分号（把 `·` 配额留给 PageDataSection 横幅「有效日 ·」前缀，§7 单行一个）；同轮程序化审计五页签 1440×900 无横向溢出（唯一越界为共享终端条跑马灯，外壳设计行为） | 业主 2026-08-13 指示「拆分多子代理，前端按照首页的标准进行改造重构」，沿用 scope 别名 + 终层收口 + ledger-pnl 版式语言既定路径；`TONE_DH_CSS_VAR` 取页面局部改造而非在 Nocturne 主色板块重映射 `--ib-up/down`（后者波及全部 15 scope，无业主授权不动共享层）。验收：theme parity 40 项、pnl-attribution/Campisi/图表/tone/LiveRouteRealPageSmoke 定向 Vitest 17 文件 135 项通过，typecheck / eslint / `debt:audit` 三项无基线增长，Playwright `live-route-dark-theme` pnl-attribution 通过（单一深色 owner、无浅色回归），1440×900 浏览器实测：外壳 grid `#161826` / rail 与终端条 `#131522` / boundary `#161826` / 页根 radius 8px / 语义色 `#5abd99`/`#d97b6c`/`#9184d9`，五页签（产品分类/规模利率/TPL/损益构成/高级归因+Campisi）截图全 Nocturne、绿涨红跌一致；高级归因页签一次 502 为后端瞬时冷启动（直连复测 200，加载后全量渲染正常），与前端无关 |
| 2026-08-13 | 风险张量 `/risk-tensor` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/类名/testid/区块顺序**）：接入走既定 scope 别名模式（**无新增色值**）——`tokens.css` 四块（主色板、ThemedRouteBoundary 兜底、主列纸面 paper、终端条）以选择器别名扩展到 `[data-moss-theme-scope="risk-tensor"]`（该路由 `showShellTerminalBar` 为真，终端条两处都加：tokens.css 终端条块 + `workbenchDeferredChrome.css` 分册），`workbenchShell.css` 收口终层五条列表各加一行，cockpit hover/active 固定列表不动（本路由走 `--desktop-aligned` 非 cockpit 壳）；**2026-08-12 外壳统一收口条目与 2026-08-13 boundary 收口条目中「`/risk-tensor` 非 Nocturne 深色对照路由、钢蓝外壳保持原样」的对照定位自本条起失效**（先例：risk-overview / pnl-by-business 条目同款声明）；`workbenchDeferredChrome.css`「Nocturne scope 铬件收敛」列表（终端条阴影、报告日 chip/操作员 pill、utility navlink、行情条顶线、子导航三条）扩列 risk-tensor（治理横幅四块不加——本路由无 temporary-exception 不渲染该横幅）；页根 `<section>` 同元素补 scope 声明（页根自带 `theme-dh-api` 类，靠 tokens.css 源序覆盖钢蓝基准，ledger-pnl/stock-analysis 同款）；页面既有 `.risk-tensor-page.theme-dh-api` 的 `--ib-*`/`--moss-color-*`→`--dh-api-*` 页根重映射块随 scope 自动翻转，块内补 `--ib-up/--ib-down`（KpiCard tone 走边界钢蓝绿红、页根重映射后翻 Nocturne）与 `neutral-500/600/900`→muted/soft/ink（共享溯源面板墨色，此前深色页近黑不可读）；`FormalResultMetaPanel` 徽章 tone 变量页内拉回 `--dh-api-*` soft 链（ledger-pnl/positions 先例，仅换色不动 16/14px 圆角与密度）；两处浅色阴影字面量 `rgba(22,35,46,.05/.06)` 收敛 `var(--moss-shadow-card)`（页根重映射为 none，§2.2 深色不靠阴影）；ECharts（KRD 柱状 + 风险雷达）由 `ibChartTheme`（IB **浅色**主题，深色页历史欠账）切至 `chartTheme.ts` 新增导出 `nocturneChartTheme`（`nocturneTokens` 常量组、与 `dhApiChartTheme` 同构 buildChartTheme；canvas 不消费 CSS 变量，组合工作台/股票分析先例；新增导出零波及既有消费方） | 业主 2026-08-13 指示风险工作台组三路由按首页 Nocturne 色系换肤，沿用 scope 别名 + 终层收口既定路径（2026-08-12「新增 scope 共四处」与 2026-08-13 boundary 收口「tokens.css 共四块」维护指引）。验收：theme parity 25 项、RiskTensorPage/RiskTensorUnitContract/chartTheme 定向 Vitest 119 项、eslint、typecheck、Playwright `live-route-dark-theme`(risk-tensor)、视觉 token 审计本次触碰文件零增长全部通过；浏览器实测外壳 grid `#161826`（渐变压平 background-image none）/ rail 与终端条 `#131522` / boundary `#161826` / 页根 `--dh-api-bg #161826` + radius 8px，全页 1389 元素扫描钢蓝 `rgb(114,167,220)`/`rgb(24,80,161)`/`rgb(12,20,33)`/`rgb(16,26,42)` 残留归零，雷达与 KRD 图 `#9184d9`、溯源面板 panel `#232532`/徽章绿 soft 链；tokens.css/workbenchShell.css/theme.test.ts 连带并行在途的 module-workbench-home scope 行一并提交（同列表块无法拆分，保持 parity 闭包） |
| 2026-08-13 | 集中度监控 `/concentration-monitor` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/类名/testid/区块顺序**）：接入走既定 scope 别名模式（**无新增色值**）——`tokens.css` 四块以选择器别名扩展到 `[data-moss-theme-scope="concentration-monitor"]`（`showShellTerminalBar` 为真，终端条两处都加），`workbenchShell.css` 收口终层五条列表各加一行，cockpit 固定列表不动（非 cockpit 壳）；`workbenchDeferredChrome.css` 铬件收敛列表扩列 concentration-monitor，且**治理横幅四块**（本路由 `governanceStatus: temporary-exception` 渲染「临时例外」横幅，2026-08-13 ledger-pnl 条目的琥珀深色规则此前仅 6 scope）同步扩列；页根 `<section>` 补 scope 声明（页根无 `theme-dh-api` 类，深色 owner 是 ThemedRouteBoundary）；页面此前**无页根变量重映射块**——`--ib-*`/`--moss-color-*` 在 boundary 上已被算成钢蓝字面值再继承（ledger-pnl 2026-08-12 结论），页根新增 20 变量语义映射重声明（`--ib-paper/surface/surface-muted/hairline/ink×3/accent/accent-surface/radius/shadow/warn/up/down/gold` + `danger-50/neutral-100/text-*`，pnl-by-business-insights 同款桥接、全 var 引用零字面量），页面 CSS 与 module CSS 全 var 化零字面量故无逐处收敛项；KpiCard tone、限额表 breach 红/near 琥珀、PageStateSurface/PageDecisionHero/EvidencePanel 均经桥接链自动翻转（限额语义为风险预警非涨跌，方向不变仅去饱和） | 业主 2026-08-13 指示风险工作台组三路由按首页 Nocturne 色系换肤（第 2/3 页），沿用 scope 别名 + 终层收口既定路径。验收：theme parity 25 项、ConcentrationMonitorPage 定向 Vitest（含并行会话同日 display_limits 后端下发重构的 6 项，当前树全绿）、eslint、typecheck、Playwright `live-route-dark-theme`(concentration-monitor)、视觉 token 审计零增长全部通过；浏览器实测外壳 grid `#161826` / rail 与终端条 `#131522` / 页根 `--dh-api-bg #161826` + radius 8px / hero 卡面 `#232532` / 治理横幅琥珀 soft 底 + `#d5b26e` 标题 + 8px 圆角，全页 966 元素钢蓝扫描残留归零；真实链路后端未下发 display_limits，限额区按并行重构显示「限额未下发」空态（表格 tone 着色路径由 Vitest 断言覆盖）；本页 TSX 与测试文件混入并行会话同文件在途的 display_limits 重构（无法按行拆分），连同其自洽依赖（contracts/bondAnalytics 类型、bond-analytics/types 转出、bondAnalyticsMockClient mock 字段）一并提交并注明 |
| 2026-08-13 | 现金流预测 `/cashflow-projection` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/类名/testid/区块顺序**；风险工作台组第 3/3 页）：接入走既定 scope 别名模式（**无新增色值**）——`tokens.css` 四块以选择器别名扩展到 `[data-moss-theme-scope="cashflow-projection"]`（`showShellTerminalBar` 为真，终端条两处都加），`workbenchShell.css` 收口终层五条列表各加一行，cockpit 固定列表不动（非 cockpit 壳）；`workbenchDeferredChrome.css` 铬件收敛列表与**治理横幅四块**（本路由 temporary-exception）扩列；页根 `<section>` 补 scope 声明（深色 owner 是 ThemedRouteBoundary）；页面 `--cf-*` 别名全量引用 `--ib-*`，`--ib-*` 在 boundary 上被算成钢蓝字面值再继承，页根（module.css `.page`）新增 24 变量语义映射重声明（15 个 `--ib-*` + text-* 三级 + DataQualityBanner 的 danger-200/50/800 与 warning-200/50/900，pnl-by-business-insights 同款桥接、全 var 引用零字面量）；**久期缺口色语义红线（DESIGN 2026-07-19 决议）保持**：CSS 类语义分配不动——正缺口 `decisionDeck_positive`→`--cf-accent`(=warn 琥珀)、小正缺口 `metricCell_gapPositive`→`--cf-subtle`(中性次级墨)、负缺口→`--cf-negative`(down 红仅标方向)，换肤后仅色值随 Nocturne 去饱和（琥珀 `#d5b26e`/红 `#d97b6c`/绿 `#5abd99`），浏览器实测正缺口态左边条与 KPI 值均为 `#d5b26e` 琥珀非绿；ECharts 月度投影由 designTokens 浅色字面量（success/danger/info-500 主系列 + neutral-200/300/500/700 轴/图例，深色页近黑文字历史欠账）切 `nocturneTokens` 常量组（资产流入=绿/负债流出=红/累计净现金流=accent，方向语义不变；canvas 不消费 CSS 变量先例）；数据源模式角标内联浅色（success/primary-50 底 + displayTokens.apiMode 前景）改 `var(--dh-api-green/blue[-soft])` 字符串（本组件仅本深色路由消费）；antd Select/Table/Alert/Spin 走 cssinjs `workbenchTheme` 钢蓝常量（`ant-select-selector`/`ant-table` 底 `rgb(12,20,33)` 2 处），按 2026-08-13 摩擦源条目既定不做过渡双主题、待全站收敛后一次切常量，Select 日期弹层 portal 主题逃逸同 bond-analysis 记为残留 | 业主 2026-08-13 指示风险工作台组三路由按首页 Nocturne 色系换肤（本条收口第 3 页），沿用 scope 别名 + 终层收口既定路径。验收：theme parity 25 项、CashflowProjectionPage/cashflowProjectionPageModel/cashflowProjectionAdapter 定向 Vitest 54 项、eslint、typecheck、Playwright `live-route-dark-theme`(cashflow-projection)、视觉 token 审计零增长全部通过；浏览器实测外壳 grid `#161826` / rail 与终端条 `#131522` / 治理横幅琥珀 soft / 页根 `--dh-api-bg #161826` + radius 8px / 正缺口决策条琥珀 / 24M 轨迹负值月 down 红 / 月度投影三系列 `#5abd99`/`#d97b6c`/`#9184d9`，全页 462 元素钢蓝扫描仅剩上述 antd cssinjs 2 处既定残留 |
| 2026-08-13 | 绩效工作台 `/performance` 与报表与数据 `/reports`（共用组件 `ModuleWorkbenchHomePage`，kind=performance/governance）按业主指示照首页 Nocturne 排版标准重做：单 scope `module-workbench-home` 服务双路由（沿 macro-toolkit 单 scope 双路由先例），`tokens.css` 主色板/paper 与收口终层五条列表、cockpit hover/active 各加一行（终端条两处不加——两路由属 `isModuleHomePage` 不渲染终端条）；工具栏收敛首页 dhTopbar 语言（20px 页题 + 一问副题 + 口径边界声明可见于 meta 行 + 右侧 ≤2 胶囊，去左竖条装饰），4 KPI 卡改单框横带（发丝竖缝 / 等宽 tabular / 标签 2 行 clamp / EM_DASH 走 tone 色 / compact 按长度判据 17px 防长读数截断、中文状态词保持 §3 主值 20px 下限），状态条带改首页产品分类摘要条语言，01/02/03 编号节题，明细列表改表格（表头 11px / 表体 12px / 发丝底边 / 数值列 ellipsis 防溢出），圆角归一 `var(--dh-api-radius)`；fail-closed 治理文案（读取失败 / 暂无数据 / 不使用前端补数）内容与语义零改动；随重排移除组件内 market/risk/portfolio 三个 kind 的死代码渲染分支（routes 仅以 performance/governance 消费本组件，三路由各有专属页面；useQuery 读链路与 `moduleHomeModel` 五 kind 能力保留），同轮把 `/risk-overview` 就绪合同从本组件死分支锚点（`module-home-risk-evidence`，靠永不执行的分支假性满足）改指向真实页面 `RiskOverviewPage`（`risk-overview-page`/`risk-overview-evidence`） | 业主 2026-08-13 指示继续按首页标准重做工作台首页；三模型（Opus/GPT/Grok）交叉审查三方 FAIL 后修复：就绪合同过期（三方一致，Grok/Opus 实跑复现 LiveRouteReadiness 失败）、compact KPI 16px 按语种触发违反 §3 且长数值不降号会被截断成另一个数字（GPT 阻断 + Opus 给出首页 17px 长度判据先例）、KPI 标签单行 ellipsis 不符 §3 两行 clamp、summary 口径声明降级进 tooltip 违反 §6、`!important` 与窄屏分隔线/数值溢出问题，均修复。验收：theme/WorkbenchShell/ModuleWorkbenchHome/DarkThemeRouteContracts/LiveRouteReadiness/RouteRegistry 18 文件 330 项 Vitest 全绿，运行态复核 summary 可见、KPI 主值 22.32px、标签 clamp=2，两路由外壳与页面翻转 Nocturne、对照 /pnl 不受影响；typecheck/lint 0 error、debt:audit 无基线增长 |
| 2026-08-13 | 市场组四路由 `/bond-trading-desk`、`/cross-asset`（`/cross-asset-drivers` 重定向同组件）、`/market-finance`、`/news-events` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/类名/testid/文案**；共享文件四块 scope 行由并行批量接入提供，本轮零改动）：四页根各补 `data-moss-theme-scope` 声明——bond-trading-desk 经 `PageV2Shell` 新增可选 `themeScope` 透传（`MarketWorkbenchFrame` 同款先例，不传零影响；页根即 shell 根无处挂属性），market-finance 落在既有 `theme-dh-api` 内层 div 同元素（源序覆盖），cross-asset / news-events 落在页根 section；`--ib-*` 在 boundary 上被算成钢蓝字面值再继承（ledger-pnl 结论），bond-trading-desk（module.css `:global` 块 8 变量 + `--moss-page-ink×3` 接管 PageHeader 浅色兜底，platform-config 先例）与 news-events（15 变量 + `--moss-color-text-primary` 供 PageAsyncSection）补页根重映射块，market-finance 既有块补 `--ib-gold` 与 text-primary/secondary（PageStateSurface definition-pending 左沿与标题墨）；cross-asset 走既有 `crossAssetTerminalTheme.css` 重映射层自动翻转，另补 `--moss-radius-sm: var(--dh-api-radius)`（页面卡面约 45 处直用全局 2px token 的偏格圆角归一），页 CSS 13 处 rgba 字面量全数收敛 var/color-mix 链（文件 rgba 归零：slate 阴影→`--ca-shadow`、amber 圈→warning-fill mix、市场纸带白底 78%→`--ca-card` mix、蓝 accent 边→`--ca-accent`/`--ca-slate` mix、黑 hover 阴影→`--ca-shadow-hover`）；`crossAssetChartTheme.ts` TERMINAL 常量组由 `dhApiTokens` 钢蓝切 `nocturneTokens`（canvas 不消费 CSS 变量，股票分析/risk-tensor 先例；waterfall 偏紧=red 的「利率上行=警戒」业务语义保持，仅换色相），四个测试文件断言同步切源；bond-trading-desk 工具条 antd 三控件 `colorBgContainer` 钢蓝实底页内压回 `--dh-api-panel`（positions CustomerDetailModal 固定列先例） | 业主 2026-08-13 指示市场组四路由按首页 Nocturne 色系换肤，沿 scope 别名 + 页根重映射既定路径。验收：外壳实测四路由（含 /cross-asset-drivers）grid `#161826` / rail 与终端条 `#131522` / 页根 `--dh-api-bg #161826` + radius 8px，全页钢蓝扫描（六族）残留归零（cross-asset 展开全部延迟分段 2063 元素）；cross-asset 23 文件 236 项 + 三页与原语 19 文件 104 项 + theme 13 文件 62 项 Vitest、Playwright `live-route-dark-theme` 四路由、typecheck（首轮失败为并行在途文件、复跑 0 error）、eslint 0 error、debt:audit 本批文件零增长（cross-asset 页 CSS rgba 13→0 净减）全部通过。已知残留：外壳治理横幅深色收敛列表（`workbenchDeferredChrome.css` 治理横幅四块，禁改共享文件）缺 market-finance / news-events / cross-asset 三行，三路由横幅现浅底近白字不可读，待共享层收口；antd 下拉弹层 portal 主题逃逸与 Tag 预设色为全站既定残留；cross-asset 附录 WatchList/EventCalendar/PageOutput 少量 designTokens 浅灰内联为历史欠账非钢蓝族 |
| 2026-08-13 | MOSS Chat `/agent` 按业主指示照首页 Nocturne 色系换肤（**仅换色，不动版式/DOM/类名/testid**；全站 themed 路由最后一个未接 scope 的页面，scope 定名与路由一致 `agent`）：接入走既定 scope 别名模式（**无新增色值**）——`tokens.css` 四块（主色板、boundary 兜底、paper、终端条；本路由非 cockpit 壳走 `--desktop-aligned`、`showShellTerminalBar` 为真故终端条两处都加）各加 `[data-moss-theme-scope="agent"]` 行，`workbenchShell.css` 收口终层五条列表各加一行，cockpit hover/active 固定列表不动，`theme.test.ts` parity 断言为推导式零改动；**scope 声明只落 workbench 变体**——页根 `<section class="agent-workbench-shell">` 由 `EmbeddedAgentCopilot` 双态复用（嵌入态 AgentPanel 在其他页面抽屉渲染同一批类名），scope 按 `isEmbedded ? undefined : "agent"` 条件声明，嵌入态与共享基线不动、跟随宿主页主题；深色 owner 仍由 ThemedRouteBoundary 独占（页根不声明 `data-moss-theme="dark"`）；页面无 antd portal（快捷入口抽屉为原生 details，无主题逃逸面）。**页面收敛全部限定 scope 内**（`AgentWorkbenchPage.css` 末尾新增 `[data-moss-theme-scope="agent"]` 段）：页根桥接重声明 7 个 `--moss-color-*`（text 三级 / link / card-bg / border-default / surface-base）+ 7 个 `--ib-*`（surface / hairline / ink 三级 / accent，`--ib-radius`→`var(--dh-api-radius)` 8px 圆角制度归一）——桥变量在 boundary 上已被算成钢蓝字面值再继承（ledger-pnl 2026-08-12 结论，实测工作流命令 span 钢蓝 `#72a7dc` 证实），桥接后 GitNexus 结果视图 / 仓库记忆 / 通用卡三个组件 CSS（全 `--ib-*` 引用）自动翻转；约 60 条浅色字面量按语义家族收敛 `--dh-api-*` var/color-mix 链的 scope 内覆盖（白卡→panel、浅蓝底→accent 8-12% mix、hover 描边→accent 34% mix、红/绿/琥珀 callout→sem 40% mix + soft 底、浅色阴影→`var(--moss-shadow-card)`=none）；`--moss-color-primary-rgb`/`success-rgb` 通道值在 scope 未重映射，按既定结论不补通道变量、消费处改 color-mix 链（发送钮 / 焦点环 / 进行中步骤 / 状态点），脉冲动画 `agentPulse` keyframes 经 `--agent-pulse-ring[-wide]` 变量出口下发（回退值=原字面量、嵌入态零变化，FormalResultMetaPanel 先例）。**readiness 外壳面收敛**（`workbenchDeferredChrome.css`；agent 是 Nocturne 路由中唯一 readiness≠live 的 scope）：gated 就绪横幅（`data-readiness="gated"`，非治理 tone）与工作台导引 hero 基础样式为浅色字面量（cream / 白渐变），Nocturne 翻转后近白 ink 落浅底不可读，按治理横幅同款制度仅收敛颜色（横幅琥珀 soft 链、hero panel 平底 + line 描边、hero gated 徽章 `--moss-color-warning-600`→amber；密度/字号不动）；GITNEXUS_GROUP_COLORS 分类色板（visual-only TSX 常量）按 bond-analysis 先例不动 | 业主 2026-08-13 指示收口最后一个未接 Nocturne 的 themed 路由，沿用 scope 别名 + 终层收口既定路径（2026-08-12「新增 scope 共四处」与 2026-08-13 boundary 收口「tokens.css 共四块」维护指引）。验收：theme parity 25 项、Agent 定向 Vitest 14 文件 248 项、eslint、typecheck、视觉 token 审计全量 passed（触碰文件零增长）；`live-route-dark-theme` spec 无 agent 条目（gated 隐藏路由不入默认可达清单）按约定报告不加；浏览器实测（独立 dev server 显式 `VITE_MOSS_AGENT_FRONTEND_ENABLED=true`、Playwright 1440×900）：外壳 grid `#161826`（渐变压平）/ rail 与终端条 `#131522` / boundary `#161826` / 页根 `--dh-api-bg #161826` + radius 8px + accent `#9184d9`，gated 横幅琥珀 soft + `#d5b26e` 标题、hero 卡面 `#232532` + ink 标题 + 琥珀徽章、composer panel mix + `#3f424d` 描边，全页 338 元素钢蓝扫描（四族 rgb/rgba 前缀）归零，首屏与整页（含展开的金融工作流 / 研究入口抽屉）截图无明暗拼接；默认降级态（开关关闭→NotFound 无 scope）实测外壳保持钢蓝渐变零回归 |
| 2026-08-13 | 16 条长尾路由 Nocturne 批量换肤收口（**仅换色，不动版式/DOM/类名/testid**；资产负债 / 市场交易 / 损益 / 绩效平台四组并行子代理，每组 4 路由，本条为批量总条目）：资产负债组 `/balance-analysis`、`/balance-movement-analysis`、`/decision-items`、`/cashflow-projection`（增补 antd Select/Table 页内 scoped 覆盖），市场组 `/bond-trading-desk`、`/market-finance`、`/news-events`、`/cross-asset`（细节已单独立条），损益组 `/pnl`（scope=pnl）、`/pnl-bridge`、`/operations-analysis`、`/product-category-pnl`（深色完成版锁零版式改动、仅色板收敛 + 月度经营分析分支根补 0-1-0 重映射块），绩效平台组 `/kpi-performance`（scope=kpi）、`/team-performance`、`/platform-config`、`/cube-query`；各页沿既定 scope 别名 + 页根重映射桥路径（`--ib-*`/`--moss-color-*` 在 boundary 上被算成钢蓝字面值再继承的 ledger-pnl 结论贯穿全批），盈亏着色统一走 `TONE_DH_CSS_VAR` 主题感知入口（浅色 `semantic.profit/loss` 直接引用 13→3），图表/canvas 常量组切 `nocturneTokens`（cross-asset TERMINAL 组、product-category 瀑布图与对比图、AG Grid 变量链；偏紧=红警戒语义保持），KPI 三弹窗（MetricEdit/MetricManage/BatchPaste）按 positions 先例 modalRender 包 scope 容器收回 portal 逃逸面，市场组约 45 处偏格圆角归一 `var(--dh-api-radius)`；`PageV2Shell` 新增可选 `themeScope` 透传（`MarketWorkbenchFrame` 同款，页根即 shell 根的路由用）。**共享层铬件清单由逐页扩列升级为全量清单制度 + 护栏断言**：终端条=`showShellTerminalBar` 推导 27 scope、子导航=终端条−market-data+macro-toolkit、治理横幅=navigation temporary-exception 推导 19 scope + liability-analytics 历史白名单（上一批市场组条目记录的 market-finance/news-events/cross-asset 三行横幅缺口随之闭合），`theme.test.ts` 新增 11 组提取 12 条 parity 断言把三清单与 tokens.css 四块/收口终层/分册锁死；balance-analysis 深层浅色字面量标注**单独轮次**（本轮仅页根重映射与 scope 接入）。三模型分工审查：Opus 审共享层热点文件 diff 四方构成（本批 16 scope + 铬件清单、并行 agent 接入行、市场组 PagePrimitives）PASS；GPT 抓出 product-category 工作区混入并行 B1 利差读数功能，提交按 hunk 拆分归位（换肤 hunks 入库，SpreadReadout import/useMemo/JSX、含TPL 口径标签与约 180 行利差测试留在工作区随其功能提交）；Grok 抓出 balance-movement 证据抽屉阴影被错改 `var(--moss-shadow-popover)`（0 18px 32px 向下阴影，对右侧滑出抽屉几何越界），修复为保留原 `-16px 0 32px` 偏移仅换色 `color-mix(in srgb, var(--dh-api-bg) 8%, transparent)`（原板岩 rgba(15,23,42,.08) 等价） | 业主批准的批量收尾；四组并行 + 清单制度化把逐页接入的清单漂移与「新 scope 忘加行」风险除根（此前每页接入都要人工记「共四处/五条列表」，现由推导式清单 + parity 断言兜底）。验收：theme parity 13 文件 62 项、theme+WorkbenchShell 合并 14 文件 105 项、BalanceMovement 4 文件 57 项、ProductCategoryPnl 7 文件 213 项提交态全绿；共享层四文件（tokens/shell/chrome/theme.test）与市场组 DESIGN 条目随并行 agent 会话提交先行入库（热点文件竞态收割，内容完整性已核对：16+agent 全 scope、三清单护栏断言在位），本批页面文件按归属分五笔提交 |
