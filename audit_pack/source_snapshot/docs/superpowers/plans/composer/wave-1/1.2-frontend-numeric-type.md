# W1.2 · 前端 Numeric 类型 + runtime parser

**目标一句话：** 在 `frontend/src/api/contracts.ts` 顶部增加 `Numeric` / `NumericUnit` TypeScript 类型定义，并新增 `frontend/src/api/numeric.ts` 提供 `parseNumeric` / `isNumeric` runtime 检查函数，配套 vitest 单元测试。

**所属波次：** Wave 1 · 基础设施（波次内与 W1.1/W1.3/W1.4/W1.5 并发）
**依赖：** 无
**预计工时：** 30-45 分钟
**对应 Spec：** `docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md` § 3.2 / § 9.2

---

## Composer Subagent 约束（必须遵守）

你是一个专注执行本单一 task 的 composer-2 subagent。

- 仅允许修改"允许修改的路径"白名单中的文件
- 遇到白名单外问题，在返回 message 中以 `BLOCKER:` 前缀报告，不要擅自修改
- 不要运行 `git push` / `git reset --hard` / `git commit --amend`
- **不要修改 `frontend/package.json`**（本任务不引入新依赖）
- 不要修改 `.cursor` / `CLAUDE.md` / `AGENTS.md` 等配置
- 完成后以下列结构返回：
  1. 修改文件列表
  2. `npm run typecheck` 和 `npm run test -- src/test/numeric.test.ts` 输出摘要
  3. 越界 / 告警（如有）
  4. 遗留问题（如有）

---

## 允许修改的路径（白名单）

**Create：**
- `frontend/src/api/numeric.ts`
- `frontend/src/test/numeric.test.ts`

**Modify（仅允许指定区域）：**
- `frontend/src/api/contracts.ts` — **仅允许**在文件最顶部（紧跟首个 `export type ApiBasis = ...` 之前的区域，或紧跟它之后）新增 `NumericUnit` 和 `Numeric` 两个 `export type`；**不得**修改该文件中任何其他已有类型

---

## 背景上下文

- `frontend/src/api/contracts.ts` 当前约 2650 行，是前端所有 API 类型定义的单一源
- 大量字段现在是 `value: string`（承载"+12.34 亿"这种已格式化文本），无法做 runtime 数字校验
- 本任务不迁移任何现有字段；只**增加**一个新的共用 `Numeric` 类型和配套 runtime parser
- 后端 W1.1 会同步定义 pydantic `Numeric`；前端 W1.2 的 TypeScript 定义必须与之 **shape 一致**（字段名、类型、字面量集合）

---

## 实现步骤

### Step 1：写失败测试

创建 `frontend/src/test/numeric.test.ts`，完整内容：

```typescript
import { describe, expect, it } from "vitest";

import { isNumeric, parseNumeric, parseNumericOrNull } from "../api/numeric";
import type { Numeric } from "../api/contracts";

describe("isNumeric", () => {
  it("accepts a full valid Numeric with positive yuan value", () => {
    const candidate: Numeric = {
      raw: 12_345_678_900,
      unit: "yuan",
      display: "+123.46 亿",
      precision: 2,
      sign_aware: true,
    };
    expect(isNumeric(candidate)).toBe(true);
  });

  it("accepts Numeric with raw = null", () => {
    const candidate: Numeric = {
      raw: null,
      unit: "pct",
      display: "—",
      precision: 2,
      sign_aware: true,
    };
    expect(isNumeric(candidate)).toBe(true);
  });

  it("accepts Numeric with negative raw", () => {
    const candidate: Numeric = {
      raw: -5_000_000_000,
      unit: "yuan",
      display: "-50.00 亿",
      precision: 2,
      sign_aware: true,
    };
    expect(isNumeric(candidate)).toBe(true);
  });

  it("rejects plain number", () => {
    expect(isNumeric(42)).toBe(false);
  });

  it("rejects string", () => {
    expect(isNumeric("+12.34 亿")).toBe(false);
  });

  it("rejects null", () => {
    expect(isNumeric(null)).toBe(false);
  });

  it("rejects undefined", () => {
    expect(isNumeric(undefined)).toBe(false);
  });

  it("rejects object missing raw field", () => {
    expect(
      isNumeric({ unit: "yuan", display: "x", precision: 0, sign_aware: true }),
    ).toBe(false);
  });

  it("rejects object with unknown unit", () => {
    expect(
      isNumeric({
        raw: 1,
        unit: "bogus",
        display: "1",
        precision: 0,
        sign_aware: true,
      }),
    ).toBe(false);
  });

  it("rejects object with negative precision", () => {
    expect(
      isNumeric({
        raw: 1,
        unit: "yuan",
        display: "1",
        precision: -1,
        sign_aware: true,
      }),
    ).toBe(false);
  });

  it("rejects object with non-string display", () => {
    expect(
      isNumeric({
        raw: 1,
        unit: "yuan",
        display: 12.34,
        precision: 2,
        sign_aware: true,
      }),
    ).toBe(false);
  });

  it("rejects object with non-boolean sign_aware", () => {
    expect(
      isNumeric({
        raw: 1,
        unit: "yuan",
        display: "1",
        precision: 2,
        sign_aware: "yes",
      }),
    ).toBe(false);
  });
});

describe("parseNumeric", () => {
  it("returns Numeric when input is valid", () => {
    const input = {
      raw: 0.0255,
      unit: "pct",
      display: "+2.55%",
      precision: 2,
      sign_aware: true,
    };
    const result = parseNumeric(input);
    expect(result.raw).toBe(0.0255);
    expect(result.unit).toBe("pct");
  });

  it("throws with descriptive message on invalid input", () => {
    expect(() => parseNumeric({ raw: 1, unit: "bogus" })).toThrow(/invalid Numeric/i);
  });

  it("throws on null", () => {
    expect(() => parseNumeric(null)).toThrow(/invalid Numeric/i);
  });
});

describe("parseNumericOrNull", () => {
  it("returns Numeric on valid input", () => {
    const input = {
      raw: 1,
      unit: "count",
      display: "1",
      precision: 0,
      sign_aware: false,
    };
    expect(parseNumericOrNull(input)?.raw).toBe(1);
  });

  it("returns null on invalid input instead of throwing", () => {
    expect(parseNumericOrNull("garbage")).toBeNull();
    expect(parseNumericOrNull(undefined)).toBeNull();
  });
});

describe("Numeric unit literal coverage", () => {
  it("accepts all 7 spec units", () => {
    const units: Array<Numeric["unit"]> = [
      "yuan",
      "pct",
      "bp",
      "ratio",
      "count",
      "dv01",
      "yi",
    ];
    for (const unit of units) {
      const candidate: Numeric = {
        raw: 1,
        unit,
        display: "1",
        precision: 0,
        sign_aware: false,
      };
      expect(isNumeric(candidate)).toBe(true);
    }
  });
});
```

### Step 2：确认测试失败

```bash
cd frontend
npm run test -- src/test/numeric.test.ts
```

**Expected：** FAIL，错误信息包含 `Cannot find module '../api/numeric'` 或导出不存在。

### Step 3：新增 `Numeric` / `NumericUnit` 到 `contracts.ts`

打开 `frontend/src/api/contracts.ts`。在文件第一行 `/** Shared API surface: envelope, health, and cross-cutting enums. */` 注释之后、紧跟 `export type ApiBasis = ...` 行**之前**，插入以下内容：

```typescript
/**
 * Shared governed numeric primitive.
 * Mirrors backend ``backend/app/schemas/common_numeric.py::Numeric``.
 * See ``docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md`` § 3.
 */
export type NumericUnit = "yuan" | "pct" | "bp" | "ratio" | "count" | "dv01" | "yi";

export type Numeric = {
  raw: number | null;
  unit: NumericUnit;
  display: string;
  precision: number;
  sign_aware: boolean;
};

```

**不得修改**该文件其他任何内容。

### Step 4：写 `numeric.ts` 实现

创建 `frontend/src/api/numeric.ts`，完整内容：

```typescript
/**
 * Runtime guards for the shared governed Numeric primitive.
 *
 * Paired with the TypeScript declaration in ``./contracts.ts`` and the backend
 * pydantic mirror ``backend/app/schemas/common_numeric.py``.
 */
import type { Numeric, NumericUnit } from "./contracts";

const NUMERIC_UNITS: ReadonlySet<NumericUnit> = new Set<NumericUnit>([
  "yuan",
  "pct",
  "bp",
  "ratio",
  "count",
  "dv01",
  "yi",
]);

/**
 * Narrow an unknown value to ``Numeric``. Pure structural check with no
 * coercion or default-filling: a value that is "almost Numeric" fails.
 */
export function isNumeric(value: unknown): value is Numeric {
  if (value === null || value === undefined) {
    return false;
  }
  if (typeof value !== "object") {
    return false;
  }
  const obj = value as Record<string, unknown>;

  if (!("raw" in obj) || !("unit" in obj) || !("display" in obj) || !("precision" in obj) || !("sign_aware" in obj)) {
    return false;
  }

  const raw = obj.raw;
  if (raw !== null && typeof raw !== "number") {
    return false;
  }
  if (typeof raw === "number" && !Number.isFinite(raw)) {
    return false;
  }

  if (typeof obj.unit !== "string" || !NUMERIC_UNITS.has(obj.unit as NumericUnit)) {
    return false;
  }

  if (typeof obj.display !== "string") {
    return false;
  }

  if (typeof obj.precision !== "number" || !Number.isInteger(obj.precision) || obj.precision < 0) {
    return false;
  }

  if (typeof obj.sign_aware !== "boolean") {
    return false;
  }

  return true;
}

/**
 * Parse an unknown value into ``Numeric`` or throw. Use at trust boundaries
 * (mock payloads, API responses) when you want loud failures.
 */
export function parseNumeric(value: unknown): Numeric {
  if (!isNumeric(value)) {
    throw new Error(
      `invalid Numeric: ${describeShape(value)}`,
    );
  }
  return value;
}

/**
 * Lenient version of ``parseNumeric`` for optional fields; returns ``null``
 * instead of throwing when the shape is wrong.
 */
export function parseNumericOrNull(value: unknown): Numeric | null {
  return isNumeric(value) ? value : null;
}

function describeShape(value: unknown): string {
  if (value === null) return "null";
  if (value === undefined) return "undefined";
  if (typeof value !== "object") return typeof value;
  try {
    return JSON.stringify(value).slice(0, 200);
  } catch {
    return "[unserializable object]";
  }
}
```

### Step 5：运行测试通过

```bash
cd frontend
npm run test -- src/test/numeric.test.ts
```

**Expected：** 所有测试 PASS（约 18 条）。

### Step 6：typecheck 全绿

```bash
cd frontend
npm run typecheck
```

**Expected：** 0 errors。

若 typecheck 暴露**非本任务导致**的已有错误，用 `BLOCKER:` 返回完整错误列表，**不要**自行修复其他文件。

### Step 7：确认未破坏其他测试

```bash
cd frontend
npm run test
```

**Expected：** 所有前端测试仍 PASS（本任务不应影响现有组件 / contracts 消费方）。

### Step 8：Commit

```bash
git add frontend/src/api/contracts.ts frontend/src/api/numeric.ts frontend/src/test/numeric.test.ts
git commit -m "feat(api): add shared Numeric type and runtime guards"
```

---

## 验收 & 返回清单

- [ ] `frontend/src/test/numeric.test.ts` 所有测试 PASS
- [ ] `npm run typecheck` 0 errors
- [ ] `npm run test`（全量）无回归
- [ ] `contracts.ts` diff 只新增 `NumericUnit` 和 `Numeric` 两个 export type，其余未动
- [ ] `git diff --name-only HEAD~1 HEAD` 应严格为：
  ```
  frontend/src/api/contracts.ts
  frontend/src/api/numeric.ts
  frontend/src/test/numeric.test.ts
  ```

**返回 message 必须包含：**
1. 修改文件列表
2. `npm run test -- src/test/numeric.test.ts` 输出尾部
3. `npm run typecheck` 输出摘要
4. 如有 `BLOCKER:` 情况详细说明

---

## 不要做的事

- ❌ 不要引入 `zod` 或其他 runtime validation 库（W1.5 另行决定）
- ❌ 不要修改 `contracts.ts` 中已有的 2650 行任何一行
- ❌ 不要替换现有 `value: string` 字段为 `Numeric`（Wave 2 的事）
- ❌ 不要在 `numeric.ts` 里添加 `formatNumeric` 等 display 构造函数（W1.3 的事）
- ❌ 不要修改 `package.json` / `tsconfig.json` / `vitest.config.ts`
- ❌ 不要创建 barrel export（`index.ts`），直接文件级 import
