# W3.3 · 前端 PnL contracts.ts 类型升级 + pnlAttributionAdapter 骨架

**目标一句话：** 把 `frontend/src/api/contracts.ts` 中 PnL 工作台相关的 17 个类型里所有 `number` / `number | null` 数字字段升级为 `Numeric` / `Numeric | null`；同时新建 `frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.ts` 作为骨架，提供 `derivePnlDataSectionState` 通用函数和 7+1 个 per-endpoint adapter 入口（返回 `{ vm, state, meta }`）。具体 tab 级 adapter 分支和组件 wiring 留到 W3.4。

**所属波次：** Wave 3 · PnL Attribution 升级（Step 2）
**依赖：** W3.1（后端 pnl schemas Numeric 已合并）+ W2.3（前端 contracts.ts Numeric type 已存在）+ W2.3 adapter（pattern 可参考）
**预计工时：** 45-60 分钟
**对应 Spec：** `docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md` § 3.3 / § 5.2

---

## Composer Subagent 约束（必须遵守）

你是一个专注执行单一 task 的 composer-2 subagent。

- 仅允许修改白名单文件
- 遇白名单外问题用 `BLOCKER:` 前缀返回
- 不要 `git push` / `git reset --hard` / `git commit --amend`
- 不要修改 `package.json` 或安装依赖
- 不要创建 git worktree
- 不要跑全量 pytest / vitest
- 完成后返回：修改文件列表、测试命令输出摘要、越界/告警、遗留问题

---

## 允许修改的路径（白名单）

**Modify：**
- `frontend/src/api/contracts.ts`（**仅**允许修改 PnL 相关 17 个类型的数字字段类型，不得新增 / 删除 export、不得动其他 2600+ 行）

**Create：**
- `frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.ts`
- `frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.test.ts`

**不得修改任何其他文件。** 特别：
- ❌ `frontend/src/features/pnl-attribution/components/*`（W3.4 / W3.5 的事）
- ❌ `frontend/src/features/pnl-attribution/pages/*`
- ❌ `frontend/src/api/client.ts` / `numeric.ts`
- ❌ `frontend/src/mocks/*`（W3.6 的事）
- ❌ `frontend/src/features/executive-dashboard/**`

---

## 背景上下文

### 需要升级的 17 个 PnL 类型（在 `contracts.ts` 里）

找到它们的定义位置（通过 grep `export type <Name>`）。逐个升级数字字段：

| 类型 | 字段升级规则 |
|---|---|
| `VolumeRateAttributionItem` | 所有 `number` / `number \| null` 除 `level` 以外全部 → `Numeric` / `Numeric \| null` |
| `VolumeRateAttributionPayload` | `total_current_pnl` / `total_previous_pnl` / `total_pnl_change` / `total_volume_effect` / `total_rate_effect` / `total_interaction_effect` → `Numeric` 或 `Numeric \| null`；`items`/`has_previous_data`/`*_period` 保持 |
| `TPLMarketDataPoint` | `tpl_fair_value_change` / `tpl_total_pnl` / `tpl_scale` → `Numeric`；`treasury_10y` / `treasury_10y_change` / `dr007` → `Numeric \| null` |
| `TPLMarketCorrelationPayload` | `correlation_coefficient` / `avg_treasury_10y_change` / `treasury_10y_total_change_bp` → `Numeric \| null`；`total_tpl_fv_change` → `Numeric`；`num_periods` 保持 `number` |
| `PnlCompositionItem` | 所有 `number` 数字字段 → `Numeric`（`level` 保持 `number`） |
| `PnlCompositionTrendItem` | 所有 `number` 数字字段 → `Numeric` |
| `PnlCompositionPayload` | 所有 `number` 数字字段 → `Numeric` |
| `PnlAttributionAnalysisSummary` | `primary_driver_pct` → `Numeric` |
| `CarryRollDownItem` | 所有 `number` / `number \| null` 数字字段 → `Numeric` / `Numeric \| null` |
| `CarryRollDownPayload` | 所有 `number` 数字字段 → `Numeric` |
| `SpreadAttributionItem` | 所有 `number` / `number \| null` 数字字段 → `Numeric` / `Numeric \| null` |
| `SpreadAttributionPayload` | 所有 `number` / `number \| null` 数字字段 → `Numeric` / `Numeric \| null` |
| `KRDAttributionBucket` | 所有 `number` / `number \| null` 数字字段 → `Numeric` / `Numeric \| null`（`bond_count` 保持 `number`） |
| `KRDAttributionPayload` | 所有 `number` 数字字段 → `Numeric` |
| `AdvancedAttributionSummary` | 所有 `number` 数字字段 → `Numeric` |
| `CampisiAttributionItem` | 所有 `number` 数字字段 → `Numeric` |
| `CampisiAttributionPayload` | 所有 `number` 数字字段 → `Numeric`（`num_days` 保持 `number`） |

**注意：** 保持所有**非数字字段**（`string` / `bool` / `int counter`（bond_count / num_days / num_periods / level）/ 数组 / object）原样。

### 识别升级规则的简单规则

对每个类型，字段逐一判断：
- **保持 number**：`level`、`bond_count`、`num_periods`、`num_days`、`total_actions` 这类**计数/层级**字段
- **升级 Numeric**：其他所有 `number` / `number | null` 字段（包括 `*_pnl`, `*_effect`, `market_value`, `weight`, `duration`, `ytm`, `krd`, `*_pct`, `*_contribution_pct` 等）

---

## 实现步骤

### Step 1：写 adapter 测试

创建 `frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.test.ts`：

```typescript
import { describe, expect, it } from "vitest";

import type { ResultMeta } from "../../../api/contracts";
import { derivePnlDataSectionState } from "./pnlAttributionAdapter";

function makeMeta(partial: Partial<ResultMeta> = {}): ResultMeta {
  return {
    trace_id: "tr_test",
    basis: "formal",
    result_kind: "pnl_attribution.volume_rate",
    formal_use_allowed: true,
    source_version: "sv_test",
    vendor_version: "vv_test",
    rule_version: "rv_test",
    cache_version: "cv_test",
    quality_flag: "ok",
    vendor_status: "ok",
    fallback_mode: "none",
    scenario_flag: false,
    generated_at: "2026-04-18T00:00:00",
    ...partial,
  };
}

describe("derivePnlDataSectionState", () => {
  it("loading flag -> loading state", () => {
    const s = derivePnlDataSectionState({
      meta: null,
      isLoading: true,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("loading");
  });

  it("error flag -> error state with message", () => {
    const s = derivePnlDataSectionState({
      meta: null,
      isLoading: false,
      isError: true,
      errorMessage: "fetch failed",
      isEmpty: false,
    });
    expect(s.kind).toBe("error");
    if (s.kind === "error") expect(s.message).toBe("fetch failed");
  });

  it("meta null + no flags -> loading", () => {
    const s = derivePnlDataSectionState({
      meta: null,
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("loading");
  });

  it("vendor_unavailable meta -> vendor_unavailable state", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta({ vendor_status: "vendor_unavailable", quality_flag: "warning" }),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("vendor_unavailable");
  });

  it("explicit_miss source_version -> explicit_miss state", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta({
        source_version: "sv_exec_dashboard_explicit_miss_v1",
        quality_flag: "warning",
        filters_applied: { report_date: "2025-11-30" },
      }),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("explicit_miss");
    if (s.kind === "explicit_miss") expect(s.requested_date).toBe("2025-11-30");
  });

  it("fallback_mode=latest_snapshot -> fallback state", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta({
        fallback_mode: "latest_snapshot",
        filters_applied: { report_date: "2025-12-30" },
      }),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("fallback");
    if (s.kind === "fallback") expect(s.effective_date).toBe("2025-12-30");
  });

  it("vendor_stale -> stale state", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta({ vendor_status: "vendor_stale", quality_flag: "stale" }),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("stale");
  });

  it("isEmpty flag (and meta ok) -> empty", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta(),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: true,
    });
    expect(s.kind).toBe("empty");
  });

  it("all ok -> ok", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta(),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("ok");
  });

  it("effective_date mixed when multiple effective_report_dates diverge", () => {
    const s = derivePnlDataSectionState({
      meta: makeMeta({
        fallback_mode: "latest_snapshot",
        filters_applied: {
          effective_report_dates: {
            pnl: "2026-04-08",
            bond: "2026-04-07",
          },
        },
      }),
      isLoading: false,
      isError: false,
      errorMessage: null,
      isEmpty: false,
    });
    expect(s.kind).toBe("fallback");
    if (s.kind === "fallback") expect(s.effective_date).toBe("mixed");
  });
});
```

### Step 2：跑测试确认 FAIL

```bash
cd frontend
npm run test -- src/features/pnl-attribution/adapters/pnlAttributionAdapter.test.ts
```

**Expected：** FAIL，cannot find module。

### Step 3：写 adapter 实现

创建 `frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.ts`：

```typescript
/**
 * PnL Attribution workbench adapter skeleton.
 *
 * This file provides the shared state-derivation helper
 * ``derivePnlDataSectionState`` used by all tab-level adapters (W3.4 will
 * add the tab-specific sub-adapter functions that map ApiEnvelope to typed
 * view-models). Components MUST NOT read ResultMeta directly; they consume
 * the ``DataSectionState`` produced here.
 *
 * Design reference: ``docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md`` § 5.2.
 */
import type { ResultMeta } from "../../../api/contracts";
import type { DataSectionState } from "../../../components/DataSection.types";

export type PnlStateDerivationInput = {
  meta: ResultMeta | null;
  isLoading: boolean;
  isError: boolean;
  errorMessage: string | null;
  isEmpty: boolean;
};

/**
 * Map raw query flags + ResultMeta into a single DataSectionState.
 *
 * Priority order:
 *   loading -> error -> (no meta yet? → loading) -> vendor_unavailable ->
 *   explicit_miss -> fallback -> stale -> empty -> ok
 */
export function derivePnlDataSectionState(
  input: PnlStateDerivationInput,
): DataSectionState {
  if (input.isLoading) return { kind: "loading" };
  if (input.isError) {
    return input.errorMessage
      ? { kind: "error", message: input.errorMessage }
      : { kind: "error" };
  }

  if (!input.meta) return { kind: "loading" };

  const meta = input.meta;
  const requestedDate = resolveRequestedDate(meta);
  const effectiveDate = resolveEffectiveDate(meta);
  const details = describeMetaDetails(meta);

  if (meta.vendor_status === "vendor_unavailable") {
    return { kind: "vendor_unavailable", details };
  }

  if (typeof meta.source_version === "string" && meta.source_version.includes("explicit_miss")) {
    return { kind: "explicit_miss", requested_date: requestedDate, details };
  }

  if (meta.fallback_mode === "latest_snapshot") {
    return { kind: "fallback", effective_date: effectiveDate, details };
  }

  if (meta.vendor_status === "vendor_stale" || meta.quality_flag === "stale") {
    return { kind: "stale", effective_date: effectiveDate, details };
  }

  if (input.isEmpty) return { kind: "empty" };

  return { kind: "ok" };
}

function resolveEffectiveDate(meta: ResultMeta): string | undefined {
  const reportDate = meta.filters_applied?.report_date;
  if (typeof reportDate === "string" && reportDate.trim()) {
    return reportDate.trim();
  }

  const effective = meta.filters_applied?.effective_report_dates;
  if (effective && typeof effective === "object" && !Array.isArray(effective)) {
    const values = Object.values(effective)
      .filter((v): v is string => typeof v === "string" && v.trim().length > 0)
      .map((v) => v.trim());
    const unique = Array.from(new Set(values));
    if (unique.length === 0) return undefined;
    if (unique.length === 1) return unique[0];
    return "mixed";
  }

  return undefined;
}

function resolveRequestedDate(meta: ResultMeta): string | undefined {
  const requested = meta.filters_applied?.requested_report_date ?? meta.filters_applied?.report_date;
  return typeof requested === "string" && requested.trim() ? requested.trim() : undefined;
}

function describeMetaDetails(meta: ResultMeta): string {
  const parts: string[] = [];
  if (meta.quality_flag && meta.quality_flag !== "ok") parts.push(`quality=${meta.quality_flag}`);
  if (meta.vendor_status && meta.vendor_status !== "ok") parts.push(`vendor=${meta.vendor_status}`);
  if (meta.fallback_mode && meta.fallback_mode !== "none") parts.push(`fallback=${meta.fallback_mode}`);
  if (meta.generated_at) parts.push(`generated_at=${meta.generated_at}`);
  return parts.join(" · ");
}
```

### Step 4：跑 adapter 测试通过

```bash
cd frontend
npm run test -- src/features/pnl-attribution/adapters/pnlAttributionAdapter.test.ts
```

**Expected：** 10 条 PASS。

### Step 5：升级 contracts.ts 的 17 个 PnL 类型

**关键：** `contracts.ts` 约 2650 行，你要找到 17 个 PnL 类型定义（通过 `export type VolumeRateAttributionItem = {` / `export type VolumeRateAttributionPayload = {` / 等），逐个把数字字段从 `number` / `number | null` 改为 `Numeric` / `Numeric | null`。

**非数字字段保持不动**（string / bool / 计数 int / 数组）。

**`Numeric` 已在 W1.2 导入 contracts.ts 顶部作为 exported type**，不需要额外 import。

参考的具体映射（每个类型的字段名 → 目标类型）：

**VolumeRateAttributionItem**：
- `current_scale: number` → `current_scale: Numeric`
- `current_pnl: number` → `current_pnl: Numeric`
- `current_yield_pct: number | null` → `current_yield_pct: Numeric | null`
- `previous_scale: number | null` → `previous_scale: Numeric | null`
- `previous_pnl: number | null` → `previous_pnl: Numeric | null`
- `previous_yield_pct: number | null` → `previous_yield_pct: Numeric | null`
- `pnl_change: number | null` → `pnl_change: Numeric | null`
- `pnl_change_pct: number | null` → `pnl_change_pct: Numeric | null`
- `volume_effect: number | null` → `volume_effect: Numeric | null`
- `rate_effect: number | null` → `rate_effect: Numeric | null`
- `interaction_effect: number | null` → `interaction_effect: Numeric | null`
- `attrib_sum: number | null` → `attrib_sum: Numeric | null`
- `recon_error: number | null` → `recon_error: Numeric | null`
- `volume_contribution_pct: number | null` → `volume_contribution_pct: Numeric | null`
- `rate_contribution_pct: number | null` → `rate_contribution_pct: Numeric | null`
- `level: number` **保持**

**VolumeRateAttributionPayload**：
- `total_current_pnl: number` → `Numeric`
- `total_previous_pnl: number | null` → `Numeric | null`
- `total_pnl_change: number | null` → `Numeric | null`
- `total_volume_effect: number | null` → `Numeric | null`
- `total_rate_effect: number | null` → `Numeric | null`
- `total_interaction_effect: number | null` → `Numeric | null`

**TPLMarketDataPoint**：
- `tpl_fair_value_change: number` → `Numeric`
- `tpl_total_pnl: number` → `Numeric`
- `tpl_scale: number` → `Numeric`
- `treasury_10y: number | null` → `Numeric | null`
- `treasury_10y_change: number | null` → `Numeric | null`
- `dr007: number | null` → `Numeric | null`

**TPLMarketCorrelationPayload**：
- `correlation_coefficient: number | null` → `Numeric | null`
- `total_tpl_fv_change: number` → `Numeric`
- `avg_treasury_10y_change: number | null` → `Numeric | null`
- `treasury_10y_total_change_bp: number | null` → `Numeric | null`
- `num_periods: number` **保持**

**PnlCompositionItem**（9 个数字字段都升级为 Numeric；`level` 保持）：
- `total_pnl` / `interest_income` / `fair_value_change` / `capital_gain` / `other_income` / `interest_pct` / `fair_value_pct` / `capital_gain_pct` / `other_pct` → `Numeric`

**PnlCompositionTrendItem**：
- `interest_income` / `fair_value_change` / `capital_gain` / `other_income` / `total_pnl` → `Numeric`

**PnlCompositionPayload**（9 个数字字段）：
- 所有 total_* / *_pct → `Numeric`

**PnlAttributionAnalysisSummary**：
- `primary_driver_pct: number` → `Numeric`

**CarryRollDownItem**（14 个字段，`ytm` 和 `curve_slope` 是 nullable）：
- `market_value` / `weight` / `coupon_rate` / `funding_cost` / `carry` / `carry_pnl` / `duration` / `rolldown` / `rolldown_pnl` / `static_return` / `static_pnl` → `Numeric`
- `ytm: number | null` → `Numeric | null`
- `curve_slope: number | null` → `Numeric | null`

**CarryRollDownPayload**（8 个）：
- `total_market_value` / `portfolio_carry` / `portfolio_rolldown` / `portfolio_static_return` / `total_carry_pnl` / `total_rolldown_pnl` / `total_static_pnl` / `ftp_rate` → `Numeric`

**SpreadAttributionItem**（12 个，3 个 nullable）：
- `market_value` / `duration` / `weight` / `treasury_effect` / `spread_effect` / `total_price_effect` / `treasury_contribution_pct` / `spread_contribution_pct` → `Numeric`
- `yield_change: number | null` / `treasury_change: number | null` / `spread_change: number | null` → `Numeric | null`

**SpreadAttributionPayload**（10 个，3 个 nullable）：
- `treasury_10y_start: number | null` / `treasury_10y_end: number | null` / `treasury_10y_change: number | null` → `Numeric | null`
- `total_market_value` / `portfolio_duration` / `total_treasury_effect` / `total_spread_effect` / `total_price_change` → `Numeric`

**KRDAttributionBucket**（8 个，1 个 nullable；`bond_count` 保持 `number`）：
- `tenor_years` / `market_value` / `weight` / `bucket_duration` / `krd` / `duration_contribution` / `contribution_pct` → `Numeric`
- `yield_change: number | null` → `Numeric | null`
- `bond_count: number` **保持**

**KRDAttributionPayload**（5 个数字）：
- `total_market_value` / `portfolio_duration` / `portfolio_dv01` / `total_duration_effect` / `max_contribution_value` → `Numeric`

**AdvancedAttributionSummary**（5 个数字）：
- `portfolio_carry` / `portfolio_rolldown` / `static_return_annualized` / `treasury_effect_total` / `spread_effect_total` → `Numeric`

**CampisiAttributionItem**（12 个数字）：
- 所有数字字段 → `Numeric`

**CampisiAttributionPayload**（11 个数字；`num_days` 保持 `number`）：
- 所有数字字段 → `Numeric`
- `num_days: number` **保持**

**禁止：** 新增任何其他 export；修改 Campisi{FourEffects,Enhanced,MaturityBuckets}* 类型等未列出的类型（它们可能也有数字字段但属于后续任务）。**只改上述 17 个类型的列出字段。**

### Step 6：typecheck

```bash
cd frontend
npm run typecheck 2>&1 | Select-Object -Last 30
```

**Expected：** 可能有 errors（因为 PnlAttributionView.tsx 和各图表组件仍消费 number 字段）。**记录 errors 数但不 fail 本任务**，这些组件在 W3.4 会迁移。如果 errors **不是**来自 `features/pnl-attribution/components/**` 或 `test/Pnl*.test.tsx`（即与 PnL 工作台无关），`BLOCKER:` 上报。

### Step 7：子集回归

```bash
cd frontend
npm run test -- src/features/pnl-attribution/adapters/ src/features/executive-dashboard/ src/test/contract/ 2>&1 | Select-Object -Last 20
```

**Expected：** 相对 Wave 2 tag 无新增 fail（executive-dashboard 测试保持 PASS；pnl-attribution adapter 新测试 PASS；contract 测试 PASS）。

### Step 8：Commit

```bash
git add frontend/src/api/contracts.ts frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.ts frontend/src/features/pnl-attribution/adapters/pnlAttributionAdapter.test.ts
git commit -m "feat(pnl-attribution): migrate contracts numeric fields + add adapter skeleton"
```

---

## 验收 & 返回清单

- [ ] `pnlAttributionAdapter.test.ts` 10 条 PASS
- [ ] `contracts.ts` 17 个 PnL 类型的数字字段全部升级到 `Numeric`（非 Numeric 字段保持）
- [ ] typecheck errors 仅来自 `features/pnl-attribution/components/**` 或 `test/Pnl*.test.tsx`（期望由 W3.4 解决）
- [ ] `git diff --name-only HEAD~1 HEAD` 严格限于 3 个文件

**返回 message 必须包含：**
1. 修改文件列表
2. adapter.test 输出尾部
3. typecheck errors 数 + 位置摘要（期望全部在 PnL 工作台组件 / 测试中）
4. `BLOCKER:` 情况（如有；特别是 typecheck errors 出现在 PnL 工作台范围之外）
5. 遗留问题

---

## 不要做的事

- ❌ 不要在 adapter 里写 per-tab 的数据转换（W3.4 做）
- ❌ 不要写 Campisi{FourEffects,Enhanced,MaturityBuckets}* 类型升级（它们未在 17 个类型清单里；留到 W3.6 或 W5）
- ❌ 不要改 `PnlAttributionView.tsx` / 各图表组件 / `PnlAttributionPage.tsx`
- ❌ 不要改 `mocks/*` / `client.ts` / `utils/*`
- ❌ 不要创建 worktree
- ❌ 不要跑 `npm run test` 全量 / pytest 全量
