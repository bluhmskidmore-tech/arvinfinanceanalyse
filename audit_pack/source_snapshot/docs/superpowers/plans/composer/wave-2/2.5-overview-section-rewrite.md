# W2.5 · OverviewSection 重写 + DashboardPage 接入 adapter

**目标一句话：** 把 `frontend/src/features/executive-dashboard/components/OverviewSection.tsx` 改写为**只读 adapter VM**、用 `DataSection` 展示 7 状态、用 `tone.ts::TONE_COLOR` 单一色彩出口、不再自己 `toFixed`/`Math.*`；同时把 `frontend/src/features/workbench/pages/DashboardPage.tsx` 里 `overview` 相关调用点切换到 `adaptDashboard()` 输出。**PnlAttributionSection 本轮不动**（W2.6 处理）。

**所属波次：** Wave 2 · Executive 升级（Step 4 前半部分）
**依赖：** W2.3 adapter + W2.4 selector + W2.7 mock + W1.3 tone/format + W1.4 DataSection（全部已合并）
**预计工时：** 45-60 分钟
**对应 Spec：** `docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md` § 2 / § 6 / § 8.1

---

## Composer Subagent 约束（必须遵守）

你是一个专注执行单一 task 的 composer-2 subagent。

- 仅允许修改白名单文件
- 遇白名单外问题用 `BLOCKER:` 前缀返回
- 不要 `git push` / `git reset --hard` / `git commit --amend` / `git rebase`
- 不要修改 `package.json` / 安装依赖
- 不要创建 git worktree
- 不要跑全量 pytest / 全量 vitest（只跑相关子集）
- 完成后返回：修改文件列表、测试命令输出摘要、越界/告警、遗留问题

---

## 允许修改的路径（白名单）

**Modify：**
- `frontend/src/features/executive-dashboard/components/OverviewSection.tsx`（完全重写）
- `frontend/src/features/workbench/pages/DashboardPage.tsx`（引入 adapter 调用 + 改 Overview 部分；**PnlAttributionSection 调用维持原样**）
- `frontend/src/test/DashboardPage.test.tsx`（如有必要修过时 assertion；**不得弱化强度**）

**不得修改**任何其他文件。特别：
- ❌ `PnlAttributionSection.tsx`（W2.6）
- ❌ `AsyncSection.tsx`（仍被其他页面使用，保留）
- ❌ `DataSection.tsx` / `DataSection.types.ts`（W1.4 固定）
- ❌ `adapters/` / `selectors/`（W2.3 / W2.4 固定）
- ❌ `mocks/*` / `contracts.ts` / `client.ts`
- ❌ `utils/format.ts` / `utils/tone.ts`

---

## 背景上下文

### 当前 OverviewSection.tsx（你要重写的）

完整内容（大约 97 行）：

```tsx
import type { OverviewPayload } from "../../../api/contracts";
import { AsyncSection } from "./AsyncSection";

type OverviewSectionProps = {
  data?: OverviewPayload;
  isLoading: boolean;
  isError: boolean;
  onRetry: () => void;
};

const toneColor = {            // ← 要删，改用 tone.ts::TONE_COLOR
  positive: "#2f8f63",
  neutral: "#5c6b82",
  warning: "#cc7a1a",
  negative: "#b74c45",
} as const;

export function OverviewSection({ data, isLoading, isError, onRetry }: OverviewSectionProps) {
  return (
    <AsyncSection title="经营总览" isLoading={isLoading} isError={isError}
      isEmpty={!data || data.metrics.length === 0} onRetry={onRetry}
      extra={<span>...{data?.metrics.length ?? 0} 项</span>}>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 16 }}>
        {data?.metrics.map((metric) => (
          <div key={metric.id} style={{...}}>
            <span style={{...}}>{metric.label}</span>
            <span style={{..., color: toneColor[metric.tone] }}>{metric.delta}</span>   // ← 直接 render metric.delta 作 ReactNode
            <div style={{ margin: 0, fontSize: 28, fontWeight: 600 }}>{metric.value}</div>  // ← render metric.value
            <p>{metric.detail}</p>
          </div>
        ))}
      </div>
    </AsyncSection>
  );
}
```

### 当前 DashboardPage.tsx（你要微创改造的）

关键片段（需要读全文）：

```tsx
import { AsyncSection } from "../../executive-dashboard/components/AsyncSection";
import { OverviewSection } from "../../executive-dashboard/components/OverviewSection";
// ...
const overviewQuery = useQuery({ queryKey: [...], queryFn: () => client.getOverview(reportDate || undefined), retry: false });
const pnlQuery = useQuery({ queryKey: [...], queryFn: () => client.getPnlAttribution(reportDate || undefined), retry: false });
// ...
<OverviewSection data={overviewQuery.data?.result} isLoading={overviewQuery.isLoading} isError={overviewQuery.isError} onRetry={() => void overviewQuery.refetch()} />
// ...
<PnlAttributionSection data={pnlQuery.data?.result} isLoading={pnlQuery.isLoading} isError={pnlQuery.isError} onRetry={() => void pnlQuery.refetch()} />
```

### 新 OverviewSection 接口（W2.5 要做的）

```tsx
type OverviewSectionProps = {
  overview: DashboardAdapterOutput["overview"]; // { vm, state, meta }
  onRetry: () => void;
};
```

组件内部：
- 用 `DataSection` 替代 `AsyncSection`（state 驱动 7 种渲染）
- 用 `selectOverviewCards(overview.vm)` 拿 cards
- 每张 card render `{metric.value.display}` / `{metric.delta.display}` 而不是 object
- tone color 从 `TONE_COLOR[metric.tone]` 取，**删除**局部 toneColor map

### DashboardPage.tsx 的改造

1. Import `adaptDashboard` from `../../executive-dashboard/adapters/executiveDashboardAdapter`
2. 用 `adaptDashboard({...})` 把 query 状态包成 DashboardAdapterOutput
3. OverviewSection 传 `overview={adapterOut.overview}` + `onRetry={...}`
4. **PnlAttributionSection 暂时维持旧接口**（仍传 `data`/`isLoading`/`isError`/`onRetry`）—— W2.6 再切换
5. 原有的 dashboard-data-warning / data-status-strip / FormalResultMetaPanel 全部保留（它们消费 `overviewMeta` / `attributionMeta` —— 现在可从 `adapterOut.overview.meta` / `adapterOut.attribution.meta` 取）

---

## 实现步骤

### Step 1：Read 当前文件

```bash
# 用 Read 工具读：
# - frontend/src/features/executive-dashboard/components/OverviewSection.tsx
# - frontend/src/features/workbench/pages/DashboardPage.tsx
# - frontend/src/test/DashboardPage.test.tsx
# - frontend/src/features/executive-dashboard/adapters/executiveDashboardAdapter.ts（熟悉导出）
# - frontend/src/features/executive-dashboard/selectors/executiveDashboardSelectors.ts
# - frontend/src/components/DataSection.tsx / DataSection.types.ts
# - frontend/src/utils/tone.ts
```

### Step 2：重写 OverviewSection.tsx

完整替换文件为：

```tsx
import { DataSection } from "../../../components/DataSection";
import { TONE_COLOR } from "../../../utils/tone";
import type { DashboardAdapterOutput } from "../adapters/executiveDashboardAdapter";
import { selectOverviewCards } from "../selectors/executiveDashboardSelectors";

type OverviewSectionProps = {
  overview: DashboardAdapterOutput["overview"];
  onRetry: () => void;
};

const CARD_STYLE = {
  display: "grid",
  gap: 10,
  padding: 18,
  borderRadius: 20,
  background: "linear-gradient(180deg, rgba(255,255,255,0.98) 0%, rgba(246,249,253,0.98) 100%)",
  border: "1px solid #e4ebf5",
} as const;

const LABEL_STYLE = { color: "#8090a8", fontSize: 12, letterSpacing: "0.04em" } as const;

const DELTA_BADGE_STYLE = {
  width: "fit-content",
  padding: "4px 10px",
  borderRadius: 999,
  background: "#eef3fb",
  fontSize: 12,
  fontWeight: 600,
} as const;

const VALUE_STYLE = { margin: 0, fontSize: 28, fontWeight: 600 } as const;

const DETAIL_STYLE = {
  margin: 0,
  color: "#5c6b82",
  fontSize: 13,
  lineHeight: 1.6,
} as const;

export function OverviewSection({ overview, onRetry }: OverviewSectionProps) {
  const cards = selectOverviewCards(overview.vm);

  return (
    <DataSection
      title="经营总览"
      state={overview.state}
      onRetry={onRetry}
      extra={
        <span
          style={{
            padding: "4px 10px",
            borderRadius: 999,
            background: "#dfe8ff",
            color: "#1f5eff",
            fontSize: 12,
          }}
        >
          {cards.length} 项
        </span>
      }
    >
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
          gap: 16,
        }}
      >
        {cards.map((metric) => (
          <div key={metric.id} style={CARD_STYLE}>
            <span style={LABEL_STYLE}>{metric.label}</span>
            <span
              style={{
                ...DELTA_BADGE_STYLE,
                color: TONE_COLOR[metric.tone],
              }}
            >
              {metric.delta.display}
            </span>
            <div style={VALUE_STYLE}>{metric.value.display}</div>
            <p style={DETAIL_STYLE}>{metric.detail}</p>
          </div>
        ))}
      </div>
    </DataSection>
  );
}
```

### Step 3：改造 DashboardPage.tsx

读 DashboardPage.tsx 全文后，做下列最小改动：

1. 顶部 imports 增加：
   ```tsx
   import { adaptDashboard } from "../../executive-dashboard/adapters/executiveDashboardAdapter";
   ```

2. 在 `const overviewMeta = ...` 之前，加一个 adapter 调用：
   ```tsx
   const adapterOutput = useMemo(
     () =>
       adaptDashboard({
         overviewEnv: overviewQuery.data,
         attributionEnv: pnlQuery.data,
         overviewLoading: overviewQuery.isLoading,
         overviewError: overviewQuery.isError,
         attributionLoading: pnlQuery.isLoading,
         attributionError: pnlQuery.isError,
       }),
     [
       overviewQuery.data,
       overviewQuery.isLoading,
       overviewQuery.isError,
       pnlQuery.data,
       pnlQuery.isLoading,
       pnlQuery.isError,
     ],
   );
   ```

3. 把原来的 `const overviewMeta = overviewQuery.data?.result_meta;` 改为 `const overviewMeta = adapterOutput.overview.meta;`；attributionMeta 同理改为 `adapterOutput.attribution.meta`。（这是**最小改动**：data-status-strip / warnings / FormalResultMetaPanel 都继续消费 meta，不需要改。）

4. 把 `<OverviewSection data={...} isLoading={...} isError={...} onRetry={...} />` 替换为：
   ```tsx
   <OverviewSection overview={adapterOutput.overview} onRetry={() => void overviewQuery.refetch()} />
   ```

5. **PnlAttributionSection 的调用点维持原样**：
   ```tsx
   <PnlAttributionSection
     data={pnlQuery.data?.result}
     isLoading={pnlQuery.isLoading}
     isError={pnlQuery.isError}
     onRetry={() => void pnlQuery.refetch()}
   />
   ```
   W2.6 才切换。

### Step 4：跑 adapter 测试 + OverviewSection 相关测试

```bash
cd frontend
npm run test -- src/features/executive-dashboard/ src/test/DashboardPage.test.tsx 2>&1 | Select-Object -Last 30
```

**Expected：** adapter / selector test 仍 PASS；`DashboardPage.test.tsx` 如果有 assertions 失败（因为组件结构变化），分析是 test 过时还是真 bug。

### Step 5：如果 DashboardPage.test.tsx 有 assertion 过时

在白名单范围内修。典型过时 assertion：
- 断言 OverviewSection 收到 `isLoading={boolean}` prop → 现在是 `overview.state`，如果测试 mock prop 层面检查过时，改为 mock 新接口
- 断言 mock data 的 `data.metrics[0].value === "—"` → 现在 value 是 Numeric object，`value.display === "—"`

**不得弱化强度**。无法原语义化表达的断言，`BLOCKER:` 上报。

### Step 6：typecheck

```bash
cd frontend
npm run typecheck
```

**Expected：** 0 errors（PnlAttributionSection 仍接旧接口 `data: PnlAttributionPayload` —— 现在 `total` 是 Numeric，React JSX 接受 object，typecheck 不挂；运行时 render 会显示 `[object Object]` 但 W2.6 会修）。

### Step 7：只跑 executive-dashboard + test 子集确认无回归

```bash
cd frontend
npm run test -- src/test/ src/features/executive-dashboard/ 2>&1 | Select-Object -Last 20
```

**Expected：** 相对 Wave 1 tag 无新增 fail（那条 pre-existing RouteRegistry fail 保持）。

### Step 8：Commit

```bash
git add frontend/src/features/executive-dashboard/components/OverviewSection.tsx frontend/src/features/workbench/pages/DashboardPage.tsx frontend/src/test/DashboardPage.test.tsx
git commit -m "feat(dashboard): rewrite OverviewSection to consume adapter VM + wire DashboardPage"
```

（如果 `DashboardPage.test.tsx` 没改动，从 `git add` 里去掉它。）

---

## 验收 & 返回清单

- [ ] OverviewSection 完全重写，不再含局部 `toneColor` map / `toFixed` / 直接 render `metric.value` object
- [ ] DashboardPage 已用 `adaptDashboard`；OverviewSection 传 `overview={...}`
- [ ] PnlAttributionSection 调用未改
- [ ] `typecheck` 0 errors
- [ ] `src/test/` + `src/features/executive-dashboard/` vitest 无新增 fail
- [ ] `git diff --name-only HEAD~1 HEAD` 严格限于 2-3 个白名单文件

**返回 message 必须包含：**
1. 修改文件列表
2. 相关 test 命令输出尾部
3. typecheck 状态
4. 如有修改 `DashboardPage.test.tsx`：列出每条 before→after 断言摘要
5. `BLOCKER:` 情况（如有）
6. 遗留问题

---

## 不要做的事

- ❌ 不要重写 PnlAttributionSection（W2.6 的事）
- ❌ 不要删除 `AsyncSection.tsx`（仍有其他页面用）
- ❌ 不要在 OverviewSection 里加业务计算 / 做 `Math.*` / `toFixed`
- ❌ 不要在 DashboardPage 里新增其他 section / 改报告日 picker 逻辑
- ❌ 不要改 `dashboard-data-warning` / `dashboard-data-status-strip` / `FormalResultMetaPanel` 的 UI 结构（保留现有）
- ❌ 不要创建 worktree
- ❌ 不要跑全量测试
