# W2.1 · Executive schemas Numeric 过渡升级（additive union）

**目标一句话：** 把 `backend/app/schemas/executive_dashboard.py` 里 `ExecutiveMetric` / `AttributionSegment` / `PnlAttributionPayload` / `RiskSignal` / `ContributionRow` 的数字字段从 `str` 升级为过渡 union `Numeric | str`（附 validator 自动把 str 包成 display-only Numeric），保持向后兼容；service 层（W2.2）迁移完成后，Wave 4 再把 union 收紧到 `Numeric` only。

**所属波次：** Wave 2 · Executive 升级（Step 1，起点 task，所有 2.x 依赖它）
**依赖：** W1.1（`backend/app/schemas/common_numeric.py::Numeric` 必须存在）
**预计工时：** 30-45 分钟
**对应 Spec：** `docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md` § 3.3 / § 13.2

---

## Composer Subagent 约束（必须遵守）

你是一个专注执行单一 task 的 composer-2 subagent。

- 仅允许修改"允许修改的路径"白名单中的文件
- 遇到白名单外问题，用 `BLOCKER:` 前缀返回
- 不要运行 `git push` / `git reset --hard` / `git commit --amend` / `git rebase`
- 不要修改 `pyproject.toml`、不要安装新依赖
- 不要修改 `.cursor` / `CLAUDE.md` / `AGENTS.md` 等配置
- 不要修改任何 `backend/app/services/*.py`（W2.2 的事）
- 不要修改任何前端文件（W2.3+ 的事）
- 完成后返回：修改文件列表、测试命令输出摘要、越界/告警、遗留问题

---

## 允许修改的路径（白名单）

**Modify：**
- `backend/app/schemas/executive_dashboard.py` — in-place 升级字段类型并加 validator

**Create：**
- `tests/test_executive_dashboard_numeric_migration.py` — 新增专项迁移测试

**不得修改**任何其他文件。特别是：
- ❌ 任何 `tests/test_executive_dashboard_schema_contract.py`（这是旧合约测试，W2.2 再同步调整）
- ❌ 任何 `backend/app/services/*.py`
- ❌ `backend/app/schemas/common_numeric.py`（W1.1 已完成，不要碰）
- ❌ 任何前端文件

---

## 背景上下文

### 当前 `executive_dashboard.py`（你将要改的文件）

```python
class ExecutiveMetric(BaseModel):
    id: str
    label: str
    value: str          # ← 要改
    delta: str          # ← 要改
    tone: str
    detail: str

class AttributionSegment(BaseModel):
    id: str
    label: str
    amount: float           # ← 要改（合并 display_amount）
    display_amount: str     # ← 要改（合并进 amount）
    tone: str

class PnlAttributionPayload(BaseModel):
    title: str
    total: str              # ← 要改
    segments: list[AttributionSegment]

class RiskSignal(BaseModel):
    id: str
    label: str
    value: str              # ← 要改
    status: str
    detail: str

class ContributionRow(BaseModel):
    id: str
    name: str
    owner: str
    contribution: str       # ← 要改
    completion: int
    status: str
```

其他 class（`OverviewPayload` / `SummaryPayload` / `SummaryPoint` / `RiskOverviewPayload` / `ContributionPayload` / `AlertItem` / `AlertsPayload`）**不动**，它们的字段要么是 string label（不适用 Numeric），要么是容器。

### 过渡策略（关键）

为了让 service（W2.2 才改）仍然能传 str 通过 schema 校验，本任务把目标字段类型设为 **union `Numeric | str`**，并加一个 `@model_validator(mode="before")` 或字段级 validator，自动把传入的 `str` 值包装成：

```python
Numeric(raw=None, unit="yuan", display=<原 str>, precision=0, sign_aware=True)
```

这样 service 仍可构造 `ExecutiveMetric(value="+12.34 亿", ...)` 通过校验；frontend 拿到的 JSON shape 是完整 Numeric（有 raw/unit/display/precision/sign_aware 五字段，但 raw=null 表示"还没 typed 迁移完"）。W2.2 让 service 直接传真正 `Numeric(raw=..., ...)`，W4.x 再把 union 收紧。

**不使用 `Numeric | str` 之外的方案**（例如纯 str + 新类并存）。本过渡策略已在 design §13.2 锁定。

### `AttributionSegment` 的特殊处理

原 `{amount: float, display_amount: str}` 两字段要合并为 `amount: Numeric`。但为了向后兼容 service 的旧调用 `AttributionSegment(amount=1.23, display_amount="+1.23 亿", ...)`，加一个 `@model_validator(mode="before")`：
- 如果 input 同时有 `amount: float` 和 `display_amount: str`，合成 `amount = Numeric(raw=amount * 1e8, unit="yuan", display=display_amount, precision=2, sign_aware=True)`
- 如果 input 的 `amount` 已经是 dict/Numeric shape，直接用
- 去掉 `display_amount` 字段（schema 中不再暴露）

---

## 实现步骤

### Step 1：写迁移测试（新文件）

创建 `tests/test_executive_dashboard_numeric_migration.py`：

```python
"""W2.1 migration tests: schemas/executive_dashboard.py accepts both legacy str
and new Numeric shapes for governed numeric fields, and coerces legacy str into
display-only Numeric.
"""
from __future__ import annotations

from backend.app.schemas.common_numeric import Numeric
from backend.app.schemas.executive_dashboard import (
    AttributionSegment,
    ContributionRow,
    ExecutiveMetric,
    PnlAttributionPayload,
    RiskSignal,
)


class TestExecutiveMetricLegacyStr:
    def test_accepts_legacy_str_value_and_coerces_to_numeric(self) -> None:
        m = ExecutiveMetric(
            id="aum",
            label="资产规模",
            value="1,234.56 亿",
            delta="+2.34%",
            tone="positive",
            detail="...",
        )
        assert isinstance(m.value, Numeric)
        assert m.value.raw is None
        assert m.value.display == "1,234.56 亿"
        assert isinstance(m.delta, Numeric)
        assert m.delta.raw is None
        assert m.delta.display == "+2.34%"

    def test_accepts_native_numeric(self) -> None:
        value = Numeric(
            raw=123456000000.0,
            unit="yuan",
            display="1,234.56 亿",
            precision=2,
            sign_aware=False,
        )
        m = ExecutiveMetric(
            id="aum",
            label="资产规模",
            value=value,
            delta=Numeric(raw=None, unit="pct", display="无环比", precision=2, sign_aware=True),
            tone="positive",
            detail="...",
        )
        assert m.value.raw == 123456000000.0
        assert m.value.display == "1,234.56 亿"
        assert m.delta.display == "无环比"

    def test_roundtrip_json_keeps_numeric_shape(self) -> None:
        m = ExecutiveMetric(
            id="aum",
            label="资产规模",
            value="1,234.56 亿",
            delta="+2.34%",
            tone="positive",
            detail="...",
        )
        dumped = m.model_dump(mode="json")
        assert isinstance(dumped["value"], dict)
        assert set(dumped["value"].keys()) == {"raw", "unit", "display", "precision", "sign_aware"}
        assert dumped["value"]["raw"] is None
        restored = ExecutiveMetric.model_validate(dumped)
        assert restored.value.display == "1,234.56 亿"


class TestAttributionSegmentMergeAmountAndDisplay:
    def test_accepts_legacy_amount_plus_display(self) -> None:
        s = AttributionSegment(
            id="carry",
            label="Carry",
            amount=12.34,
            display_amount="+12.34 亿",
            tone="positive",
        )
        assert isinstance(s.amount, Numeric)
        assert s.amount.raw == 12.34 * 1e8
        assert s.amount.display == "+12.34 亿"
        assert s.amount.unit == "yuan"
        assert s.amount.sign_aware is True

    def test_accepts_native_numeric_amount(self) -> None:
        amount = Numeric(
            raw=1_234_000_000.0,
            unit="yuan",
            display="+12.34 亿",
            precision=2,
            sign_aware=True,
        )
        s = AttributionSegment(
            id="carry",
            label="Carry",
            amount=amount,
            tone="positive",
        )
        assert s.amount.raw == 1_234_000_000.0

    def test_serialized_shape_does_not_expose_display_amount(self) -> None:
        s = AttributionSegment(
            id="carry",
            label="Carry",
            amount=12.34,
            display_amount="+12.34 亿",
            tone="positive",
        )
        dumped = s.model_dump(mode="json")
        assert "display_amount" not in dumped
        assert isinstance(dumped["amount"], dict)
        assert dumped["amount"]["display"] == "+12.34 亿"


class TestPnlAttributionPayloadTotalNumeric:
    def test_accepts_legacy_str_total(self) -> None:
        p = PnlAttributionPayload(
            title="经营贡献拆解",
            total="+32.11 亿",
            segments=[
                AttributionSegment(
                    id="carry", label="Carry", amount=32.11, display_amount="+32.11 亿", tone="positive"
                ),
            ],
        )
        assert isinstance(p.total, Numeric)
        assert p.total.display == "+32.11 亿"
        assert p.total.raw is None


class TestRiskSignalValueNumeric:
    def test_accepts_legacy_str_value(self) -> None:
        r = RiskSignal(
            id="duration",
            label="久期风险",
            value="3.5 年",
            status="stable",
            detail="...",
        )
        assert isinstance(r.value, Numeric)
        assert r.value.display == "3.5 年"


class TestContributionRowContributionNumeric:
    def test_accepts_legacy_str_contribution(self) -> None:
        c = ContributionRow(
            id="rates",
            name="利率组",
            owner="按团队",
            contribution="+12.34 亿",
            completion=72,
            status="核心拉动",
        )
        assert isinstance(c.contribution, Numeric)
        assert c.contribution.display == "+12.34 亿"

    def test_completion_stays_int(self) -> None:
        c = ContributionRow(
            id="rates",
            name="利率组",
            owner="按团队",
            contribution="+12.34 亿",
            completion=72,
            status="核心拉动",
        )
        assert c.completion == 72
        assert isinstance(c.completion, int)


class TestBackwardCompatForExistingCallsites:
    """Legacy kwargs used by the current service layer must still succeed.

    W2.2 will switch service callsites to construct Numeric directly; until
    then these must continue to pass, otherwise the entire /ui/home/* stack
    would break.
    """

    def test_executive_overview_style_call(self) -> None:
        ExecutiveMetric(
            id="dv01",
            label="组合DV01",
            value="1,234,567",
            delta="+1.23%",
            tone="warning",
            detail="来自 bond analytics 风险快照...",
        )

    def test_pnl_attribution_style_call(self) -> None:
        PnlAttributionPayload(
            title="经营贡献拆解",
            total="+32.11 亿",
            segments=[
                AttributionSegment(id="carry", label="Carry", amount=12.1, display_amount="+12.10 亿", tone="positive"),
                AttributionSegment(id="roll", label="Roll-down", amount=-2.3, display_amount="-2.30 亿", tone="negative"),
            ],
        )
```

### Step 2：确认测试失败

```bash
python -m pytest tests/test_executive_dashboard_numeric_migration.py -v
```

**Expected：** FAIL，至少因为新测试里用 `Numeric` 类型的 assertion 失败（`isinstance(m.value, Numeric)` 而当前 `value: str`）。

### Step 3：升级 schema

完全**替换** `backend/app/schemas/executive_dashboard.py` 为以下内容：

```python
"""Executive dashboard payload schemas.

Wave 2.1 migrates governed numeric fields from ``str`` to a transitional
``Numeric | str`` union with a coercion validator. Callsites that still
pass raw display strings (service layer, pre-W2.2) are automatically
coerced into display-only Numerics (``raw=None``). Wave 4 will tighten
the union to ``Numeric`` only after service and frontend have migrated.

See ``docs/superpowers/specs/2026-04-18-frontend-numeric-correctness-design.md``
§ 3.3 / § 13.2.
"""
from __future__ import annotations

from typing import Any

from pydantic import BaseModel, model_validator

from backend.app.schemas.common_numeric import Numeric


def _coerce_display_numeric(value: Any) -> Any:
    """Coerce a bare display ``str`` into a display-only ``Numeric``.

    - ``str`` → ``Numeric(raw=None, unit="yuan", display=value, precision=0, sign_aware=True)``
    - ``dict`` with Numeric shape → passthrough (pydantic will validate)
    - ``Numeric`` → passthrough
    - other → passthrough (pydantic will raise)
    """
    if isinstance(value, str):
        return {
            "raw": None,
            "unit": "yuan",
            "display": value,
            "precision": 0,
            "sign_aware": True,
        }
    return value


class ExecutiveMetric(BaseModel):
    id: str
    label: str
    value: Numeric
    delta: Numeric
    tone: str
    detail: str

    @model_validator(mode="before")
    @classmethod
    def _coerce_legacy_str(cls, data: Any) -> Any:
        if not isinstance(data, dict):
            return data
        out = dict(data)
        if "value" in out:
            out["value"] = _coerce_display_numeric(out["value"])
        if "delta" in out:
            out["delta"] = _coerce_display_numeric(out["delta"])
        return out


class OverviewPayload(BaseModel):
    title: str
    metrics: list[ExecutiveMetric]


class SummaryPoint(BaseModel):
    id: str
    label: str
    tone: str
    text: str


class SummaryPayload(BaseModel):
    title: str
    report_date: str | None = None
    narrative: str
    points: list[SummaryPoint]


class AttributionSegment(BaseModel):
    id: str
    label: str
    amount: Numeric
    tone: str

    @model_validator(mode="before")
    @classmethod
    def _coerce_legacy_amount_display(cls, data: Any) -> Any:
        """Merge legacy ``{amount: float, display_amount: str}`` into ``amount: Numeric``."""
        if not isinstance(data, dict):
            return data
        out = dict(data)
        amount = out.get("amount")
        display_amount = out.pop("display_amount", None)

        if isinstance(amount, (int, float)) and isinstance(display_amount, str):
            # Legacy kwargs: amount is yi-denominated float, display is pre-formatted
            out["amount"] = {
                "raw": float(amount) * 1e8,
                "unit": "yuan",
                "display": display_amount,
                "precision": 2,
                "sign_aware": True,
            }
        elif isinstance(amount, str) and display_amount is None:
            out["amount"] = _coerce_display_numeric(amount)
        # else: amount is already dict / Numeric → let pydantic validate
        return out


class PnlAttributionPayload(BaseModel):
    title: str
    total: Numeric
    segments: list[AttributionSegment]

    @model_validator(mode="before")
    @classmethod
    def _coerce_legacy_total(cls, data: Any) -> Any:
        if not isinstance(data, dict):
            return data
        out = dict(data)
        if "total" in out:
            out["total"] = _coerce_display_numeric(out["total"])
        return out


class RiskSignal(BaseModel):
    id: str
    label: str
    value: Numeric
    status: str
    detail: str

    @model_validator(mode="before")
    @classmethod
    def _coerce_legacy_value(cls, data: Any) -> Any:
        if not isinstance(data, dict):
            return data
        out = dict(data)
        if "value" in out:
            out["value"] = _coerce_display_numeric(out["value"])
        return out


class RiskOverviewPayload(BaseModel):
    title: str
    signals: list[RiskSignal]


class ContributionRow(BaseModel):
    id: str
    name: str
    owner: str
    contribution: Numeric
    completion: int
    status: str

    @model_validator(mode="before")
    @classmethod
    def _coerce_legacy_contribution(cls, data: Any) -> Any:
        if not isinstance(data, dict):
            return data
        out = dict(data)
        if "contribution" in out:
            out["contribution"] = _coerce_display_numeric(out["contribution"])
        return out


class ContributionPayload(BaseModel):
    title: str
    rows: list[ContributionRow]


class AlertItem(BaseModel):
    id: str
    severity: str
    title: str
    occurred_at: str
    detail: str


class AlertsPayload(BaseModel):
    title: str
    items: list[AlertItem]
```

### Step 4：运行迁移测试通过

```bash
python -m pytest tests/test_executive_dashboard_numeric_migration.py -v
```

**Expected：** 所有新迁移测试 PASS（约 12 条）。

### Step 5：确认 service 层相关测试仍通过（关键回归）

```bash
python -m pytest tests/test_executive_service_contract.py tests/test_executive_dashboard_endpoints.py tests/test_executive_dashboard_schema_contract.py tests/test_result_meta_required.py tests/test_common_numeric.py -v 2>&1 | Select-Object -Last 40
```

**Expected：** 所有测试 PASS。**如果 `test_executive_dashboard_schema_contract.py` 或 `test_executive_service_contract.py` 因为 shape 变化 fail**，在返回 message 中以 `BLOCKER:` 前缀详细列出失败 assertion，**不要**自行修改这些测试文件（那是 W2.2 的事）。

如果 blocker 出现且是因为 contract test 断言字段是 `str`，汇报给主理决定是纳入本任务还是 W2.2 处理。

### Step 6：更大范围回归

```bash
python -m pytest -q 2>&1 | Select-Object -Last 30
```

**Expected：** 失败数相对于 Wave 1 tag 点（`numeric-correctness-w1-done`）**不增加**。如果增加了新 failing tests，`BLOCKER:` 前缀列出。

### Step 7：Commit

```bash
git add backend/app/schemas/executive_dashboard.py tests/test_executive_dashboard_numeric_migration.py
git commit -m "feat(schemas): migrate executive dashboard numeric fields to Numeric|str union"
```

---

## 验收 & 返回清单

- [ ] `test_executive_dashboard_numeric_migration.py` 12 条测试 PASS
- [ ] Step 5 相关 executive / schema / result_meta 测试无回归
- [ ] Step 6 全量 pytest 失败数相对 w1-done tag 无增加
- [ ] `git diff --name-only HEAD~1 HEAD` 严格等于：
  ```
  backend/app/schemas/executive_dashboard.py
  tests/test_executive_dashboard_numeric_migration.py
  ```

**返回 message 必须包含：**
1. 修改文件列表
2. Step 4 / Step 5 / Step 6 pytest 输出尾部
3. `BLOCKER:` 情况（如有；特别是 Step 5 contract tests 因 shape 变化 fail）
4. 遗留问题

---

## 不要做的事

- ❌ 不要改 `backend/app/services/*.py`（W2.2 的事）
- ❌ 不要改 `tests/test_executive_dashboard_schema_contract.py` 或 `tests/test_executive_service_contract.py`（如需改，`BLOCKER:` 上报）
- ❌ 不要给 `_coerce_display_numeric` 加"智能解析 display 字符串回 raw 数字"的逻辑（那是伪造数据）
- ❌ 不要删除 `OverviewPayload` / `SummaryPayload` 等未被本任务覆盖的类
- ❌ 不要引入新依赖
- ❌ 不要修改 `common_numeric.py`（W1.1 已完成）
