# Local-only production/preview override.
# Keep preview and static build pointed at the real backend unless you intentionally
# want a mock-only bundle for demos.
VITE_DATA_SOURCE=<REDACTED>
VITE_API_BASE_URL=
