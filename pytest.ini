[pytest]
addopts =
    --ignore-glob=.tmp*
    --ignore-glob=tmp*
    --ignore-glob=py_temp*
    --ignore-glob=py_verify
testpaths = tests
norecursedirs =
    .git
    .omx
    .venv
    .pytest_cache
    .ruff_cache
    .tmp*
    tmp*
    py_temp*
    py_verify
